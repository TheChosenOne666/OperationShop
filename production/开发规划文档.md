# 《街角百货铺》开发规划文档

> 版本 v1.22 · 2026-09-24
> （⚠️ 头部版本号曾停在 v1.14 未随 v1.15/v1.16 递增，本次一并校正）
> 上游文档：《需求文档》《玩法与技术方案》《系统 GDD · 经营与成长》
> 本文件是模块排期与验收的唯一依据；每个模块完成后停下来等确认，不自动进入下一个

> **v1.1 修订记录（2026-09-14）**：本文件多处残留"关店"功能（该功能已于同日被用户移除，铺位只剩 未开放 / 待开业 / 营业中 三态）。本次逐条清理，并同步推进顺序与数值待办的拍板结果。
>
> | 位置 | 原内容 | 处置 |
> | --- | --- | --- |
> | M02 交付物 | 新增接口含"关店" | 删除 |
> | M02 交付物 | 校验规则按 B5.3 调整 | 改为"无需调整"（关店移除后 prepared 单调递增） |
> | M02 验收 #6 | 满铺加成在关掉同层任一店后失效 | 改写为"同层出现未开放/待开业时加成不生效" |
> | M05 名称/交付物/验收 #4 | 含"关店" | 删除并替换 |
> | 待确认项 3 | 推进顺序待选 | 已拍板：M02 先行、M03 并行 |
> | 待确认项 5 | 项目根目录尚未 git init | 已解决，删除 |

> **v1.2 修订记录（2026-09-14）**：依据 `docs/M02-验收口径评审.md`（SC-M02-QA-001，M02 开工前评审）的结论与用户四项拍板，补齐 M02 的 4 项 P0 缺陷。
>
> | 位置 | 处置 |
> | --- | --- |
> | M02 校验规则 | 修正金币下界不变量：`coins >= initialCoins` → **`coins >= 0`**。v2 引入消费后原不变量会误杀合法存档（玩家一解锁铺位，金币即低于初始值 → 存档被拒加载） |
> | M02 验收 #3 | 明确为「幂等成功，`changed=false`」，取代原先含糊的"不重复扣费" |
> | M02 验收 #4 | 补**空段**（`visitors == 0`）语义，使逐段恒等式可判定 |
> | M02 验收 #5 | 冻结时机改为「当日**首次实际产生客人的**结算」（`openShopCount ≥ 1`），消除"客户端轮询先于玩家开店把首日上限冻成 20"的缺陷 |
> | M02 验收 | 新增 #8（待开业店铺禁止升级）、#9（并发）、#10（消费后存档仍能自证） |
> | M02 单元测试 | 补并发用例、空段用例、消费后自证用例 |

> **v1.3 修订记录（2026-09-14）**：依据 `docs/M02-验收口径评审.md` §6 复核结论（SC-M02-QA-002）与 SC-P2-004 / SC-P2-005 / SC-ARCH-006 的产出，把 M02 验收的**剩余依赖项落地**——原 #5 标注的「依赖 SC-P2-004 对"未冻结"表示方式的定义」已由 GDD v1.2 `§5.7` 结清，本轮去掉该待定标注并补上可判定依据；同时把 `unlockedSlots` 规格（GDD v1.3 `§3.7`）与「开店建首个分段」（GDD v1.3 `§4.3`）接入既有验收条目，**验收条数维持 11 条不变**（避免再次触发条数口径的传播链）。
>
> | 位置 | 处置 |
> | --- | --- |
> | M02 验收 #5 | **去掉「依赖 SC-P2-004」的待定标注**，改为引用 GDD `§5.7` 的 `capFrozen` 二态规格（未冻结 ⇒ 存档占位 `0` / 接口 `null` / HUD 不显示分母；已冻结 ⇒ 值落合法档位集合） |
> | M02 验收 #4 | 补「**开店（`prepare`）建立首个分段**」场景与 `prepare` 硬顺序（置 `prepared` → 重算满铺 → 建段），使触发满铺的新开业店只产生一条 bonus 价段 |
> | M02 验收 #2 | 补 `unlockedSlots` 加载校验口径（未知 / 跳序 / 重复 / 数量越界 ⇒ 存档损坏拒绝加载，见 GDD `§3.7` / `§5.5`） |
> | M02 交付物 · 存档 schema | `unlockedSlots` 规格指向 GDD `§3.7`；`dailyVisitorCap` 的「未冻结」表示指向 GDD `§5.7` |
> | M02 校验规则 | 补 `capFrozen` 二态校验（未冻结 ⇒ `dailyVisitorCap == 0` 占位；已冻结 ⇒ 落合法档位集合） |
> | M02 单元测试 | 补开店建段用例、`unlockedSlots` 加载校验用例 |

> **v1.4 修订记录（2026-09-14）**：用户三项拍板落地——**M02 暂不开工**（质量门已通过，保持待命）、**素材全部重制开工**（SC-M03-002）、**暂不提交**。
>
> | 位置 | 处置 |
> | --- | --- |
> | M02 行 | 状态改为「P0 已补齐，质量门已通过；**待用户拍板后开工**」（用户已选暂不开工） |
> | M03 行 | 状态改为「**重制已开工**」（SC-M03-002 产出：5 件程序化资产可用 / 12 件待外部生成 / 1 件字体被上游阻塞） |
> | M03 交付物 | 素材体积目标口径已改为**运行时压缩纹理（ASTC）形态**（《美术圣经》v1.2 §5.3.0）；首包 ≤2.27 MiB / 分包 ≤3.34 MiB / 全量 ≤5.61 MiB |
>
> ⚠️ **M03 的一个未决前置**：`《美术圣经》§5.4` 的「必须保留 PNG/JPG 默认图」若指**默认图随包分发**，则首包预算需从情形 A 切到情形 C（≈2.93 MiB）。已派 engineering-lead 核对 Cocos 3.8.8 发布微信小游戏时的默认图行为。
>
> **v1.5 修订记录（2026-09-15，SC-M02-001）**：用户拍板开工后，**M02 已实现**（规则 v2、存档 schema v2、解锁与升级两条命令、动态客流上限、账单分段、并发原子性），交付物与 11 条验收的**自验**结果见 `docs/M02-后端扩展.md` §七。本节仅更新状态，**不改动 M02 的任何交付物与验收口径**（条数维持 11 条）。
>
> | 位置 | 处置 |
> | --- | --- |
> | M02 行 | 状态改为「🔄 **实现已完成，待独立复核**（2026-09-15）」，并给出覆盖率与实现文档指针 |

> **v1.6 修订记录（2026-09-18，SC-M02-QA-004）**：**M02 的实现侧独立复核已完成**——质量门由复核方独立重跑（数字与实现文档一致），11 条验收逐条以**真实进程 + 真实 HTTP + 真实存档**复现，并另写 8 个独立探针（`P-A`~`P-H`）取证；结论见 `docs/M02-验收口径评审.md` §8。本节仍**只更新状态，不改动 M02 的交付物与验收口径**（条数维持 11 条）。
>
> | 位置 | 处置 |
> | --- | --- |
> | M02 行 | 状态改为「🟡 **独立复核已出具：11 条验收全部实测通过，判定为有条件放行**（2026-09-18）」，并登记 1 项待裁定（`P1-A`）+ 1 项建议修（`P1-B`）+ 4 项不阻塞的 `P2` |
> | 第八节 | 新增待确认项 9：`P1-A` 的裁定（改实现使空闲结算 `changed=false` 且不落盘，或改 GDD §5.7.2 删去该承诺）——**未裁定前 M02 不标记为已验收** |
>
> ⚠️ **`docs/M02-后端扩展.md` §7.2 #5 的「0 店开业不落盘」表述已在原处以勘误形式回修**（复核判定为误记，冻结行为本身正确）。`docs/当前状态与接手指引.md` 已于 `cc43ce7` 删除，见 §8.3 **P2-D**，是否有意删除待确认。

> **v1.7 修订记录（2026-09-18，SC-M02-QA-004 回修）**：用户裁定 **`P1-A` 改实现**、**`P1-B` 本轮一并修**，两处均已修复并用发现问题的同一批探针复验消失，端到端 11 条重跑全部维持通过，质量门全绿（覆盖率 `internal/game` 97.3% / `internal/api` 99.1%）。**M02 判定为通过（PASS）**。规格文本未改动——是实现向 GDD §5.7.2 对齐。详见 `docs/M02-验收口径评审.md` §8.5–§8.6、`docs/M02-后端扩展.md` §7.5。
>
> | 位置 | 处置 |
> | --- | --- |
> | M02 行 | 状态改为「✅ **已完成并通过独立复核**（2026-09-18）」，附保留意见 |
> | 待确认项 9 / 10 | 结清（已按裁定修复），保留原文以备追溯 |
> | M07 块 | 新增前置：`P2-B` 的分段数上界须成为云端存档的校验项 |
> | `架构现状.md` | 用户随即指派清 `P2-E` 文档债：**整篇按 M02 代码现状刷新**（标题改 M01+M02、§0 ADR 状态、§2 锁覆盖、§3 存档形态与自证清单重写、§4 错误码 6+9=15、§6 路由 5→7、§7.1 「未实现」清单改「已落地」+ 新增冲突 I/J、§8 补三条边界、§9 基线数字），头部带 v2 修订对照表 |
>
> ⚠️ **保留意见（流程）**：本次两项 P1 的发现、修复与复验由同一执行体完成，与「写验分离」不符；复验虽用的是原始探针与原始 e2e 脚本（未重写判据），但 48 行生产代码改动严格意义上未经独立第二人复核。若要零风险，进入 M04 前先对该 diff 单独走一次 code review。

> **v1.8 修订记录（2026-09-18）**：登记两项 M03 未决口径到 §八，并**查明其真实成因**（此前只写在 `assets/M03-素材投喂包.md` §4，本文件作为「排期与验收唯一依据」却未收录）。
>
> | 位置 | 处置 |
> | --- | --- |
> | 待确认项 13 | 新增：一层立绘交付尺寸 A `640×768` vs B 咖啡 `704×1024`／花束 `640×1024`。**查明分歧之根是取数基准不同**（A 统一一档生成预设 vs B 按 `美术圣经` §4.5 外框逐铺位取档），并**推翻投喂包 §4 原先"A 侧比例不自洽、B 更强"的推测性说法**；需美术先定「按外框还是按可绘区出图」 |
> | 待确认项 14 | 新增：外部生成件 13／12／9 三口径。**查明 12 与 9 均是算术错误而非分组口径**——写 12 的四处各自列举的条目数就是 13；写 9 的那行括号内加总为 12 且漏了 `ui_hud_bar`。连带发现程序化件数也有 5 与 7 两套（M03「已完成」分母同样不统一） |
> | §二 M03 行 / §5.1 | **本次未改数字**——两者都属上述待裁定的口径，裁定后按项 14 列出的 8 处一次性回改，避免先改一半制造新的不一致 |
>
> 证据行号（2026-09-18 实测）：`美术圣经` §4.5 L285、§5.3 L384/L385；`资产基准模板` §1.2 L73/L74、§3.7 L428、§3.8 L450、§3.10.1 L523/L525、§5.3 L726/L729；`素材处理记录` §3.1 L84、§9.3 L375。

> **v1.9 修订记录（2026-09-19）**：**M03 入库管线建成** → `tools/m03_ingest.py`（依 `素材处理记录` §2.1 的既有指引，从 `.work/` 移到 `tools/` 入库；依赖 Pillow + numpy，未新增第三方库）。
>
> | 位置 | 处置 |
> | --- | --- |
> | M03 行 | 状态改为「🔄 重制进行中——**入库管线已建成待投喂**」，并记管线处理链与自检覆盖面 |
> | M03 交付物 | 新增一项：可复现入库管线脚本 `tools/m03_ingest.py`（键控/定尺/自检/择优入库/回写进度） |
> | `assets/M03-素材投喂包.md` | 升 v1.1：§3 交付动作指向脚本并列出 4 条已自动把关项；**§4 两项冲突的成因改为本轮清点出的实测结论**（避免与本文件待确认项 13/14 说法相反） |
> | `assets/素材处理记录.md` | 新增 §2.4（管线方法与参数）与 §4.5（用脚本复检现存入库件的实测结果） |
>
> **管线建成后立刻测出的一项真实素材缺陷**（记入 `素材处理记录` §4.5）：现存 5 张店铺立绘不满足《资产基准模板》§1.8.3 的「等高」——内容顶/底边最大差 **17.5%**（阈值 ≤3%），`shop_coffee_open` 的内容高度只占画布 **46.2%**（`shop_bookstore_open` 为 80.8%）。**成因是逐张各自裁切**（咖啡那张从含上层住宅的 `8.jpg` 硬裁到单层），正好印证《美术圣经》附录 A.1「先立正视图基线、再派生」的阻塞项判断。**故本轮重制要求：五张立绘共用同一条顶边/底边基线，由 `check` 的组校验把关，不达标退回。**
>
> ⚠️ **另有两项口径待裁定**（不阻塞投喂，见待确认项 13 / 14）：一层立绘交付尺寸按「外框」还是「可绘区」；外部生成件与程序化件的分母。

> **v1.10 修订记录（2026-09-19）**：**M03 改道「占位图先行」，M04 的素材硬前置解除**（用户 2026-09-19 拍板「先用占位图做吧，我后面再把生成的图换进去」）。
>
> | 位置 | 处置 |
> | --- | --- |
> | M03 行 | 状态改为「🔄 重制进行中——**13 件路径已由占位图占住，等真素材替换**」；真素材进度仍是 0/13 |
> | M03 交付物 | 新增 `tools/m03_placeholder.py`（按投喂包 §1 尺寸生成 13 件占位图）；`tools/m03_ingest.py` 的 `check` 缺省扫描排除 `_archive/` |
> | M04 行 / §M04 前置 | 前置由「M03 素材已可用」改为「**M03 占位图已就位**」——路径/尺寸/透明语义/安全边距四项已钉死，可并行开工；真素材到位后原地覆盖，工程侧零改动 |
> | `assets/M03-素材投喂包.md` | 升 v1.2：§3 增「现在不急」说明、§5 表头标注 13 件当前全是占位图（**占位图不计 ☐→✅**）；§2 红线 1/2 同步校准后的键色与水印判据 |
> | `assets/素材处理记录.md` | 升 v1.2：新增 §2.5（占位图方法与两个自踩的坑）、§2.4 补判据第二次校准的实测数据、§4.5 补旧件归档去向 |
> | `design/art-bible/资产基准模板` §1.7 A11 | 补第四判据「幕布可定位」与校准后的平整度阈值（边缘带 L1 95 分位 <24） |
>
> **入库管线的判据校准（本轮第二处，实测驱动）**：`幕布平整` 前两版判据（四角 L1 离散度 <12 / 键色 ±192 带内均值）经三张真图实测**没有区分力**——真图 45.3／46.9／56.1，合成 8% 渐变才 60～72，几乎重叠，且一个角落杂物就能把平整幕布误诊成渐变。最终改为**边缘带像素到键色 L1 的 95 分位 <24**，由一次性取数脚本在三张真图上叠加五种合成退化校准（脚本与样本在 `.work/`，不入库；数据与过程见 `素材处理记录` §2.4）：真图 6／11／16，8% 渐变 31～55，15% 渐变 54～79，±6 噪点 17～22。`散块` 判据由「判退」改为**自动剔除 + 面积分级**（≤2% WARN、>2% FAIL），`主体完整` 改用清理后的掩膜包围盒。校准后三张真图各自**只因真正的问题被拦**：带水印的豆包 c1 与带角落杂物的自产图通过（杂物被自动剔掉），幕布上有溢出投影的豆包 c2 被「主体完整」拦下（投影与建筑相连，连通域抓不到）。
>
> **旧件处置**：属于 13 件清单的 8 件 interim 素材（5 立绘 + 2 空铺底图 + `ui_hud_bar`）`git mv` 到 `assets/_archive/m03-interim-2026-09/` 留作材质与暖光参考，原路径由占位图接管。

> **v1.11 修订记录（2026-09-19）**：**M04 启动——示踪弹打通 + 素材目录迁入 Cocos 工程**。
>
> | 位置 | 处置 |
> | --- | --- |
> | 素材目录 | 用户定案：素材**单份**存放于 `NewProject/assets/textures/`（引擎目录内）。`assets/textures/` 整体 `git mv` 进去（29 张 PNG）；`tools/m03_ingest.py` 的 `TEXTURES` 改指新位置；7 份文档 39 处路径同步。**归档件留在工程外** `assets/_archive/`——不会自动进包，但会被导入资产库、存在被误引用即进包的风险（仅 `ui_card_postcard.png` 即 972 KiB）。迁移后复跑 `check`：13 件 FAIL 0、等高组校验 0.1%，与迁移前逐项一致；**重新构建实测产物仍 2.83 MiB 逐目录不变 → 未被场景引用的资源不进包**。另注：《玩法与技术方案》B3.4 的目录约定本就按 Cocos 工程根书写，此次是归位而非另立 |
> | M04 行 | 状态改为 🔄 **进行中——工具链已打通**：手写 `Boot.scene` → CocosCreator CLI **无头构建 wechatgame 成功（48s）** → 微信开发者工具 `cli.bat open` → **模拟器实测渲染出场景清屏色**，链路有截图为证 |
> | 工程配置 | 设计分辨率 **750×1334 / policy 3（FIXED_HEIGHT＝Fit Height）** 已在构建产物 `src/settings.json` 中验证生效。真实键路径是 `general.designResolution`（顶层 `designResolution` 不生效，已踩实） |
> | ⚠️ AppID | Cocos CLI **无法**设置 appid：`--build "appid=…"` 与写 `settings/v2/packages/builder.json` 均不生效（真实参数在 `packages.wechatgame.appid`），产物始终是 Cocos 默认测试号，导致开发者工具报「登录用户不是该小程序的开发者」。当前靠**构建后补丁** `project.config.json` 解决 → **需要一个构建脚本固化「打包 → 补 appid/projectname → open」** |
> | 🔴 包体前提 | 空工程产物 **2.83 MiB**，其中 `cocos-js/` **2541 KiB 在包内**、`game.json` 无引擎声明；DevTools 另报该文件超 500KB 被跳过压缩与 ES6→ES5。**这与 `平台事实核对` §1.1.1「引擎不占主包」直接冲突**，且该结论原引的 §2.2 实为「自定义 Shader 限制」、属引用错位。已在 §1.1.1 标注两条前提待重核，**核实前不得据其做美术预算**；连带地，M04 接入素材（被场景引用）前必须先配好**纹理压缩（ASTC）**，否则进包的是无损源图而非压缩纹理 |
> | ~~未决~~ 已结案 | 构建日志的 `SIGTERM in task build-script` / 退出码 36：**是编译子进程收尾时被杀的信号，不是编译失败**。同一日志随后即 `run build task 打包脚本 success`；放入探针脚本 `BuildProbe.ts` 实测，其代码确实出现在产物 `assets/main/index.js`（1176 B）中。**排查教训**：Cocos 的工程脚本落在 `assets/main/index.js`，**不在** `src/chunks/bundle.js`——我最初只搜后者，据此误判为真失败，是检查方法错了 |

> **v1.12 修订记录（2026-09-19）**：**M04 构建入口固化为 `tools/m04_build.py`**，v1.11 记的「AppID 待固化成构建脚本」就此收口。
>
> | 位置 | 处置 |
> | --- | --- |
> | 新增 | `tools/m04_build.py`：无头构建 → 补 AppID/项目名 → 报包体 →（`--open`）打开开发者工具；`--size-only` 只量现有产物。**实测端到端**：构建后 appid 确实退回默认测试号，脚本自动改回 `wx5a00e2270436e6e0` |
> | 设计取舍 | ① **起始场景 uuid 从 `Boot.scene.meta` 读，不写死**——写死会在换机/重建后对不上；② **工具链路径不进仓库**，先读环境变量 `COCOS_CREATOR` / `WX_DEVTOOLS_CLI`，其次探测官方默认安装位置，都找不到就把两条路都打出来；③ AppID/项目名放 `config/client.json`（项目身份、非机密，AppID 本就随包公开），与服务器规则配置 `config/development.json` 分文件 |
> | 体积口径 | 脚本每次打印「合计 / 占 4 MiB 百分比 / 其中引擎占比」。当前实测 **2.83 MiB＝上限的 70.7%，其中引擎 2541 KiB 占 62.0%**。这是 M04 验收第 4 条的取数口，也是 #12（引擎计不计入主包）的量感依据 |
> | 未随做 | `--open` 分支未实跑（底层命令与手工验证过的 `cli.bat open` 完全一致，但没再弹一次窗口去确认）。构建退出码 36 的 `SIGTERM in task build-script` 仍未解释，脚本按「警告但继续查产物」处理，不静默放过 |
> **v1.13 修订记录（2026-09-19）**：**引擎是否计入主包——已结案，答案是「计入」**，v1.11 登记的 🔴 悬案解除。
>
> | 位置 | 处置 |
> | --- | --- |
> | 官方明文 | Cocos Creator 3.8《微信小游戏引擎插件使用说明》FAQ 逐字：「Q：启用引擎插件后，是否仍然会把引擎代码算入首包包体中？A：根据微信的规则，目前仍然会计算在内。」同页另明文「引擎插件提供的是完整版引擎……不会影响原有项目的包体」→ **启用引擎分离/插件不减包；减包手段是功能裁剪** |
> | `平台事实核对` §1.1.1 / §1.1.1.1 | 原「引擎运行时 = 0，不占主包」**判为错误并作废**（其标注依据 §2.2 实为「自定义 Shader 限制」，属引用错位）。✅ 结案块与重算后的预算已写入 |
> | 功能裁剪（实测） | 关掉 12 个 v1 用不到的模块（spine / spine-3.8 / dragon-bones / tiled-map / video / webview / profiler / physics-2d / intersection-2d / particle-2d / graphics / rich-text）后：**引擎 2541.1 → 1443.0 KiB（−43.2%）**，**产物 2897.2 → 1786.5 KiB**，占 4 MiB 由 70.7% → **43.6%**。配置落在 `NewProject/settings/v2/packages/engine.json` |
> | 🔴 预算后果 | 非美术基线由「估 400–800 KiB」变为**实测 1,786.5 KiB**（尚不含任何游戏素材）→ **主包美术可用 ≈ 2.26 MiB**，而《美术圣经》§5.3.0 的首包素材预算是 **≤2.27 MiB**：**余量约 0**，且未计 M04 之后新增的 TS 代码。**素材分包/远程包从"优化项"升级为"必做项"**，需回改《美术圣经》§5.3.0 与 M04 交付物（未擅自改美术预算，待主理人裁定） |
> | ⚠️ 待补验证 | 裁剪后的包在开发者工具中**是否仍正常出画未取得可视证据**（截图被其他窗口抢占）。裁掉的模块均不被"只有 Canvas + Camera 的空场景"使用，风险判断为低——但这是推理不是实测，**M04 摆第一个界面元素前必须补这次目视确认** |

> **v1.14 修订记录（2026-09-19）**：**M04 首条数据链路跑通**——`Boot` 组件 → `ApiClient` → 本地 Go 后端 → 界面渲染，控制台与截图双证。新增 `docs/M04-客户端工程与主界面.md` 承载细节。
>
> | 位置 | 处置 |
> | --- | --- |
> | 新增代码 | `NewProject/assets/scripts/Boot.ts`（开机取 config + mall 写入 Label；`dailyVisitorCap` 为 null 时显示 `—` 而非假的 0/0）；场景加 `Status` 文本节点并挂 Boot |
> | 实测证据 | 控制台 `[m04] 金币 1280　客流 0/—`、`[m04] 已取到服务端快照 revision=1 规则版本=m02-v2`；截图为米色画布上的 `#4A2F1E` 文本。**验收第 2 条（显示服务端真实值）在最小形态上成立** |
> | 🔴 关键坑 | **场景里脚本组件的 `__type__` 必须用编译期 class id，不是 `.ts.meta` 的 uuid**——填 uuid 会报 `Missing class` / `Script … is missing or invalid`。class id 可在一次构建后从 `assets/main/index.js` 的 `_RF.push` 注册表读出（它是 uuid 的确定性压缩形式）。故可行流程是「先构建拿 id → 再接场景」 |
> | 验证方法 | 走 **web-desktop 构建 + 本地静态服务 + 浏览器**，可完全无 GUI 地验证客户端逻辑与服务端契约。注意浏览器会发 CORS 预检，须给后端 `MALL_ALLOWED_ORIGINS`；**微信小游戏运行时无此约束**（adapter 把 XHR 映射到 `wx.request`），故这是环境差异而非代码 bug |
> | 操作教训 | 第一次 CORS 失败的真因是**旧后端进程仍占 18081**、新进程 bind 失败退出，而 `/healthz` 返回 200 造成「配置没生效」的假象。重启服务后必须看日志确认 `local development server started`，不能只看端口有响应 |
> | 顺带结案 | 上一轮挂的「裁剪后是否仍出画」已补证：web 与小游戏共用同一份 `engine.json`，裁剪后 2D/Label 渲染正常 |
> | 仍未做 | 微信开发者工具／真机内的首次取数（证据目前只来自 web-desktop）；`--open` 分支未实跑 |
>
> **v1.15 修订记录（2026-09-20）**：**主界面 Mall 场景与 HUD 五槽跑通渲染** + **素材分包改造落地**（用户 2026-09-19 拍板"现在就上分包"）。细节全部记在 `docs/M04-客户端工程与主界面.md` §7，本节只登记口径变化。
>
> | 位置 | 处置 |
> | --- | --- |
> | M04 交付物 | 新增两项：`Mall.scene`（B3.3 全节点树，49 节点）与 `MallScene.ts`（取数 + Bundle 加载 + 三态/HUD 渲染）；~~主界面尚未搭建~~ 已完成 |
> | 素材目录 | `NewProject/assets/textures/{shop,slt,prp,chr}` → `bundles/mall-art/`（Bundle `mall_art`，分包目标）、`{bg,ui,ico,state}` → `bundles/ui-main/`（Bundle `ui_main`，主包内）；`textures/app` 留在 Bundle 外。**`m03_ingest.py` 的 `POLICY[*]["subdir"]` 同步改为带 bundle 前缀**，改后 `check` 13 件 FAIL 0 项，入库管线未断 |
> | 🔴 新事实 | **Asset Bundle 不看引用、整目录进包**（与 `main` bundle 的"未引用不进包"相反）。实测：加 `ui_main` 全量 → 主包 72.0%；把 M06/M08 才用的 5 张预留件挪到工程外 `assets/_staging/` → **65.1%（2668.4 KiB）**，省回 280.3 KiB。**规矩：Bundle 目录里只放会被引用的素材** |
> | 验收第 2 条 | 由"最小形态成立"升级为**主界面成立**：金币 `1280`、客流 `0/—`、`解锁 $600`、`待开业`/`未开放` 全部取自服务端字段，截图为证 |
> | 验收第 4 条 | 主包数字有了持续取数口：**2668.4 KiB / 4 MiB = 65.1%**（含引擎 1443.0 KiB）。⚠️ 这是**未配 ASTC 的无损源图**口径，配压缩后需重测 |
> | ⚠️ 未结案 | 「分包标记落成 `subpackages/`」仍未通：Bundle 已生效但产物落在 `assets/mall_art`，`game.json` 无 `subpackages` 字段。已查明构建器代码读的是**构建任务里的 bundle 列表 `isSubpackage`**，而 CLI 只传 `platform/startScene` 时该列表取"上一次编辑器内的构建设置"——工程从未开过该面板，故从未置位。**需一次 GUI（或从面板导出 `configPath` 配置）取到真实结构后无头复现**。运行时不受影响（Bundle 名一致），故不阻塞 M04 其余条目 |
> | 新增前置 | `美术圣经` §5.3.0 首包预算需按 §7.2 的切分回改（主包常驻件 ASTC 6×6 ≈ **1231 KiB**、分包 ≈ 1903 KiB）；**未擅自改美术预算数字，待主理人裁定** |
>
> **v1.16 修订记录（2026-09-20）**：**M04 铺位三态材质 + Boot→Mall 场景切换跑通**，`架构现状` §10 的渲染方案就此落地。
>
> | 位置 | 处置 |
> | --- | --- |
> | 新增资产 | `NewProject/assets/bundles/ui-main/effects/slot-emissive.effect`（继承 `builtin-sprite` 渲染路径，暴露 `u_emissive`）；材质**运行时由 `Material.initialize({effectAsset, defines})` 建立**，不产 `.mtl` 资产——引擎安装内无 `.material` 样本可照抄，少一个未知格式 |
> | 三态实测 | 同屏两档亮度：S1 三铺压暗 0.30、S2 两铺 1.00，贴图正常采到。§10 首选方案成立，**不需要走"预烘 PNG 变体"降级** |
> | 🔴 实现约束（新坑） | 运行时建的材质 `defines` 不写时**默认全为 0**（引擎 `material.ts` 的 `IMaterialInfo` 注释），`#if USE_TEXTURE` 会走不采图分支、精灵变纯色块 → 必须显式传 `defines: { USE_TEXTURE: true }`。另：删掉 `#if` 分支改为恒定采样是**错路**，会让整条 pass 编译失败、连累同批精灵全部消失 |
> | 场景切换 | `Boot.ts` 拉到数后 `director.loadScene("Mall")`（延 0.2s，避开在 `start()` 里同步切场景）。**顺带修掉一处手写场景缺陷**：`Boot.scene` 的 `Canvas._components` 多列了一个属于 `Status` 节点的 UITransform，导致 Boot 被卸载时对象二次销毁、控制台报 `Error 5000`（`core/data/object.ts:372`）——Boot 作为唯一场景时从不暴露，切场景才炸。已删多余引用并复验零报错 |
> | 架构现状 §10 | 回填"已实现 + web 实测通过 + 上述实现约束"；⚠️ **真机复验仍未做**，该节的降级路径判据保持有效 |
> | 包体 | 主包 2668.4 → **2678.9 KiB（65.4%）**，着色器只多占 ≈10.5 KiB |
> | M04 剩余 | 只剩两件**不在代码里**的：ASTC 纹理压缩（当前进包为无损源图，已成欠账）、分包标记落成 `subpackages/`（需一次编辑器操作）。代码侧剩预制体与 HUD 安全区适配 |
>
> **v1.17 修订记录（2026-09-21）**：**M05 经营闭环实现完成，四条验收全部实测通过**（待独立复核）。细节全部记在 `docs/M05-经营闭环.md`，本节只登记口径变化。
>
> | 位置 | 处置 |
> | --- | --- |
> | M05 状态 | 改为 🔄 **已实现，待独立复核**：开店交互、5 秒结算轮询、客人走位/金币飘字/数字滚动、切后台停轮询/回前台取数四件齐全，端到端有截图与控制台双证 |
> | 后端 | **零改动**。开店/结算/快照三条接口 M01+M02 已备齐，`go test ./...` 复跑全绿 |
> | 新增客户端文件 | `MallStore.ts`（编排层，**刻意不 import `cc`** 以便用 `node --experimental-strip-types` 直跑断言，不为此引入测试框架）、`GuestStage.ts`、`NumberRoll.ts`；工具 `tools/m05_probe.ts`（19 项断言）、`tools/m05_web_drive.mjs`（无头 Chrome + CDP 端到端驱动，支持点击/连拍/合成前后台） |
> | ⚠️ 规格偏离（已裁定） | ① **开店入口做在主界面**点「待开业」铺位，而非设计稿的经营页——经营页属 M06，而 M05 验收第 1 条要能自证；② **`Guest` 不落 prefab**，代码建节点（同时推翻 M04 §6「绕不过预制体」那句判断）；③ **回前台发 `settle` 而非 B3.6 字面的 `GET /api/v1/mall`**——`Snapshot()` 明写不结算收益、不刷新业务日，照字面做拿不到"这段时间的客流"。三条均由主理人 2026-09-21 拍板，理由见 `docs/M05-经营闭环.md` §2 与 §9-⑧ |
> | ADR 0001 | 追加**口径澄清**：客户端可对服务端累计计数做差以决定"这轮画几位客人、进哪家店"，不含规则常量即不算数值推导。见该 ADR「口径澄清（M05 追加）」节 |
> | 素材 | `ico_coin.png` 从工程外 `assets/_staging/` 挪回 `ui_main` Bundle（《美术圣经》第 414 行指定它做飘字金币）。另：本模块一度误登记「`chr_guest` 不是占位图 ⇒ 真素材应为 3/13」，核对投喂包 §5 后**撤销**——那三件本就不在 13 件清单内，「真素材 0/13」原表述无误，详见 §八-16 |
> | ⚠️ 验收口径修订 | §M05 验收第 1 条「客流计数**减少** 1」按 §6.1 HUD 契约改写为「已到店 **+1**」（`visitorsRemaining` 相应 −1）。系 M01 措辞残留、非实现缺陷；2026-09-21 主理人裁定，过程见 §八-15 |
> | 包体 | 主包 2678.9 → **2717.6 KiB（66.3%）**，M05 增量 +38.7 KiB（`ico_coin` 26.1 + 新 TS ≈12.6）。⚠️ 仍是**未配 ASTC** 口径 |
> | 🔴 实测新坑 | ① 3.8 的 `Sprite` **没有 `flipX`**（2.x API），可翻的 `SpriteFrame.flipUVX` 是资产级、一改连累所有共用该帧的客人 → 只能用节点负 `scale`；② web-desktop 构建里点击**必须以 canvas 元素中心为原点**——`FIXED_HEIGHT` 下横向视野外扩而 Canvas 居中，按左边缘算会平移到场景之外，且"事件没送到"与"送错地方"在日志上长得一样；③ 错误横幅**不能只靠下一次成功更新来清**：空转响应被 revision 守卫丢弃、根本不触发更新，当日客流耗尽后横幅会永久卡在招牌上 → 改定时自清 |
> | ⚠️ 已知限制（未修） | 后端在**启动阶段**不可达时 `start()` 整体失败且不重试，界面停在「读取失败 NETWORK」直到重开游戏。M04 即此行为，不在 M05 交付物内，上线前要处理 |
> | M05 剩余 | 切后台/回前台的**真机复验**（web 侧 `EVENT_HIDE` 引擎明写不保证触发，本次为合成驱动）、§八-6 要求的**实机手感确认**（走位 1.6s / 飘字 0.8s / 滚动 400ms / 单轮 5 位四个时长都是本次定的） |
> | 🔍 交叉审查 `SC-M05-QA-001` | 把 diff 交给另一上下文窗口审查，要求只读代码与规格原文、不采信实现文档。**结论：无 P0**，但抓到三处：① 串行链会被一次 observer 异常**永久投毒**（轮询静默停摆、不报错）；② 守卫只比 revision，服务端重建档后界面**永久冻结在旧值**；③ **探针自证**——夹具把 `prepared` 一律写死 `true`，使「开店」「未开业不出客人」两条在断言自己造的假数据，19 项全绿却有一条根本没在验需求。①② 已修并各补一条用例（`84b2fa6`）；另**驳回**一条（质疑冷启动用 `GET /mall` 违反验收第 2 条——服务端在 settle 之前根本没产生那笔金币，客户端与服务端本就一致）。审查还翻出两条**文档级冲突**：B3.6 那句「拉快照时服务端会把这段时间的客流算出来」与 `Snapshot()` 不结算矛盾；未冻结时 HUD 显示 `0/—` 与 §6.1 要求的 `— / —` 不一致 → 已升格为 `架构现状` §7.2 冲突 **K**、**L** 交裁定。逐条处置见 `docs/M05-经营闭环.md` §13 |
>
> **v1.18 修订记录（2026-09-21，SC-M05-QA-002 + 两项裁定落地）**：M05 的**独立复核结论已出具**（有条件放行、无 P0），两条文档冲突 K/L 已裁定并落地，复核点名"该改实现"的五条已改。细节在 `docs/M05-验收口径评审.md`（复核方自写）与 `docs/M05-经营闭环.md` §14（逐条处置），本节只登记口径与状态变化。
>
> | 位置 | 处置 |
> | --- | --- |
> | M05 状态 | 🔄 **实现完成 + 独立复核已出具 = 有条件放行（无 P0）**。⚠️ **仍不等于"已通过验收"**：那次复核跑在另一个会话、只喂规格与 diff、明令不读实现文档，但 prompt 由实现方起草，按最严口径算**半独立**；且放行后的修复又回到同一执行体手里。保留意见全文见 `docs/M05-经营闭环.md` 头部①②③ |
> | 架构现状 §7.2 冲突 K | ✅ **结清（主理人裁定「改 GDD」）**：`玩法与技术方案` B3.6 该条原文改写为「回到前台立即**结算一次**（`POST /api/v1/mall/settle`）」，并补约束「这是写命令、会推进服务端状态并可能落盘，不得实现成心跳或高频触发」。M05 的 `resume()` 自此**与字面一致**，§9-⑧ 那条偏离登记同时撤销 |
> | 架构现状 §7.2 冲突 L | ✅ **结清（主理人裁定「改实现向 §6.1 对齐」）**：`MallScene.renderHud` 未冻结分支改为**先 `stopRoll` 再写 `— / —`**（不停补间的话在飞的 tween 会在 `onUpdate` 里把数字写回来），`Boot.render` 同口径。⚠️ 连带推翻 M04 已验收的 `0/—` 读数——`docs/M04` §1/§4 已加注；**2026-09-22 重建 web-desktop 产物后已重取**：运行时 Label 实测首帧 `— / —`、冻结后 `2/35`（取证 `docs/M05-经营闭环.md` §14.1）。顺带消掉 SC-M05-QA-002 P2-F 那条跨 0 点向下滚的观感问题 |
> | 客户端改动 | **P1-A**：`Arrival` 增加 `unitPrice`，`diffArrivals` 从**上一份已应用快照**取价，`GuestStage` 不再现取本次响应的 `unitPrice`（随之不可达的"快照里没有店铺"兜底一并删）。**P2-G**：`enqueue` 的出错分支把 `onStoreError` 包一层 try/catch，`run` 自此不会 reject——未按复核建议在 `tick()`/`resume()` 等三处各补 `.catch`，改在源头一处解决。**P2-D**：`ApiClient.fetchMall` 注释回改真实用途（只在启动首帧；回前台走 settle）。**后端与存档格式零改动**，`go vet` / `go test ./...` 复跑全绿 |
> | 工具改动 | **P1-B**：`m05_web_drive.mjs` 视口与 `DESIGN` 分离（新增 `VIEWPORT = 1280×1400`；产物 `build/web-desktop/index.html:26` 的 `GameDiv` 写死 1280×960，视口给 750 会把 x>146 的铺位触点裁掉）；每次 `click` 后等 1.5s 找 `[m05] 铺位/玩家请求开店` 日志，等不到计入 `silentClicks` 并**以退出码 1 报出**——原先是静默假阴性。**P2-E**：`burst` 字节数判据措辞改为「画面有变化，不可归因到 HUD」，并**补上 `hud` 步骤**——直接读运行中的 Label 字符串（金币/客流/招牌/在场人数/飘字文本），落盘 `hud-reads.json`，把这条缺口从"只能看像素"变成"能归因到显示值"（复验 P1-A 用的就是它）。**P2-B**：`m05_probe.ts` ⑥⑦ 组名与用例名显式标「仅编排层，不证验收 N」，⑦ 的未开业店累计值由 `0` 改为非零常量 5 使那条断言真的可能失败，另新增一条 P1-A 取价用例（**20 项全绿**；把实现改回取本次价会红，已做反向验证） |
> | 待裁定 | 新增 **§八-17**：复核提出的四项规格取向（验收 #1「5 秒内」措辞 / `unitPrice` 用途边界 / 飘字金额的验收观测点补进 M06 / ADR 0001 那条澄清的程序追认），附 `系统-经营与成长` §6.1「不显示分母」的措辞笔误 |
> | M05 剩余 | 两件，都不在代码里：① 切后台/回前台的**真机复验**；② §八-6 要求的**实机手感确认**。<br>✅ 原本列的第一件「重建 web-desktop 产物后复验 P1-A 与 `— / —`」已于 **2026-09-22 做完**：无头出包 7 秒、真实浏览器读到首帧 `— / —`、P1-A 现场飘字 `+6` 与服务端 `+24/4 位` 一致、`f1-s2` 点击送达断言成立。取证与配方见 `docs/M05-经营闭环.md` §14.1、§12-④ |
---

> **v1.19 修订记录（2026-09-23，SC-M06-IMPL）**：**M06 任务 1–8 全部完成**（实现与实测细节在 `docs/M06-页面与弹窗.md` §9.4/§10.2–§10.5），本节只登记口径与状态变化。
>
> | 位置 | 处置 |
> | --- | --- |
> | §二 M06 行 | 状态改为 🔄 **实现完成（任务 1–8 全过），独立复核未做**：五项裁定（D-1~D-5）落地、五屏按稿实现、十条验收逐条有实测读数；⚠️ 按写验分离口径，验收门仍要主理人另开会话只喂规格与 diff，本会话内的子代理审查只记「加强自查」 |
> | §三 M06 验收标准 | 维持原文不动（`docs/M06` §5 已按其具体化为十条，原文不重复） |
> | §八-17 | B/C/D 三条已于 2026-09-22 随裁定结清（上一轮已登记）；**A 条（「5 秒内」措辞）仍待裁**，原文未动 |
> | 环境事实登记 | ① 本机 `core.autocrlf=true` 使 `gofmt -l .` 在全 CRLF 工作区恒列全部文件——M06 起该判据的表述改为「LF 归一化后零输出」；② Windows Application Control 策略拦截 cgo 的 gcc，`go test -race` 在本机不可用（环境限制，非代码问题） |
>
> ⚠️ **M06 遗留待裁定**：`架构现状` §7.2 冲突 **O**——主界面点「待开业」铺位仍直接开店（M05 已验收行为），与 M06 §9.3「收口到经营页」的说法矛盾；代码按「不顺手改已验收面」未动，两条出路（收口 / 追认快捷入口）待主理人拍板。

> **v1.20 修订记录（2026-09-24，SC-M07-IMPL）**：**M07 任务 1–7 全部完成**（需求与技术方案在 `docs/M07-微信登录与云端存档.md`，实测在 §10），本节只登记口径与状态变化。
>
> | 位置 | 处置 |
> | --- | --- |
> | §二 M07 行 | 状态改为 🔄 **实现完成（任务 1–7 全过），独立复核未做**：双模鉴权 / 会话签发续期 / 账号级存档 / 分段 ≤6 加载校验 / 客户端自动重登重放全部落地并有实测读数；⚠️ 按写验分离口径，验收门仍要主理人另开会话只喂规格与 diff |
> | `P2-B` 结清 | 每店 `segments` ≤6 由「实现逻辑保证」升格为**存档加载硬校验**（越界判损坏、拒绝加载、原档保留），上界按 `1+ UpgradeCosts+1` 从配置推导而非写死。`schemaVersion` 维持 3（只加校验不加字段） |
> | §八 `P2-B` 前置 | 由「转入 M07 的前置」→ **已结清**（M07 任务 1） |
> | 上线清单前两条 | 「关闭开发令牌」「会话令牌具备过期与续期机制」标注 **代码侧已具备**（dev/wechat 双模开关 + TTL 会话），**待 M09 部署态确认**后勾选 |
> | 环境事实复核 | 沿用 v1.19 两条：① `gofmt -l .` 的 autocrlf 全量误报（对 git blob 的 LF 内容零输出为准）；② `go test -race` 本机被 Windows Application Control 拦截——**M07 竞态项未能实跑**，仅在 e2e 里以「10 路并发 prepare → revision 只 +1」间接证并发不重复，如实登记 |
>
> ⚠️ **M07 遗留（AppSecret 缺失所致，非代码缺口）**：wechat 模式的**真实**端到端两环未跑——① 真 AppSecret 换票（当前用假 Secret，服务端已确认真打微信 API 并正确拒 40125）；② 真机 `wx.login` 的自动重登重放（web-desktop 产物无 `wx`，dev 模式又不发 `SESSION_EXPIRED`）。均待 Secret 到手 / M09 真机补。

> **v1.21 修订记录（2026-09-24，SC-M06-QA-002）**：**M06 的独立验收门跑完**，总判定**有条件放行**。这道门按项目「写验分离」口径执行：主审会话未参与实现，复核员只喂 `docs/M06-页面与弹窗.md` **第 1–170 行（§1–§8，含六条裁定）+ 五张设计稿 + 上游规格**，明令不读该文件第二部分与本文的 M06 行、`架构现状` §7.2/§7.3。全文见 `docs/M06-验收口径评审.md`。
>
> | 位置 | 处置 |
> | --- | --- |
> | §二 M06 行 | 🔄「独立复核未做」→ **🟡 SC-M06-QA-002 有条件放行**（无 P0 / 无 P1；三条 P2：一条成立且根因在稿、一条对稿偏差、一条一半驳回为误报） |
> | 十条验收判定 | 7 条 ✅；验收 1 带对稿瑕疵；验收 4 / 10 本轮只判到「源码可判 + 补数」层（主理人选择静态复核，未重跑 e2e） |
> | 质量门与反向验证 | 复核方自跑：`go vet` 干净、`go test -count=1 ./...` 全绿（含 M07 的 `internal/session`）、探针 **28 项全通过**。两条反向验证证明断言非空转：删 `internal/game/service.go:744` 的 `+item.FloorBonus` → `TestShopViewPriceSplit` 红；把 `MallStore.ts:91` 飘字取价 `before.unitPrice` 改成 `shop.unitPrice` → 探针 26 通过 / 2 失败 |
> | 包体（验收 10） | 当前 HEAD（含 M07 合并）重跑 `tools/m04_build.py` = **2773.4 KiB = 4 MiB 的 67.7%**（assets 1133.4 / 引擎 1453.4 / src 28.4），**取代 v1.19 记的 2772.2 与修复轮的 2772.5**；+1.2 KiB 全在 assets 内的脚本编译产物（M07 客户端 +94 行），引擎与素材未动、零新素材。日志 `.work/qa-m06-size-post-m07.log` |
> | `架构现状` §7.2 | **新增冲突 R**（布局页同屏两分子，稿 01 的加成分子与 `floorFull` 矛盾）；**补登冲突 Q**——SC-M06-QA-001 记录声称已登记，实际本表当时停在 P，`git show e9c4279` 只改了文件头与 §7.3 各一行 |
> | §八 | 新增第 **18** 条：M06 复核提出的两条对稿口径待裁（002-01 / 002-02） |
> | 取证完整性 | 修复轮（SC-M06-QA-001）的复验件 `qa-m06-fix4/fix6/fix7`、`qa-m06-errors-codes.txt`、`qa-m06-final2-build.log` 随 worktree 删除**未回主仓库**；且该轮记的加成行读数「二层 `1 / 3`」与其自身 §10.3 的「仍 `0 / 3`」互斥（在盘 `runE.txt:18` 支持后者）→ 登记，e2e 结案时重取 |
> | 代码改动 | **本轮零代码改动**（复核发现的两条对稿问题需先裁定 + 改后要出包验证，避免复核对象漂移）。HEAD 仍为 `47dd74b` |

> **v1.22 修订记录（2026-09-24，SC-M06-QA-002 回修）**：主理人裁「两条对稿问题一起改掉」，两条已改并出包复验。细节在 `docs/M06-页面与弹窗.md` §10.9 与 `docs/M06-验收口径评审.md` §7.1，本节只登记口径与状态。
>
> | 位置 | 处置 |
> | --- | --- |
> | 002-01 结案 | 满铺加成卡**跟规则走**（分子 = 已营业数，判据不变），措辞「再开放 K 个铺位」→「K 家店**开业**」，与楼层标题「n / m 已开放」（已解锁数）分开。§3.2 需求侧已加更正注；**稿 01 印的数字本身不改**（改稿属主理人职权）。冲突 **R** → 已结清 |
> | 002-02 结案 | 经营页页内提示位移到顶栏右侧（与布局页同坐标），列表顶回稿值 336 ⇒ 末行底缘 1120 → **1098**，禁放余量 1px → **23px** |
> | 改动面 | 只 `MallPages.ts` 两处（字符串与坐标），零后端、零新素材；包体改前改后同为 **2773.4 KiB = 67.7%**，注释收敛后重出仍同数（`.work/qa-m06-fixF-wechat.log`，web-desktop 同步重出） |
> | 复验 | 当前 web-desktop 包 + 真实后端（隔离端口 18082/8124，按 §10.7 先查占用）：加成新文案读到、行位 +22、停后端 6 次失败金币恒 1515、整轮写命令 0；质量门 `go vet` / `go test` / 探针 28 项全过。取证 `.work/qa-m06-fix{A,B,C,D,E}-run.txt` |
> | 新增待查 002-04 | 页内提示位**可见性未取证**：`MallScene.ts:182` 只写给当时可见那一页、切页不重放，且横幅 3 秒自清 vs 轮询 5 秒节拍 ⇒ 失败提示可能落在看不见的页上或读取窗口外（数值与写命令行为不受影响）。修法待裁，已登记 §八-18④ |
> | §二 M06 行 | 状态位维持 🟡，「转 ✅ 前仍欠」清单更新为三条（e2e 结案 / 002-04 / 冲突 O） |

---

## 一、规划原则

1. **模块化推进，后端与单元测试优先**：每个模块按「后端接口 → 单元测试通过 → 客户端 → 联调验证 → 文档更新」的顺序走。
2. **每个模块必须有可独立验证的交付物**：说不清怎么验收的模块不进计划。
3. **完成一个模块就停下**，由你确认是否进入下一个模块。
4. **数值与规则不写进代码**，全部集中在规则配置里；调数值只改配置。
5. **没有设计图的页面不动工**，绝不脑补视觉。
6. **素材先处理再使用**：现有设计图全部是 JPG 假透明，抠图是独立的前置任务。
7. **文档随代码走**：任何新增/修改/删除都要同步更新对应文档，不留无文档的代码改动。

---

## 二、模块总览

| 模块 | 名称 | 前置 | 状态 |
| --- | --- | --- | --- |
| M01 | 本地开发后端（服务端权威闭环） | — | ✅ 已完成（2026-09-14） |
| M02 | 后端扩展：规则 v2、解锁与升级、动态客流 | M01 + 4 项 P0 补齐 | ✅ **已完成并通过独立复核**（2026-09-18，SC-M02-QA-004）。质量门由复核方独立重跑（`gofmt` / `go vet` / `go test -race` 全绿，覆盖率 `internal/game` 97.3% / `internal/api` 99.1%）；11 条验收逐条以真实进程 + 真实 HTTP + 真实存档复现通过。复核发现的 `P1-A`（空闲结算 `changed=true` 且落盘，违反 GDD §5.7.2）与 `P1-B`（`Config.Validate` 未禁止 `(floor,position)` 重复）已按裁定修复并用同批探针复验消失。**遗留**：`cmd/server` 无单元测试（沿 M01）；`P2-A`/`P2-C`/`P2-D` 为登记项不阻塞；`P2-B` 转 M07 前置；`P2-E`（`架构现状.md` M02 化）✅ 已于同日清账。详见 `docs/M02-验收口径评审.md` §8、`docs/M02-后端扩展.md` §7.5。⚠️ 保留意见：回修的发现与复验为同一执行体，48 行生产代码未经独立第二人复核（见 v1.7 记录） |
| M03 | 素材管线：抠图转透明 PNG 并整理入库 | M01（可并行） | 🔄 **重制进行中——13 件路径已由占位图占住，等真素材替换**（2026-09-19）：5 件程序化资产已入库可用；**13 件外部生成件全部由 `tools/m03_placeholder.py` 的占位图占位**（几何/尺寸/透明语义/安全边距逐件对齐投喂包 §1，`check` FAIL 0 项），**真素材进度仍是 0/13**；1 件字体被上游阻塞。**入库管线 `tools/m03_ingest.py`**：按图实测键色 → 去溢色 → 腐蚀去毛边 → 连通域剔散块 → 包围盒裁切 → 等比定尺 → 客观自检（尺寸/ASTC 体积/真透明/毛边/键色残留/贴边/正圆/纯黑/禁色带/描线色/原图短边≥1024/幕布可定位·平整·高饱和·覆盖）→ 择优入库 → 回写进度表；需人眼项一律列「人工待勾」不伪装通过。**建成后测出两项事实**：① 现存 5 张立绘「等高」不通过（顶/底边最大差 17.5%，阈值 ≤3%）——印证必须先立正视图基线再派生，已随 interim 件归档；② 幕布平整判据前两版无区分力，经合成退化样本重新校准（见 v1.10 记录）。见 v1.4 / v1.9 / v1.10 记录、`素材处理记录` §2.4 / §2.5 / §4.5 |
| M04 | 客户端工程与主界面（只读联调） | M02、M03 | 🔄 **进行中——工具链已打通**（2026-09-19，见 v1.11 记录）：手写 `Boot.scene` → CocosCreator CLI 无头构建 wechatgame 成功（48s）→ 微信开发者工具 `cli.bat open` → 模拟器实测渲染出场景清屏色。设计分辨率 750×1334 / policy 3（Fit Height）已在产物中验证。**未完**：AppID 由 `tools/m04_build.py` 构建后自动补丁（CLI 覆盖不了，已固化）；**引擎计入主包已结案**（官方 FAQ 明文），功能裁剪后引擎 2541 → 1443 KiB、产物占主包 43.6%；但由此算出**主包美术可用仅 ≈2.26 MiB**，与《美术圣经》首包 ≤2.27 MiB 预算几乎无余量 → **素材分包/远程包升级为必做项**（见 v1.13）；**首条数据链路已跑通**（v1.14）：Boot 组件 → ApiClient → 本地 Go 后端 → 渲染，有控制台与截图双证；**主界面 Mall 场景与 HUD 五槽已跑通渲染**（v1.15，2026-09-20）：B3.3 全节点树 49 节点、两层 5 铺位三态文字与锁、HUD 金币 `1280` + 三按钮 + 客流 `0/—` 全部取自服务端字段并有全页截图为证；素材已按分包改造（Bundle `mall_art` 分包目标 / `ui_main` 主包内），主包实测 **2668.4 KiB = 65.1%**。**未完**：ASTC 纹理压缩（图已真进包，当前是**无损源图**口径，已成欠账）、分包标记落成 `subpackages/`（需一次编辑器操作取结构，见 v1.15）、`Slot`/`ShopView`/`Hud`/`Panel` 预制体、HUD 底部固定与 iOS 安全区适配（B3.2）、以及**三态材质的真机复验**（§10 硬性要求，目前只有 web 证据）。AppID 由 `tools/m04_build.py` 构建后自动补丁（CLI 覆盖不了，已固化）。
| M05 | 经营闭环：开店 / 客人到店与收益表现 | M04 | 🟡 **独立复核已出具：有条件放行（无 P0）**（2026-09-21，SC-M05-QA-002，见 v1.17/v1.18）。复核方自己重跑质量门（`gofmt`/`go vet`/`go test -race` 全绿、`internal/game` 覆盖率自采 97.3%、**后端零改动经 `git diff` 核实为真**），四条验收在真实进程 + 真实 HTTP + 真实浏览器上各自复现：#2/#3/#4 **PASS**，#1 实质达成但字面「5 秒内」不可达（判据写错了单位，见 §八-17）。⚠️ **本门仍未闭合**：复核会话虽跑在另一个上下文、只喂规格与 diff 且明令不读实现文档，但 prompt 由实现方起草 → 按项目最严口径算**半独立**；且复核后的修复（P1-A 取价、P1-B 驱动器、裁定 L 的 HUD 改动）又由原实现体完成、未经再一轮复核。真门要主理人自己新开会话、只贴规格路径与 diff 区间。此前自验记录（点 `f1-s1` 开店 → 1280→1286、已到店 0→1、上限冻结 35；刷新重进逐字段一致；`revision` 守卫丢弃空转与乱序；只开咖啡时到店全归咖啡）与放行条件逐项处置见 `docs/M05-验收口径评审.md`、`docs/M05-经营闭环.md` §14。**剩两件不在代码里**：① 切后台/回前台真机复验；② §八-6 实机手感确认。<br>✅ 2026-09-22 已重建 web-desktop 产物复验：首帧 Label 实测 `— / —`、P1-A 现场飘字 `+6` 与服务端 `+24/4 位` 一致、`f1-s2` 点击送达断言成立（`docs/M05-经营闭环.md` §14.1） |
| M06 | 页面与弹窗：经营页、布局页、店铺详情、解锁升级确认 | M02、M05 | 🟡 **独立复核 SC-M06-QA-002：有条件放行**（2026-09-24；实现完成任务 1–8 于 2026-09-23）：经营页/布局页/三弹窗按 @750 稿代码建节点（P-1 甲），`hud` 读数逐屏对稿；后端补 `unitPriceBase`/`floorBonus`/`nextUnitPrice`/`todayEarned` 四个派生字段（D-1/D-2/D-3 裁定「三个都补」，`schemaVersion` 3、开发期删档）；写命令结果码日志结清冲突 **E**；异常态（金币不足两路径/满级/未开业/未解锁/失败保留成功值）真实进程 e2e + 六拒绝码 curl 复现；包体在含 M07 合并的同一 HEAD 重跑为 **2773.4 KiB = 67.7%**（零新素材，P-2 甲兑现）。复核实测：**无 P0 / 无 P1**，质量门与探针 28 项由复核方自跑，两条反向验证证明断言非空转。**裁定后两条对稿问题同日已改并出包复验**（v1.22：加成卡措辞按开业口径、与「已开放」分开；页内提示位移顶栏、列表回稿值 336 ⇒ 禁放余量 1px → 23px；包体改前改后同数 2773.4 KiB）。**转 ✅ 前仍欠**：① 一次 e2e 结案（验收 1 的转灰度可分 + 验收 4 的同场对账 + 六码的 UI 侧实到）；② 新登记的 **002-04** 页内提示位可见性缺口待裁修法（§八-18④）；③ 冲突 **Q** 与修复轮的复验读数不在盘（e2e 结案时一并重取）。另核账两处：上一轮声称登记的冲突 **Q** 实际从未写进 `架构现状`（本轮补登）、其修复轮取证件随 worktree 删除不在盘。遗留：冲突 **O**（主界面待开业铺位直开 vs §9.3 收口说法）待主理人裁定；ASTC 与真机复验沿用 M04/M05 遗留批次。详见 `docs/M06-验收口径评审.md` |
| M07 | 微信登录与云端存档 | M02、M05、**AppID** | 🔄 **实现完成（任务 1–7 全过），独立复核未做**（2026-09-24）：后端 `internal/session`（换票 + 进程内会话）+ `internal/api` 双模中间件（`MALL_AUTH_MODE=dev/wechat`）+ 账号级存档目录 + 分段 ≤6 加载校验（结清 `P2-B`）+ `cmd/server` 双模启动 + 三个新错误码全落地；客户端 `ApiClient`（`ensureSession`/微信登录/`SESSION_EXPIRED` 自动重登重放一次）+ `ApiTypes.SessionResponse` + `Boot` 先登录再取数 + `m04_build` 注入 `authMode`（`MallStore`/探针零改动）。质量门：`gofmt`（LF 归一）/`go vet`/`go test -count=1` 全绿，`-race` 本机被拦（沿用环境事实）。实测：dev 模式会话 e2e 23/23（登录换票 / 账号隔离 / 过期→`SESSION_EXPIRED`→重登状态不丢 / 并发 prepare 只提交一次 / 原型单档未被污染 / 模式护栏）+ web-desktop dev 回归（产物含 M07 标记、hud `金币=1280` 鉴权直通、settle POST 走新 `request`）+ 探针 28/28。⚠️ 仍不等于已验收（写验分离：验收门要主理人另开会话只喂规格与 diff）。wechat 模式真实端到端（真 AppSecret 换票、真机 `wx.login` 自动重登）因 AppSecret 缺失留待后续（见 `docs/M07` §10）。细节见 `docs/M07-微信登录与云端存档.md` §9.4/§10 |
| M08 | 好友经营排行榜（开放数据域） | M07 | 待启动 |
| M09 | 打磨与提审上线准备 | 全部 | 待启动 |

---

## 三、模块明细

### M01 本地开发后端 ✅ 已完成

- **目标**：把「开店 → 客流 → 收益 → 业务日刷新」的服务端权威闭环在本机跑通。
- **交付物**：`cmd/server`、`internal/game`、`internal/api`、`config/development.json`、`docs/M01-本地开发后端.md`。
- **验收结果**：`gofmt` / `go vet` 无告警，`go test -race` 全绿，`internal/game` 覆盖率 96.2%、`internal/api` 94.8%，真实进程端到端联调 17 项场景全部符合预期。
- **遗留**：`cmd/server` 靠端到端联调覆盖，无单元测试。

### M02 后端扩展：规则 v2、解锁与升级、动态客流

- **目标**：把《玩法与技术方案》A2–A5 的规则实现到服务端，并保持 M01 的存档自证基线。
- **设计依据**：`design/gdd/系统-经营与成长.md`（Phase 2 首份逐系统 GDD，覆盖 G1–G5，含 config schema、公式、边缘情况与验收对应表）。
- **交付物**：
  - 规则配置 v2（铺位解锁价格与顺序、等级单价表、满铺加成、基础客流 + 单店客流）
  - 存档 schema v2（`unlockedSlots`、`dailyVisitorCap`、`spent`、`level`、`segments`）
    - `unlockedSlots`：序列化与加载校验规格见 GDD `§3.7` / `§5.5`（无重复、构成 `unlockOrder` 前缀的 `slotId` 数组；未知 / 跳序 / 重复 / 数量越界 ⇒ 存档损坏、拒绝加载）
    - `dailyVisitorCap`：与 `capFrozen`（bool）配对，**「未冻结」的表示方式见 GDD `§5.7`**（存档 `capFrozen == false` ⇒ 落盘占位 `0`；接口层转 `null`；**不得用 `0` 作哨兵**）
    - `segments`：每店分段数上界 **≤ 6**（1 初始 / 开店 + 4 升级 + 1 满铺），见 GDD `§4.3` / `§5.6`
  - 新增接口：解锁铺位、升级店铺
  - 校验规则：**「未开业店铺不得有累计客流」无需调整**（关店移除后 `prepared` 单调递增，M01 现有校验即最终形态）；但 **金币下界不变量必须修正**——M01 的 `state.Coins < s.cfg.InitialCoins → 拒绝`（`internal/game/service.go:219`）在 v2 会**误杀合法存档**：玩家一解锁铺位，金币即低于初始值，存档被判定损坏、拒绝加载。v2 放宽为 **`coins >= 0`**，且金币恒等式改为 `coins == initialCoins + Σrevenue − spent`
  - 校验规则（新增）：**客流上限二态校验**（GDD `§5.7`）——`capFrozen == false` ⇒ `dailyVisitorCap` 必须 `== 0`（占位，读取方忽略）；`capFrozen == true` ⇒ 必须落在合法档位集合 `{baseVisitors + visitorsPerShop × k | k ∈ [1,5]}`。**不得**用 `dailyVisitorCap` 的取值反推冻结状态
  - 单元测试：正常路径 + 边界（金币不足、跳过解锁顺序、重复解锁幂等、满级再升级、对「待开业」店铺升级被拒、解锁后当日客流不变、单价变化时账单分段正确、**空段（`visitors == 0`）语义**、**开店建立首个分段（含触发满铺时只产生一条 bonus 段）**、**`unlockedSlots` 加载校验（未知/跳序/重复/越界）**、**并发解锁/升级只提交一次**、**消费后存档仍能通过自证**）
- **验收标准**（每条含**前置条件**，便于直接转成测试用例）：
  1. **前置**：某二层铺位处于「未开放」，且金币不足其 `unlockCost`。**期望**：解锁失败返回 `INSUFFICIENT_COINS`，金币与存档零变化。
  2. **前置**：衣橱（`unlockOrder=1`）仍处于「未开放」。**期望**：直接解锁甜屋（`unlockOrder=2`）被拒绝（`SLOT_ORDER`），零变化。**加载校验**：存档 `unlockedSlots` 含未知 `slotId` / 跳序（缺 `unlockOrder=n-1`）/ 重复项 / 数量越界，均判定为存档损坏并拒绝加载（见 GDD `§3.7` / `§5.5`）。
  3. **前置**：某铺位已解锁。**期望**：重复解锁为**幂等成功**（`changed=false`），不重复扣费、不写存档。
  4. **前置**：店铺处于「营业中」且当日已产生客流。**期望**：升级后新产生的收益按新等级单价记账，历史账单不被改写，且每一段 `revenue == visitors × unitPrice` 成立（**含 `visitors == 0` 的空段**）。
     - **开店建段（新增）**：**前置**：店铺处于「待开业」。**期望**：开店（`prepare`）时建立**首个分段**，其 `unitPrice` 取重算满铺后的值；`prepare` 的硬顺序为「置 `prepared` → 重算满铺 → 建/append 段」，**不得调换**。若该店恰好触发所在层满铺，则它**只产生一条 bonus 价段**（`unitPrice = 等级单价 + 满铺加成`），**不得**出现 spurious 的 pre-bonus 空段；同层其他已开业店各 append 一条新段。
  5. **前置**：当日尚未产生任何客人（`openShopCount == 0`）。**期望**：当日上限**不冻结、不落盘**；在首次实际产生客人的结算（`openShopCount ≥ 1`）时才冻结为 `dailyVisitorCap`；冻结后中途再开店不改变当日上限，次日生效。
     - **「未冻结」的判定依据（GDD `§5.7`）**：以 `capFrozen`（bool）为**唯一判据**。`capFrozen == false` ⇒ 存档 `dailyVisitorCap` 落盘占位 `== 0`、接口返回 `null`、HUD 显示占位 `— / —` 且**不显示分母**；`capFrozen == true` ⇒ 值落合法档位集合 `{baseVisitors + visitorsPerShop × k | k ∈ [1,5]}`，HUD 显示 `已到店 / 上限`。**不得**用 `0` 作哨兵、不得用 `dailyVisitorCap` 的取值反推冻结状态。
  6. **前置**：某层至少有一个铺位处于「未开放」或「待开业」。**期望**：该层满铺加成不生效（0）；当该层全部铺位处于「营业中」时加成生效（+1）。
  7. **前置**：某铺位处于「未开放」。**期望**：其累计客流与累计收益恒为 0（该不变量在解锁前后均须成立）。
  8. **前置**：店铺处于「待开业」。**期望**：升级被拒绝返回 `SHOP_NOT_OPEN`，且状态零变化。
  9. **并发**：
     a. **并发解锁同一铺位**：只提交一次、只扣一次费；另一请求为幂等成功或明确拒绝。
     b. **并发升级同一店铺**：只提交一次、只扣一次费。
     c. **并发解锁与升级**：两笔互不干扰、各自正确扣费，`coins` 与 `spent` 守恒。
     d. 解锁与升级必须与 `prepare` 共用同一套互斥锁与"先落盘再发布"语义。
  10. **消费后存档仍能自证**：解锁/升级使 `coins < initialCoins` 之后，存档仍被正常加载（下界为 `coins >= 0`）。
  11. `gofmt` / `go vet` / `go test -race` 全绿。
- **风险**：存档 schema 变更会让旧存档被拒绝加载（预期行为）；开发期直接删档。
- **收尾**：更新 `docs/M01-本地开发后端.md` 或另立 `docs/M02-*.md`，并更新 `design/gdd/玩法与技术方案.md` 中与本模块相关的实现状态。

### M03 素材管线

- **目标**：把现有设计图处理成引擎可直接使用的规范素材，并明确缺口。
- **交付物**：
  - `NewProject/assets/textures/` 下按约定目录与命名整理的透明 PNG（店铺立绘、空铺位底图、顾客、UI 底板、图标）
  - 每张图的处理记录：原图 → 输出 → 尺寸 → 压缩后体积
  - **可复现入库管线脚本 `tools/m03_ingest.py`**（2026-09-19 建成）：品红键控 → 去溢色 → 腐蚀去毛边 → 包围盒裁切 → 等比定尺 → 客观自检 → 多候选择优 → 入库 → 回写投喂包进度；`spec` 子命令另责成「投喂包尺寸/体积目标 ↔ 美术圣经 ASTC 算式」的对账
  - 素材缺口清单（见第五节）确认版
- **验收标准**：抠图边缘干净无白边；尺寸统一到引擎规格；单张压缩后体积满足包体预算；导入引擎后无多余棋盘格残留。
- **风险**：AI 生成图边缘常有阴影与碎屑，需要人工抽检；部分素材（如楼梯、遮阳伞）只有主界面合成图里有，需要单独重出。
- **说明**：本模块与 M02 无依赖，可与 M02 并行推进。**已于 2026-09-14 并行启动。**

### M04 客户端工程与主界面（只读联调）

- **目标**：Cocos 工程能构建出微信小游戏，主界面按设计图还原，并能读到服务端的真实数据。
- **交付物**：
  - 工程配置：设计分辨率 750×1334、适配策略、构建目标设为微信小游戏、AppID 配置
  - `Boot` / `Mall` 两个场景 + `Slot` / `ShopView` / `Guest` / `Hud` / `Panel` 预制体
  - 启动时拉取 `/api/v1/config` 与 `/api/v1/mall` 并驱动界面
  - 构建产物能在微信开发者工具中打开
- **验收标准**：
  1. 主界面与 `主界面图.jpg` 逐项比对一致：招牌、两层铺位顺序、底部 HUD 三段布局、客流分母与规则一致。
  2. 金币与客流显示的是服务端返回的真实值，不是写死的假数据。
  3. 未解锁铺位显示为空铺位素材。
  4. 构建产物的主包体积有明确数字，并记录在文档里。
- **前置**：M02 的字段（解锁状态、等级、客流上限）已就位；M03 的 13 件素材路径**已由占位图占住**（2026-09-19 改道，见 v1.10 记录）——路径、像素尺寸、透明/不透明语义、安全边距四项可直接依赖。
  ⚠️ **占位图不是素材**：验收标准第 1 条「主界面与 `主界面图.jpg` 逐项比对一致」**在占位图下必然不成立**，M04 该条须等真素材替换后再判；其余三条（真实数值、空铺位素材、主包体积数字）不受影响，可照常验收。
- **风险**：这是本项目第一次用 Cocos Creator，构建链路、适配、包体积都是未知项，预留调试时间。

### M05 经营闭环

- **目标**：玩家能在主界面开店，并看到客人到店与收益增长。
- **交付物**：开店的交互；主界面每 5 秒结算轮询；客人走位、金币飘字、数字滚动补间；切后台停止轮询、回前台立即拉快照。
- **验收标准**：
  1. 开店后 5 秒内能看到第一位客人到店，金币增加、客流「已到店」**+1**（服务端 `visitorsRemaining` 相应 −1）。
     > 原文写「客流计数减少 1」，是 M01 时代 HUD 直接显示 `visitorsRemaining` 的措辞；M02 起 HUD 契约改为「已到店 / 上限」（`系统-经营与成长` §6.1），到店时该数递增。**2026-09-21 主理人裁定按 HUD 契约改写**，过程见 §八-15。
  2. 完全退出小游戏再进入，金币、客流、店铺开关状态完全一致。
  3. 界面数值永远以最近一次服务端响应为准，不出现金币跳变或回退。
  4. 未开业的铺位不产生收益，也不消耗客流。
- **风险**：客人走位与轮询节奏不同步会出现"看到客人却没有收益"的错觉，需要把表现对齐到服务端返回的到店数。

### M06 页面与弹窗

- **目标**：补齐经营页、布局页、店铺详情、解锁/升级确认。
- **前置**：设计稿**已就绪**（`design/ui-mockups/` 六屏，2026-09-14 完成）。~~原先"设计图缺失"的阻塞已解除。~~
- **交付物**：经营页（各店状态与当日收益）、布局页（铺位解锁总览与下一铺位花费）、店铺详情弹窗（等级、单价、累计数据 + 升级入口）、解锁与升级确认弹窗。
- **验收标准**：逐屏与设计图核对字段、文案、布局、按钮状态；金币不足、已满级等异常态有明确提示且不出错。
- **风险**：~~无图动工必然返工~~ **已缓解**——六屏设计稿已完成并逐屏渲染验证；剩余风险是设计稿与最终 UI 资产（正式字体、重制后的立绘）之间的落差，需在实现时以设计稿的尺寸与色值为准。

### M07 微信登录与云端存档

- **目标**：从本地开发令牌切到真实微信登录，存档上云。
- **前置**：微信小游戏 AppID **已就绪**（`wx5a00e2270436e6e0`，名称「街角百货铺」）；但工程尚未把 AppID 写入构建配置，属 M04 的活。类目是否支持目标玩法仍需确认。
- **交付物**：`code2session` 换 openid 与会话；客户端登录流程；存档云端化；本地开发令牌与"仅本机访问"限制的开关化。
- **验收标准**：
  1. 换设备或清缓存后，用同一微信账号进入，数据与服务端一致。
  2. 同一账号并发请求不会重复结算或重复扣费。
  3. 会话过期后能自动重新登录，不丢数据。
- **风险**：存档从单文件迁到云端会影响 M01 已有的原子替换与锁语义，需要重做并发与持久化方案。
- **新增前置（SC-M02-QA-004 `P2-B` 转入）**：一旦服务端开始接收**外部提交**的存档，GDD §5.6 的「每店分段数 ≤ 6」必须升格为加载校验项。M02 阶段该上界只由实现逻辑保证（`floorFull` 不可逆、等级单价只增、同价不 append），`validateState` 经实测**接受**一份 20 段但逐段恒等式自洽的存档——本地单账号自产档无风险，云端入口不加此校验即等于放开「用任意分段数构造合法账目」的面。同期需一并复核 §5.5 其余校验项在外部输入下的强度。

### M08 好友经营排行榜

- **目标**：玩家能看到自己与微信好友之间的金币排行榜。
- **前置**：M07 完成；开放数据域与好友授权的规则**动手前必须核对官方最新文档**。
- **交付物**：主域上报权威金币到微信托管数据；开放数据域内渲染排行榜；无好友时的降级显示。
- **验收标准**：能拉到好友并正确显示自己的名次；一个好友都没有时显示自己的分数而不是空白页。
- **风险**：好友相关能力受平台授权与类目限制，若受限则按《玩法与技术方案》B7 的降级方案处理。

### M09 打磨与提审上线准备

- **目标**：可提审。
- **交付物**：性能优化记录、包体积确认、上线检查清单逐项确认、隐私协议与合规材料。
- **验收标准**：见第七节上线检查清单全部勾选。

---

## 四、推进顺序建议

**推荐顺序**：`M02 → M03（可与 M02 并行）→ M04 → M05 → M06 → M07 → M08 → M09`

**为什么 M02 排在 M04 前面**：主界面需要显示的数据里，铺位解锁状态、店铺等级、客流上限这三项都由 M02 定义。如果先做 M04，主界面的铺位与 HUD 会在 M02 落地后**再做一遍**。M01 已经把后端的底子铺好，M02 的增量不大，先用一个模块的成本换掉主界面的返工，划算。

**备选：M04 先行**。好处是更早暴露引擎风险（构建链路、包体积、真机表现都是本项目最大的未知数）；代价是主界面要返工。如果更担心"引擎这条路走不通"这个风险，可以先做一个只搭工程与静态布局、不接数据的 M04 探路版。

**并行建议**：M03 素材管线跟后端没有依赖，任何时候都可以并行起，而且它必须先于 M04 完成。

> **2026-09-14 拍板**：采用推荐顺序——**M02 先行，M03 并行**。备选方案（M04 探路先行）不予采用。

---

## 五、美术素材缺口清单

> **本节已按 SC-M03-001 / SC-M03-002 重写（v1.4）**。素材的**权威源是 `design/art-bible/美术圣经.md` §5.3 与 `assets/素材处理记录.md`**——本节只做「规划视角」的摘要与状态跟踪，**不复制具体规格**（避免「下游没跟改」）。
>
> **结构性结论（`美术圣经` 附录 A.1）**：现有 19 张 JPG 是**「方向样张」，不是「待切素材」**。🔴 阻塞项 7 条中有 6 条是**基准/管线问题**，不是精修问题。**最省钱的路径是先把正视图基准确立（重制 `bg_mall_base` + 2 张空铺底图 + 5 张立绘统一模板），再据此产出全部派生资产——先抠图再定基准 = 白扣一遍。**

### 5.1 重制/新制状态（SC-M03-002，2026-09-14）

| 分类 | 内容 | 数量 | 状态 |
| --- | --- | --- | --- |
| 🟢 **已产出可用**（程序化） | `ovl_veil_locked`、`ico_lock`、`ico_plus`、`ui_tag_wood`、`ico_visitor` | 5 件 | ✅ 已落 `NewProject/assets/textures/state/` 与 `ico/`（真 alpha、无文字、达体积目标） |
| 🟢 **免出图**（结论已定） | `app_boot` 复用 `bg_mall_base`；楼梯并入背景；空地盆栽复用 `prp_planter` | 3 项 | ✅ 不新增资产 |
| 🔵 **必须外部生成** | `bg_mall_base`、`slt_f2_empty`、`slt_f1_empty`、5 张店铺立绘、`prp_lights`、`prp_planter`、`app_icon`、`app_share`、`ui_hud_bar`（**5 槽**） | 12 件 | 🔵 规格与生成提示词已就绪（`资产基准模板与生成提示词.md` §3.10），可立即投喂 |
| 🟠 **被上游阻塞** | `font_main_subset`（中文字体子集） | 1 件 | 🟠 需字体授权 + 文案定稿 |

**基准与流程**：`资产基准模板` 已落地为**强制制作流程**（模板文件 `design/art-bible/template/asset-base-template.svg` + 生成前检查清单 + 逐资产客观验收口径），6 步流程见 `资产基准模板与生成提示词.md` §1.8。

### 5.2 体积预算口径（**已按平台事实重算，勿沿用旧数字**）

- **计量形态**：一律按 **运行时压缩纹理（ASTC）** 形态；源 PNG 无损文件**只约束像素尺寸、不设 KB 上限**（`美术圣经` v1.2 §5.3.0）。
- **预算**（运行时 ASTC 形态）：**首包 ≤2.27 MiB / 分包 ≤3.34 MiB / 全量 ≤5.61 MiB**。
- ⚠️ **默认图必须随包且计入主包 4MB**（`平台事实核对` §1.1.2，SC-ARCH-007 核对）→ 故走**情形 C（降规格默认图）**，首包含代码后 ≈3.53 MiB，仍在上限内（余 ≈0.5 MiB）。
  - 配置：按 alpha 分流两套压缩预设（无 alpha 背景 → ASTC 8×8 + **JPEG 低质量**默认图；含 alpha → ASTC 6×6 + **PNG 最低质量**默认图）。
  - **源工程仍保留全分辨率无损 PNG**（供生成 ASTC），**只对默认图降规格**，且**默认图只对进首包资产出**。
- 旧数字（首包 ≤1.53MB / 全量 ≤3.98MB）**已作废**——它低估了 mipmap（+33%）与图集开销，且漏算了新制资产。

### 5.3 仍存在的缺口（不阻塞 M04，但需排期）

| 类别 | 缺什么 | 影响 | 归属 |
| --- | --- | --- | --- |
| 顾客动画 | 行走帧或骨骼动画（现只有静帧三视图） | 客人走动只能做整体位移，观感偏生硬 | **已拍板延后到打磨阶段**（第一版用整体位移） |
| 顾客多样性 | 多套顾客外观 | 长时间玩只看到同一张脸 | 后续版本 |
| UI 九宫格切线 | 5 槽 `ui_hud_bar` 与 `ui_panel_wood` 的四条切线 | 拉伸会变形 | 美术 + 工程（重出后由工程侧标定） |
| 自定义材质真机复验 | `美术圣经` §7.6「一份材质 + 运行时参数」方案 | 官方未设「支持/不支持」，存在未知 | 工程（M04 真机复验） |
| 音效 | 点击、收银、金币入账等 | 第一版可无音效，上线前建议补 | 音频（**尚未启动**） |

> **页面设计稿**：✅ 已补齐（`design/ui-mockups/` 六屏）。仅主界面沿用 `主界面图.jpg` 作为视觉基准。

---

## 六、平台与合规依赖

| 依赖项 | 现状 | 需要确认 |
| --- | --- | --- |
| 微信小游戏 AppID 与类目 | **已注册**：`wx5a00e2270436e6e0`，名称「街角百货铺」 | 类目是否支持目标玩法；AppID 尚未写入工程构建配置（M04） |
| 主包与总分包体积上限 | 未核对 | 具体数值以官方最新文档为准，不要凭记忆 |
| 开放数据域与好友授权 | 未核对 | 授权入口、限制条件、是否受类目影响 |
| 未成年人保护与防沉迷 | 未核对 | 具体条款与接入方式 |
| 隐私协议 | 未准备 | 采集范围、展示位置、审核材料 |

---

## 七、上线检查清单（M09 逐项确认）

- [ ] 关闭本地开发令牌（`MALL_DEV_TOKEN`）与"仅本机可访问"限制
- [ ] 会话令牌具备过期与续期机制
- [ ] 存档从本地单文件迁到云端，并发与持久化方案重新验证
- [ ] 规则配置里的所有数值经过实机手感确认
- [ ] 主包体积在限制以内，超出部分已分包
- [ ] 中低端安卓机实测帧率与加载时间达标
- [ ] 隐私协议与合规材料齐备
- [ ] 未成年人保护相关要求已满足
- [ ] 全量接口的错误码与客户端提示一一对上
- [ ] 所有模块文档与实现同步，无"代码改了文档没改"的项

---

## 八、待确认项

1. ~~微信小游戏 AppID 与类目是否已注册~~ **已于 2026-09-14 解决**：AppID `wx5a00e2270436e6e0`，名称「街角百货铺」。（该 AppID 原属已放弃的《动物咖啡馆经营》，现由本作承接。）剩余待确认：类目是否支持目标玩法、AppID 何时写入工程构建配置。
2. ~~布局页 / 经营页 / 好友页 / 各类弹窗的设计图缺失~~ **已于 2026-09-14 解决**：六屏设计稿落在 `design/ui-mockups/`。剩余待确认的是素材侧的"是否先打 3 张试打样"与"统一加雨棚"（见 `design/art-bible/` 附录 B）。
3. ~~推进顺序选推荐方案（M02 先行）还是备选方案（M04 探路先行）~~ **已于 2026-09-14 拍板**：采用推荐顺序，M02 先行、M03 并行。
4. 素材由谁产出：现有素材是 AI 生图（豆包）产出，缺口部分是继续用 AI 生图 + 抠图管线，还是需要手工绘制。
5. ~~项目根目录尚未 `git init`，`NewProject/` 是独立空仓库——建议先建仓库，否则每个模块的"存档点"（提交）都无从谈起。~~ **已于 2026-09-14 解决**：仓库已建立并推送，远端 `https://github.com/TheChosenOne666/OperationShop`，分支 `main`。`NewProject/` 原内嵌的空 git 仓库已移出备份，Cocos 工程由根仓库统一管理。
6. 数值全部为开发期初版，需要在 M05 完成后做一轮实机手感确认再定稿。
7. **「解锁铺位 vs 升级店铺」的取舍不成立**：解锁回本 3.0–7.0 天，升级约 12 天，解锁全面占优。**已于 2026-09-14 拍板留待 G5 数值定稿**，M02 先按现有数值实现（数值全部外置到 config，改价只改配置，不阻塞主线）。详见 `design/gdd/系统-经营与成长.md` 附录 B。
8. **满铺口径需收敛**：草案 A4 全表用"不含满铺"口径，A5 的"二层全解锁 950""五店满级 1672"用"含满铺"口径，同一份源文档混用两套。需在 G5 定稿时二选一并统一全文档。详见 `design/gdd/系统-经营与成长.md` §3.6。
9. ~~**`P1-A`：空闲结算的 `changed` 语义无权威口径（M02 收口的唯一阻塞）**~~ **已于 2026-09-18 裁定并结清：选 A 改实现**，GDD §5.7.2 文本未改。原文备查：GDD §5.7.2 写明「0 店开业的结算：`changed = false`，不写 `capFrozen`、不写 `dailyVisitorCap`」，但改前的实现（`internal/game/service.go` `mutate` 无「实质变化」判定）在**零开业、间隔未满、已冻结但无新客人**三种空闲轮询下都返回 `changed=true` 并递增 `revision`、写盘。取证与修复见 `docs/M02-验收口径评审.md` §8.3 / §8.5、`docs/M02-后端扩展.md` §7.5。~~两个选项：~~
   - ~~**A 改实现**~~ ✅ **已采用**：`mutate` 增加 `sameCommittedState`（忽略 `lastObservedAt` 的深比较）判定，空转结算返回 `changed=false` 且不落盘；`accrue` 的空转分支不再移动结算游标，「首次开店以准备时刻起算」的锚定移交 `Prepare` 的提交闭包；`service_test.go` 中被固化成 `true` 的 4 处断言改回 `false` 并补落盘次数断言。
   - ~~**B 改规格**~~ 未采用。
   - ⚠️ **改后语义（M04/M05 必读）**：`Result.changed` 严格等于「服务端是否发布了新状态」；`lastObservedAt` 是**最后一次发布状态的时刻**，不是最后一次被轮询的时刻，空闲期保持不变属预期。
10. ~~**`P1-B` 是否本轮顺手修**~~ **已于 2026-09-18 裁定「本轮一并修」并结清**：`validateSlots` 增加「两个 `slots[]` 不得共用同一 `(floor, position)`」。原文备查：改前 `Config.Validate()` 对此放行，触发后 `slotIDOfShop` 返回空串 → `slotIndex[""]` 静默取 0 → 该店的解锁门禁与满铺判定读到**错误铺位**且零报错。**未加**「每个 shop 必须被 slot 覆盖」那条校验——在既有约束下它是双射的可证推论，加了即死代码（与「不为不可能发生的场景加防御」一致）。新增配置用例覆盖 `P-F` 的原始触发场景。
11. **`P2` 四项登记（不阻塞 M02）**：`P2-A` GDD §5.5「轮询游标指向已开业店铺」未实现且**按字面实现会误拒合法存档**（游标 `+1 % n` 落在未开业店是常态），建议改规格表述；`P2-B` GDD §5.6「每店 ≤6 段」未落为加载校验（实测上界成立且可取等），**实质风险在 M07 接收外部存档时**才成立 → ✅ **已登记为 M07 前置**（见 §三 M07 块）；`P2-C` 未知铺位 id 复用 `SHOP_NOT_FOUND`，M06/M09 错误码对表时需确认；`P2-D` 交接件 `docs/当前状态与接手指引.md` 已于 `cc43ce7` 删除，是否有意待确认。
12. ~~**`P2-E`：`docs/architecture/架构现状.md` 仍是 M01 快照，须在 M04 开工前 M02 化**~~ **已于 2026-09-18 清账**：整篇按代码现状刷新，标题改为「M01 + M02 本地开发后端」，头部附 v2 修订对照表逐行记录旧新内容。原文备查：该文档自称「现状实拍（以代码为准）」、最后更新停在 2026-09-14，§7.1 曾把铺位解锁 / 店铺升级 / 满铺加成 / 动态客流上限 / 账单分段 / 存档 schema v2 六项标为「未实现」，§1 的锁覆盖范围只写 `Prepare` / `Settle`——这是 `cc43ce7` 未同步刷新现状文档留下的文档债（违反规划原则 7）。刷新时另将本模块的 `P2-A`、`P2-B` 收进该文档 §7.2 作冲突 **I**、**J**（该表是"代码与文档冲突"的归口），并补三条 M02 已知边界（详见其 §8）。
13. **M03 一层立绘的交付尺寸两套口径并存**（源自 `assets/M03-素材投喂包.md` §4 冲突 1，`fe57287` 入库，本文件此前未登记）。需二选一并统一全文档：
    - **A. 咖啡与花束均 `640×768`**——`美术圣经` §5.3 逐资产规格表 L384/L385、`资产基准模板` §3.10.1 L523、`素材处理记录` 逐图处理表 L84（其体积目标 ≤304 KiB 即按 `⌈640÷6⌉×⌈768÷6⌉×16 ×1.33` 算出）。
    - **B. 咖啡 `704×1024`、花束 `640×1024`**——`资产基准模板` §1.2 基准画面尺寸表 L73/L74 与 §3.7 / §3.8 提示词正文 L428/L450。
    - **暂定 A**，依据是本文件 §五 已声明「素材的权威源是 `美术圣经` §5.3 与 `素材处理记录`」。
    - ✅ **真实成因已查明（2026-09-18 逐处核对，推翻了投喂包 §4 原先"A 侧比例不自洽、B 更强"的推测）**：两侧是**取数基准不同**——A 侧把两张一层立绘**统一取一档生成预设**（咖啡与花束同为 `640×768`，尽管两者外框宽 345 与 265 差很多）；B 侧则**按 `美术圣经` §4.5 的外框（@750）逐铺位取档**（咖啡 345×493→704×1024、花束 265×493→640×1024，比例 0.688/0.625 与外框 0.700/0.537 各自接近）。故 **A 的画布必然含横向留白、B 更贴外框比例**，但"B 更强"只在**素材按外框出图**的前提下成立。
    - ⚠️ **需美术先答的子问题**：立绘素材该按 §4.5 的「**外框**」出图，还是按「**立绘实际可绘区**」（咖啡 ≈290×420 / 花束 ≈225×420）出图？`美术圣经` §5.3 的"显示尺寸"列取的正是后者，而 §1.2 取前者——**这才是两套尺寸分歧的根**。定了这条，尺寸口径会自然收敛。
    - **不阻塞投喂**：两套口径的**建议生成比例同为 1:1**（品红底整图生成后再裁），差异只在入库时的目标像素尺寸与体积目标（A 按 640×768 算 ≤304 KiB；B 若按 704×1024 则需重算，`slt_f1_empty` 同尺寸档为 ≤448 KiB）。裁定后需同步 `美术圣经` §5.3、`资产基准模板` §1.2/§3.7/§3.8/§3.10.1、`素材处理记录` 与投喂包共 **6 处**。
14. **M03 外部生成件的件数口径三个数字并存，直接决定 M03 何时算完成**（源自投喂包 §4 冲突 2；成因经 2026-09-18 逐处打开原文清点，**与投喂包 §4 原先的推测性解释不同，实为算术错误**）：
    - **13 件**——`assets/M03-素材投喂包.md` §1 逐件展开：背景 1 + 空铺底图 2 + 店铺立绘 5 + 道具 2（`prp_lights`/`prp_planter`）+ 应用级 2（`app_icon`/`app_share`）+ HUD 木条 1 = **13**。**投喂清单与入库管线现按此执行**。
    - **12 件**——`开发规划文档` §5.1、`素材处理记录` §9.3（L375）、`资产基准模板` §5.3（L726）与其 L729「一句话结论」共 **四处**。⚠️ **这四处各自列举的条目数就是 13**（`shop_{clothing,dessert,bookstore,coffee,flowers}_open` 按花括号展开为 5 件），数量栏却统一写 12 —— 是同一个错数被抄了四遍，**不是"把两件并作一件"的不同分组口径**。
    - **9 项**——`资产基准模板` §3.10.1「来源统计」（L525）。⚠️ 该行括号内列举「背景、空铺 ×2、立绘 ×5、彩灯、花箱、app_icon、app_share」实际加总为 **12**，且**漏了 `ui_hud_bar`**——即这个数既不符 9 也不符 13。
    - **连带发现（同一行）**：`资产基准模板` §3.10.1 记 🟢 程序化可产出 **7 项**（状态件 ×4 + 客流图标 + `app_boot` + 楼梯并入 + 盆栽复用），而本文件 §5.1 与 §二 M03 行记的是 **5 件程序化**（前者把 3 项「免出图」也算进了 7）。**M03 的"已完成"分母同样有两套数。**
    - **影响**：M03 完成判定、进度表分母、§二 M03 行与 §5.1 的全部数量表述。裁定后需一次性回改 `开发规划文档`（§二 M03 行、§5.1、v1.4 修订记录）、`素材处理记录` §9.3、`资产基准模板` L525/L726/L729 与投喂包 §4 共 **8 处**；其中程序化件数（5 vs 7）需先定「免出图的复用项算不算交付件」这一条子规则。**纯口径统一，不改变任何素材的实际规格与出图比例。**
15. ~~**`P2-F`：§M05 验收第 1 条的「客流计数减少 1」与 M02 起的 HUD 契约相反，需裁定改哪一侧**~~ **已于 2026-09-21 主理人裁定「按 HUD 契约改规格」并结清**，§二 M05 验收第 1 条原文已改写。原文备查：
    - 冲突：本文件 §二 M05 验收第 1 条原写「金币增加、**客流计数减少 1**」；而 `系统-经营与成长` §6.1 规定的 HUD 字段是 `visitorsServed` + `dailyVisitorCap`，即「**已到店 / 上限**」——客人到店时这个数是 **+1**。真正减少的 `visitorsRemaining` 不在 HUD 上（M01 时代曾以它作显示值，见 `架构现状` §7.2 冲突 G，M02 已改）。
    - 定性：**M01 措辞残留**，不是实现缺陷。M05 按 §6.1 实现并实测通过（`01-initial.png` 显示 `0/—` → `03-polling.png` 显示 `1/35`）。
    - 为什么这次选「改规格」而非默认取向「改实现向规格对齐」：§6.1 是 M02 **已验收通过**的对外契约，改实现会连带回改 HUD 与 `Boot` 的显示口径，等于推翻一个已收口的模块。裁定过程本身即「规格与实现冲突先交主理人」这条流程的又一次执行。
16. ~~**M03「13 件全是占位图」的表述与仓库现状不符：`chr_guest_a_{front,side,back}` 是真实抠图产物，真素材进度应为 3/13 而非 0/13**~~ **本条系登记者的误判，已于同日核对原文后撤销——「真素材 0/13」这句本来就是对的**。
    - 误判成因：看到 M05 截图里客人是完整人物而非斜纹占位，就假定它属于那 13 件。**实际 `chr_guest_a_{front,side,back}` 从来不在投喂包 §5 的 13 件清单内**——那 13 件是 背景 1 + 空铺底图 2 + 店铺立绘 5 + 道具 2 + 应用级 2 + HUD 木条 1。`tools/m03_placeholder.py` 不生成 `chr_guest` 不是漏了，而是因为本就不该生成。
    - 准确事实：客人三视图是 `素材处理记录` §3.1 逐资产表编号 8–9–10 的**已产出件**（源 `6.jpg` 三分切 → 抠图 + 去地面阴影，256×256、30–37 KiB），在该文件 §4 的跨文档对表里状态为 **⏳「已产出但为 interim 参考件（待统一服装/头身比）」**——与同表的 `prp_cat_sleep`、`ui_panel_wood` 同类，属"用参考图派生的过渡素材"这条线，不占"外部 AI 生成 13 件"的分母。
    - 因此 M03 的进度表述（§二 M03 行、v1.10 记录、`docs/M04` §6/§7.3）**无需改动**；M05 用真客人图验走位也不构成任何口径冲突。
    - **保留本条的理由**：件数口径本来就是 §八-14 未结的争议（13/12/9 三个数并存、程序化 5/7 两套数），"某张图算不算在分母里"正是那条待裁定的直接后果。登记一次误判，比抹掉它更能防止下一个人重犯——**判"某资产不在清单里"之前要回到清单原文数一遍，不能靠截图观感反推**。
17. **独立复核 SC-M05-QA-002 提出的四项规格取向**（出处 `docs/M05-验收口径评审.md` §6「该改规格」；按惯例**未擅自改验收标准与 GDD 原文**，只做登记）：
    > ✅ **2026-09-22 主理人已裁：B / C / D 结清，A 仍待裁**（逐条裁定结果如下，下方原文不改）：
    > - **B 结清**——口径定为 `unitPrice` = 该店**当前**的单客收益档位，与「本轮客人按什么价入账」不是一件事。服务端为此新发三个派生字段：逐店 `unitPriceBase` / `floorBonus` / `nextUnitPrice`，**不由客户端推算**。落点 `internal/game/service.go` 的 `ShopView` 注释与 `TestShopViewPriceSplit`；取证与需求侧描述见 `docs/M06-页面与弹窗.md` §4 D-1/D-3、§9.2a。
    > - **C 结清**——「飘字 / 详情显示的金额与本轮入账一致」已写进 **M06 验收第 4 条**（`docs/M06-页面与弹窗.md` §5），并有一条编排层用例守「同一响应里弹窗读新价、飘字读旧价」（`tools/m05_probe.ts` ⑧）。M05 四条验收措辞不动。
    > - **D 结清**——该段「口径澄清」的**写入程序由主理人追认**；ADR 0001 已追加「纯算术边界」一节，把 M06 的差额与余额预览纳入允许范围并写明追认日期（`docs/architecture/adr/0001-服务端权威与客户端零计算.md`）。
    > - ⬜ **A 仍待裁**——§三 M05 验收第 1 条「5 秒内」的措辞（复核建议改为「第一个结算周期内服务端产生第一位客人，界面在下一次轮询即反映」并给实测区间）。**本文件该条原文未动**。
    - **A｜§M05 验收第 1 条的「5 秒内」字面不可达**：服务端 `visitorIntervalSeconds = 5` 本身就吃掉整个窗口（首次开店把结算游标锚在点击那一刻，实测第一位客人最早 T0+5.14s 存在），加上客户端轮询相位 0–5s 与走位 1.6+0.5s，玩家可见落点是 **5.1–10s**、飘字 7.2–12.1s。复核方判定为**判据写错了单位**（把服务端节拍写成了玩家可见延迟），不是实现缺陷。建议改写为「开店后**第一个结算周期内**服务端产生第一位客人，界面在**下一次轮询**即反映（金币与已到店各 +1），客人走位在 B3.5 的 1.6s+0.5s 内完成」并给出实测区间。
    - **B｜`unitPrice` 的用途边界没写清（SC-M05-QA-002 P1-A 的根）**：`系统-经营与成长` §6.1 只说它「用于店铺详情弹窗展示构成（可选派生）」，没写它**不能**当「每位客人实付额」——而服务端 `mutate` 先 `accrue` 后 `apply`（`service.go:436-441` + `:675`），同一条 Prepare/Upgrade 响应里 `unitPrice` 是**加价后的新价**、客人却按**加价前的旧价**入账。M05 已在客户端绕过（`Arrival.unitPrice` 取上一份快照，见 `docs/M05-经营闭环.md` §14），但口径未定，**M06 详情弹窗一旦展示同一字段就是常驻错误**。建议 §6.1 明确：`unitPrice` 是「该店**当前**的单客收益档位」，与「本轮客人按什么价入账」不是同一件事；后者若表现层要用，必须由服务端在 `Result` 里给逐店明细。
    - **C｜M05 四条验收没有任何一条覆盖「表现层显示的金额」**，P1-A 因此从判据上漏网。建议 M05 措辞不动，把「飘字/详情显示的金额与本轮入账一致」补进 **M06 验收**。
    - **D｜ADR 0001 那段「口径澄清」的程序追认**：它是 M05 收尾时由实现侧写入的，而该 ADR 自称「决策范围：全部模块，M02/M06/M07 不得回退」。复核方独立认可**内容站得住**（只覆盖不含规则常量的累计计数做差），但改全局口径的**程序**宜由主理人追认，不宜作为模块收尾的附带项。原文备查：`docs/architecture/adr/0001-服务端权威与客户端零计算.md`「口径澄清（M05 追加）」节、`docs/M05-经营闭环.md` §9-⑥。
    - **附带发现（同一处 §6.1，措辞笔误）**：那行写「未冻结时 HUD 显示占位 **`— / —`**、**不显示分母**」——未冻结时缺的是**分母**（上限未知），而显示 `— / —` 实际是**分子也不显示**。裁定 L 已按 `— / —` 落地实现，这行自相矛盾的措辞要不要一并改，随本条一起裁。
18. **独立复核 SC-M06-QA-002 提出的两条对稿口径**（出处 `docs/M06-验收口径评审.md` §4；按惯例**未擅自改设计稿、需求原文与代码**，只做登记）：
    - **① 布局页同屏两个分子（002-01，成立）**：楼层标题「n / m 已开放」读**已解锁数**（`MallPages.ts:512`），紧邻的满铺加成卡读**已营业数**（`:507`）——同一个词两层意思，实测 `.work/qa-m06-pop/runE.txt:18` 是 `1 / 3 已开放` 与「二层 `0 / 3` —— 再开放 3 个铺位」并排。**根因在稿不在代码**：稿 01 的 `:215` 用解锁数当加成分子，而 `floorFull`（`internal/game/service.go:415-421`）要的是「每铺位既解锁又开业」，照稿数字实现会出现「3 / 3 却没有加成」。已登记 `架构现状` §7.2 冲突 **R**。两条出路：改稿文案（分子注明「已营业」或措辞改「再让 K 家店开业」）／维持稿面但在文字上区分两口径。✅ **2026-09-24 已裁并改完（v1.22）**：跟规则走——加成卡分子维持已营业数、措辞改「K 家店**开业**」（已生效行不变），楼层标题「n / m 已开放」不动，两词分开；复验 `.work/qa-m06-fixA-run.txt:19`。**稿 01 的数字未改**（改稿另议），冲突 **R** 结清。
    - **② 经营页列表整体下移 22px（002-02，成立）**：稿 02 `.list` 顶 = **336**，实现取 **358**（`MallPages.ts:255`，为页内错误提示位让路）⇒ 五行底缘 **1120**，距 HUD 上方的禁放线 1121 只剩 **1px**（取稿值有 23px 余量）。同类提示位在**布局页**的处理是挪到顶栏右侧、不占列表上方（先例见 `docs/M06-页面与弹窗.md` §10.1-3），两页做法不一致。建议与布局页统一（提示位移顶栏 + 列表回 336），**改后需出包用 `hud` 复核顶栏不与标题重叠**。附带：`MallPages.ts:248` 那条注释把出处写成 §9.5，实际记录在 §10.1。✅ **同日已改（v1.22）**：提示位移到顶栏右侧（与布局页同坐标），列表顶回 336 ⇒ 末行底缘 **1098**、禁放余量回到 **23px**；注释指针已更正。但**提示位本身的可见性没验到**，另见下面 ④。
    - **③ 附带一条驳回**：002-03 的「括号改全角」子项为**误报**——§3.1 第 47 行原文本就是全角「（800 金币）」，实现逐字一致，不存在偏离；同条的「删铺位二字」与 §10.3、SC-M06-QA-001 P2-3 重复，不另计。
    - **④ 修 002-02 时新登记的待查项（002-04，本轮未修）**：页内提示位的**可见性取不到证**——五轮 `hud` 里 `PageManage/Notice`、`PageLayout/Notice` 从未非空，而同一时刻招牌位读到过「连接中断，稍后会自动重试」。根因线索：`MallScene.ts:182` 只把提示写给**当时可见那一页**、切页不重放，且横幅 3 秒自清 vs 轮询 5 秒节拍 ⇒ 失败提示可能落在看不见的页上或落在读取窗口外。**数值与写命令行为不受影响**（本轮已复验：停后端后金币与客流逐字不变）。修法候选二选一，待主理人裁：① `MallPages.show()` 里重放最后一条提示文本；② 页内提示位不自清，由下一次成功渲染清。
