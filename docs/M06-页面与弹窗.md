# M06 页面与弹窗 · 需求文档

> 状态：需求部分**已确认**（2026-09-22 主理人拍板 D-1~D-5，见 §8）；技术方案**已确认可开工**（第二部分）。
> 实现与实测：任务 1–8 已完成（§10.2/§10.3/§10.5）。**独立复核已做**：SC-M06-QA-002（2026-09-24，`docs/M06-验收口径评审.md`）**有条件放行**——无 P0/P1，三条 P2 里一条需裁定。**裁定后两条对稿问题（002-01 / 002-02）已改并复验**（§10.9），包体 **2773.4 KiB = 67.7%**（改动前后同数，不进素材）；另新登记一条待查缺口 **002-04**（页内失败提示位在页面层的可见性），§10.9 末尾。
> 本文只写「要做什么、显示什么、怎样算做对」，不写技术栈、不写架构、不写实现顺序——那些归关卡③的技术方案。
>
> 事实来源（逐条回原文核对过）：
> `design/ui-mockups/01,02,04,05,06`（五屏设计稿，含各自的「设计说明」面板）· `design/gdd/需求文档.md` §6.2 与 R-02/R-03/R-07/R-08/R-09/R-13 ·
> `design/gdd/系统-经营与成长.md` §6.1/§6.2/§6.3 · `docs/architecture/架构现状.md` §7.2/§8 ·
> `docs/M05-经营闭环.md` §11.6-6/7/8 与 §14 · `production/开发规划文档.md` §三 M06 与 §八-17

---

## 1. 这一模块解决什么

| 痛点 | 现状证据 | 后果 |
| --- | --- | --- |
| 玩家看不到「这家店赚了多少」 | 主界面只有全局金币与客流两个数（`MallScene.renderHud`），逐店 `visitors`/`revenue` 服务端已返回但界面从不显示 | 单店好坏无法判断，「解锁还是升级」这个核心取舍（R-07/附录 B-2）变成盲选 |
| 两笔最大额不可逆支出没有确认环节 | 解锁 600/800/1200、升级 700～若干，客户端目前点铺位直接发命令（`MallScene.onSlotTouched` 只处理开店） | 误触即扣钱且不可撤销；R-02 要求的「金币不足时给出原因」至今没有任何落点 |
| 开店入口是临时位置 | M05 的「点待开业铺位即开店」是**已裁定的规格偏离**（§八 记录 ①），正式入口是经营页的金色「开业」按钮 | 经营页缺席一天，这条偏离就一天不结案 |
| 飘字金额的口径雷区会常驻 | M05 用「单价取上一份快照」绕过（`docs/M05-经营闭环.md` §14 的 P1-A 修复），但 `unitPrice` 的用途边界仍未定（§八-17-B） | 详情弹窗一显示这个字段，瞬时偏差就变成玩家随时可看到的**常驻错误** |

## 2. 使用场景（玩家路径）

1. **看经营**：主界面 → 底部「经营」→ 今日客流 / 今日收益 / 满铺加成三格概览 → 五家店铺按客流到店顺序排列 → 找「待开业」的那行点「开业」。
2. **升级**：经营页点一家营业中的店 → 详情弹窗看等级、单客收益构成、累计客流、累计收益 → 点「升级到 Lv(n+1)」→ 升级确认弹窗看单价对比 + 花费/现有/余额 + 「不追溯历史」说明 → 确认升级或再想想。
3. **扩铺**：主界面或经营页 → 底部「布局」→ 两层铺位总览（每格三态之一）+ 每层满铺进度 → 底部主按钮「解锁「X」铺位 N 金币」→ 解锁确认弹窗看前后对照 + 三行金币账 + 解锁后会怎样 → 确认解锁或再想想。
4. **出问题时**：任何界面请求失败，提示要能看懂，且数字保留最后一次成功值（M05 已定口径，本模块沿用，不得回退）。

## 3. 逐屏需求点

> 设计稿是**唯一权威形状**：尺寸、色值、文案、状态编码全部按稿执行（@750×1334 基准；稿内「设计说明」面板的规格条目算需求的一部分）。
> 「口径」列写明这个数字**必须**取自哪里；标 🔴 的是服务端当前**没有**的字段，见第 4 节。

### 3.1 经营页（稿 02）

| 元素 | 需求 | 口径 |
| --- | --- | --- |
| 概览格「今日客流」 | `已到店 / 上限`，未冻结时整格 `— / —` | `visitorsServed` + `dailyVisitorCap`（未冻结为 `null`）；沿用 §7.2 裁定 L |
| 概览格「今日收益」 | `+N` 金币 | 🔴 **D-2**：服务端无当日增量字段；逐店 `revenue` 是**历史累计**（`ShopState` 注释「cumulative」），跨日不清零，不能当今日数用 |
| 概览格「满铺加成」 | 生效层名（如「一层生效」） | 逐层 `已开业数 / 该层铺位数`（§6.1 允许由 `unlockedSlots`+`prepared` 派生） |
| 说明条 | 「按客流到店顺序排列（一层临街优先）」 | 顺序 = 服务端 `shops` 数组顺序，客户端不自己排 |
| 店铺行 ×5 | 店名、等级徽章、缩略店面、单客收益、累计客流·累计收益、右侧状态或操作 | `shops[].name/level/unitPrice/visitors/revenue` |
| 「单客 8 金币 (7+满铺1)」 | 单价**必须写出基础 + 满铺的构成**（稿内明示：不写玩家看不懂数字为什么变） | 🔴 **D-1**：服务端只给合计 `unitPrice`，无拆分 |
| 状态位「营业中」 | 绿色对勾 + 文字；**不给关店按钮**（关店已于 2026-09-14 移出范围） | `prepared == true` |
| 「开业」按钮 | 待开业行显示，点击开店、无二次确认（R-03：不花金币、幂等） | `unlockedSlots` 含该铺且 `prepared == false` |
| 未开放行 | 文案「未开放 · 需先在布局页解锁铺位（800 金币）」/「需先解锁二层 2 号铺位」，缩略图压暗 + 纱罩 + 锁形 | `slots[]`（`floor`/`position`/`shopId`/`unlockCost`）与 `unlockedSlots` |
| 底部 HUD | 与主界面同一套三导航 + 金币 + 客流；当前页导航钮高亮为按下态 | 复用主界面 HUD 契约 |

### 3.2 布局页（稿 01）

| 元素 | 需求 | 口径 |
| --- | --- | --- |
| 楼层分组标题 | 「二层 · 楼面」「一层 · 临街」+ 右侧 `n / m 已开放` | `slots[]` 的 `floor`/`position` + `unlockedSlots` |
| 铺位卡 | 店名 + 三态之一：`营业中 · 单客 N 金币` / `待开业` / `未开放 · N 金币`；未开放要纱罩 + 锁角标 + 压暗 | `shops[]` + `unlockedSlots` + `slots[].unlockCost`；**三态编码须转灰度仍可分**（稿内硬要求：靠锁 vs 加号、暗 vs 亮、文字三通道，不靠颜色） |
| 满铺加成卡 | 逐层两行：已生效（实心点 + 「该层单客收益 +1（已生效）」）/ 未生效（空心点 + 「再开放 K 个铺位即可获得 +1」） | 计数派生 + `config.fullFloorBonus` 的**显示值**（不得在客户端写 `+1` 这个常量） |
| 主按钮 | 「解锁「甜屋」铺位 800 金币」，可解锁态带金色呼吸光晕 + 加号角标 | `nextSlotId` + `nextUnlockCost` + 该 slot 的 `shopId` 对应店名 |
| 主按钮禁用态 | 金币不足时文字改「再攒 N 金币」（**写出差多少**，不写「金币不足」）+ 锁角标 + 不透明度 78%；**禁止变红**、禁止灰到看不见 | 差额由 `coins` 与 `nextUnlockCost` 得出（见第 4 节 D-4：这条要不要服务端给） |
| 一层铺位卡 | 只显示「营业中 / 待开业」，一层两铺为开局已解锁、无解锁按钮 | `unlockedSlots` 初值 |

> ⚠️ **2026-09-24 裁定后更正（SC-M06-QA-002 002-01 / 冲突 R）**：加成卡上稿印的 `1 / 3`（拿已解锁数当分子）**与满铺规则不符**——`floorFull` 要的是该层每个铺位「既解锁又开业」（`internal/game/service.go:415-421`），照稿数字会出现「3 / 3 却没有加成」。裁定为**跟规则走**：该卡分子取**已营业数**，措辞由「再开放 K 个铺位」改为「K 家店**开业**」（已生效行不变），使它与楼层标题「n / m 已开放」（已解锁数，本表上一行的口径）不再同词两义。实测见 §10.9。

### 3.3 店铺详情弹窗（稿 04）

| 元素 | 需求 | 口径 |
| --- | --- | --- |
| 标题行 | 店名 + 等级徽章 + 营业状态 | `shops[].name/level/prepared` |
| 店面示意 | 一张店面图（正式资产未到位时用现成的 `shop_*_open` 占位件） | 复用主界面取图路径 |
| 单客收益 | `8 金币` + 构成条「基础 7 + 满铺 1 = 8」 | 🔴 **D-1** |
| 累计客流 / 累计收益 | `24 位` / `192 金币`，**两行都读服务端原值** | `shops[].visitors` / `shops[].revenue` |
| 升级预告 | 「升到 Lv3 后 单客 9 金币 (+1)」+「升级花费 700 金币」 | 花费 = `upgradeCost`（已有）；升级后单价 🔴 **D-3**（GDD §6.1 那一行写着「该店 `upgradeCost` + 升级后单价」并标 M02 新增，代码里没有） |
| 已满级 | 稿未画（见第 8 节待裁定）；`upgradeCost == null` 即满级，此时不得出现任何「可升级」暗示 | `upgradeCost == null` |
| 金币不足 | 「确认升级」换成禁用态：锁角标 + 文字「再攒 260 金币」+ 不透明度 78%；**点了不发写命令** | `coins` vs `upgradeCost` |
| 关闭 | 点遮罩或右上角关闭钮 = 取消，**不做二次确认**（升级有独立确认弹窗） | 稿内交互说明 |
| 主按钮 | 「升级到 Lv3」→ 打开升级确认弹窗（稿 06），本页不直接提交升级 | — |

⚠️ 硬约束（`架构现状` §8-8）：`unitPrice`/`upgradeCost` 对**未解锁铺位**也照常返回。详情弹窗**不得**照字段有值就渲染成"可以升级"，须按 `prepared`/`unlockedSlots` 自行置灰。

### 3.4 解锁确认弹窗（稿 05）

| 元素 | 需求 | 口径 |
| --- | --- | --- |
| 标题 + 副标 | 「解锁铺位」/「二层 · 2 号铺位「甜屋」」 | `slots[]` 的 `floor`/`position`/`shopId`→店名 |
| 前后对照 | 左「未开放」（纱罩 + 锁角标 + 暗）→ 右「可开业」（暖光亮起）；**同一张空铺底图**，只靠自发光档位与纱罩区分 | 沿用 §10 三态渲染约定（同一贴图 + 材质档位 + 独立遮挡子节点，不预烘多套 PNG） |
| 金币账三行 | 解锁花费 `− 800` / 现有金币 `1012` / 解锁后余额 `212 金币`（余额用绿色，把结果先算给玩家看） | 花费 `nextUnlockCost`、现有 `coins`；余额是**确认前预览**，第 4 节 D-5 |
| 变化清单 | 三条：变成可开业 / 该层进度变成 `n/m`、还差 K 个 / 满铺后该层单客 +1（并说明另一层已生效） | 计数派生 + `config.fullFloorBonus` 显示值 |
| 按钮 | 「再想想」取消 /「确认解锁」提交 `POST /api/v1/slots/{slotId}/unlock` | — |
| 只有可解锁态 | 稿没有禁用态：入口在布局页主按钮，金币不足时该按钮本身已禁用，不给进弹窗的路 | 需求侧记录为**有意约束**，避免重复编码禁用态 |

### 3.5 升级确认弹窗（稿 06）

| 元素 | 需求 | 口径 |
| --- | --- | --- |
| 标题 + 等级阶梯 | 「升级「咖啡」」+ `Lv2 → Lv3` | `level` 与 `level+1` |
| 单价对比 | 左「当前单客 8 / 基础 7 + 满铺 1」→ 右「升级后单客 9 / 基础 8 + 满铺 1」（升级后那块用橱窗暖光底色）+ 下方「每位客人多赚 1 金币」 | 🔴 **D-1 + D-3** 同时命中；差额文案也依赖 D-3 |
| 金币账三行 | 升级花费 `− 700` / 现有 `1012` / 升级后余额 `312 金币` | `upgradeCost` + `coins`；余额同 D-5 |
| 规则说明（不得省略） | 「升级从**下一位到店的客人**开始生效；已经赚到的 192 金币不会改变，也不会被重新计算」 | 对应 R-07 的「历史收益不被追溯改写」；数字取该店 `revenue` 原值 |
| 按钮 | 「取消」/「确认升级」提交 `POST /api/v1/shops/{id}/upgrade` | — |

### 3.6 跨屏通用要求

- 弹窗一律带遮罩（稿值 `rgba(74,47,30,.58)`）、木面板 + 内嵌奶白内容板（木底压正文对比度不足）、关闭钮直径 64 压在面板右上角外沿。
- 正文落奶白板、按钮高 ≥88 触控区、字号按稿（标题 34 / 正文 25–26 / 辅助 21）。
- 三导航（经营 / 布局 / 好友）在任何一页都在：**好友页属 M08，本模块只保留入口与「未开放」占位，不得做假榜单**。
- 顶部 48px 避让区不放任何信息；HUD 160px + 其上方 53px 禁放区。

## 4. 硬前置：四个未结清的显示口径

> 这几条不裁定就无法验收，全部由设计稿的字段要求倒推出来。**本文不擅自改 GDD 原文与验收标准**（按项目规则先登记、先交裁定）。

| # | 设计稿要什么 | 规格怎么说 | 代码现状 | 不裁定的后果 |
| --- | --- | --- | --- | --- |
| **D-1 单客收益构成** | 「基础 7 + 满铺 1 = 8」两处（详情弹窗、升级弹窗） | GDD §6.1 只写 `unitPrice` = 等级单价 + 满铺加成、「用于展示构成」，**没写拆不拆**；§6.2 又明令客户端**不得自行判定满铺加成** | 只返回合计 `unitPrice` | 客户端要么现算（违反 §6.2 与 ADR 0001），要么显示不出稿要求的构成 |
| **D-2 今日收益** | 经营页概览格 `+332` | 需求文档 §6.2 经营页要求「各店状态与**当日收益**」；GDD §6.1 字段表**无此字段**；`架构现状` §8-9 说明分段不落时间，无法按天回溯 | 无字段，逐店 `revenue` 为历史累计 | 要么砍稿上的一格（改设计稿需裁定），要么服务端加当日增量口径 |
| **D-3 升级后单价** | 「升到 Lv3 后 单客 9 金币」+ 对比右侧的 9 | GDD §6.1「升级弹窗」行**明确要求** `upgradeCost` **+ 升级后单价**，并标「M02 新增」 | 只有 `upgradeCost`，无升级后单价 | 已验收模块（M02）与规格互相矛盾且**至今无人登记**；M06 只能靠客户端查等级单价表推算 = 直接违反 ADR 0001 与 `ApiTypes.ts` 里「客户端只读不推」那句 |
| **D-4 差额与余额** | 「再攒 N 金币」「解锁后余额 212」 | 稿 05/06 的「待办依赖」自己写着「余额需服务端返回，客户端不自己算」 | `coins`/`nextUnlockCost`/`upgradeCost` 都有，做减法即得 | **需要一条口径**：`coins − cost` 这种「不含规则常量的纯减法」算不算 ADR 0001 禁止的推导？M05 那次「对服务端累计计数做差」已开过先例（ADR 0001 的口径澄清，程序本身还在 §八-17-D 待追认） |

📌 D-1 与 §八-17-B 是同一件事的两个面，**建议一并裁定**；D-2/D-3 是本次新发现的、此前未登记在任何冲突归口里的缺口（我准备把它们补进 `架构现状` §7.2，登记不改原文）。

## 5. 验收标准

> 对应 `production/开发规划文档.md` §三 M06 那句「逐屏与设计图核对字段、文案、布局、按钮状态；金币不足、已满级等异常态有明确提示且不出错」的具体化。**下面每条都要有可取证的实际读数**，不接受"应该没问题"。

1. **逐屏对稿**：五屏（经营页、布局页、店铺详情、解锁确认、升级确认）各自的字段、文案、布局尺寸（@750 基准）与设计稿一致；三态编码**转灰度后仍可分辨**（形状 + 文字 + 明度三通道齐）。
2. **零本地规则常量**：界面显示的每个数字都能指到最近一次服务端响应的某个字段；客户端代码里不出现等级单价表、解锁价、升级价、满铺值中的任何一个（ADR 0001 / ADR 0004 / `ApiTypes.ts` 的「只读不推」）。
3. **累计口径**：详情弹窗的「累计客流 / 累计收益」逐字等于服务端 `shops[].visitors` / `shops[].revenue`；**不得**用「累计客流 × 单价」现算（单价会变，分段的存在理由就是这笔账不能现推）。
4. **显示金额与入账一致**（§八-17-C 要求补进 M06 的那条）：飘字与详情/升级弹窗上出现的「单客收益」，与该轮服务端实际入账额（`earnedCoins` ÷ `visitorsUsed` 的口径核对）在同一次取证里对得上；不一致就判不通过。
5. **金币不足**：解锁与升级两条路径都给「差多少」的明确文案（不是「金币不足」四个字），且**不发写命令**；按钮保持三重编码的禁用态，不变红。
6. **已满级**：`upgradeCost == null` 时界面不出现任何"可升级"暗示，等级与文案有明确交代（具体形态见第 8 节，需先裁定）。
7. **未解锁铺位**：不因 `unitPrice`/`upgradeCost`/`nextUnlockCost` 有值就渲染成可操作（§8-8 那条边界的正面验收）。
8. **失败不出错态数字**：错误码逐个映射为玩家可读提示（`INSUFFICIENT_COINS`/`MAX_LEVEL`/`SLOT_LOCKED`/`SLOT_ORDER`/`SHOP_NOT_OPEN`/`SHOP_NOT_FOUND`），且失败时界面数值保留最后一次成功值，不跳变、不回退（沿用 M05 验收第 3 条）。
9. **页面切换不放大请求量**：切页、开合弹窗都不额外发请求；轮询节拍不变；后台期间请求数为 0（沿用 M05 的实测口径）。
10. **包体数字**：本模块每次构建都要报主包体积，并记录进文档（M04 起的固定要求）。

## 6. 测试策略

| 层 | 测什么 | 怎么算测过 |
| --- | --- | --- |
| 后端（仅当 D-1/D-2/D-3 裁定为「服务端补字段」） | 新派生字段的**取值正确性**与**时机**：跨业务日、上限未冻结、满铺刚触发的同一笔结算内、升级前后的同一条响应里 | `internal/game` 单测逐条覆盖 + `internal/api` 覆盖响应形状；`gofmt -l .` / `go vet ./...` / `go test -count=1 -race ./...` 全绿 |
| 编排层（`MallStore`，不引测试框架直跑的探针） | 切页与弹窗开合不产生请求；写命令成功后用**响应内**的新状态重渲染而不是再拉一次；失败分支不动数值 | Node 跑探针断言，含一条反向验证（把实现改错，用例必须红） |
| 客户端渲染 | 五屏逐屏读数：金币、客流、单客收益、构成、累计、差额、余额预览 | 无头 Chrome + 驱动器 `hud` 步骤取运行时 Label 字符串（唯一能归因到"显示的是哪个数"的手段）；视口 ≥ 产物画布，点击送达有断言 |
| 异常态 | 金币不足（解锁 + 升级两处）、已满级、未解锁铺位、以及至少一类服务端拒绝码 | 每条各一次真实进程 e2e 实测读数，不看截图观感下结论 |
| 包体 | 主包体积增量 | 出微信包报数字，与 M04/M05 基线（65.1%）对比记录 |

**本模块不测**：好友页与排行榜（M08）、微信登录与云存档（M07）、真机 `EVENT_HIDE` 与实机手感（并入 M05 遗留那一批）、正式字体与真素材替换后的对稿（M03 真素材仍 0/13）。

## 7. 明确不做

- 不做好友页内容，只留导航入口的占位态。
- 不做「按天回溯历史账单」（§8-9 的既定取舍，分段不落时间，做不到）。
- 不把数值写进客户端（改价只改 `config/`）。
- 不顺手改 M04/M05 已验收的主界面布局与 HUD 口径，除非对稿时发现新冲突并先登记裁定。

## 8. 待主理人裁定（本模块开工前）

> ✅ **2026-09-22 主理人已裁，本节六条全部结清，下表保留作提案原文备查**：
> - **D-1 / D-2 / D-3 = 「三个都补」**：由服务端在快照里补三个派生字段（逐店单价拆分、当日累计收益、升级后单价），界面完全按稿做，客户端不碰规则常量 → 落点见 §9.2。
> - **D-4 = 允许纯算术**：客户端对服务端字段做**不含规则常量的加减**（差额、余额预览）不构成违规推导；同时**追认 §八-17-D** 那段 ADR 0001「口径澄清」的写入程序。口径注解补进 `docs/architecture/adr/0001` 与本文件 §9.2。
> - **D-5 满级态 = 按本节建议执行**（`Lv5 MAX` 徽章 + 隐藏升级预告 + 主按钮收成「已达最高等级」禁用态），已升为 §9 的一条需求落点。
> - **§八-17-B/C**：随 D-1 与验收第 4 条一并结清；§八-17-A（「5 秒内」措辞）与 §八-17-D 的程序追认由主理人同批处理，回写见 §9.6。

| # | 事项 | 我的建议 |
| --- | --- | --- |
| **D-1** | 单客收益「基础 + 满铺」的构成由谁给 | 服务端逐店补两个派生字段（合计已有，补拆分），客户端只渲染。理由：§6.2 明令客户端不得自行判定满铺 |
| **D-2** | 经营页「今日收益」 | 二选一：服务端补当日累计增量；或改稿砍掉这一格（改稿要同时改需求文档 §6.2 那句「当日收益」） |
| **D-3** | 升级后单价 | 按 GDD §6.1 原文补进快照，属「改实现向规格对齐」的默认方向 |
| **D-4** | 「再攒 N 金币」与「余额」这类纯减法 | 裁一条边界：允许客户端对服务端字段做**不含规则常量的加减**（与 M05 的做差先例一致），并追认 §八-17-D 那段 ADR 0001 澄清的程序 |
| **D-5** | 满级态的界面形态（稿 04/06 都没画） | 建议：等级徽章显示 `Lv5 MAX`、隐藏升级预告两行、主按钮收成禁用态「已达最高等级」；确认后我把它当需求补进 §3.3 |
| **§八-17-B/C** | `unitPrice` 的用途边界 + 「显示金额与本轮入账一致」这条验收 | 沿用 M05 复核的建议：§6.1 明确它「是该店当前的单客收益档位，不等于本轮客人按什么价入账」；第 5 节验收 #4 我按这个口径先写进本文 |

> 六条里 **D-1 ~ D-4 决定界面能不能按稿做出来**，D-5/D-6 只影响局部文案与形态。可以一次裁完，也可以先裁 D-1~D-4、其余走默认建议由我在技术方案里落实后再对。

---

# 第二部分 · 技术方案（关卡③ · **2026-09-22 主理人已确认，可开工**）

> 已拍板：**P-1 = 代码建节点 + 单场景分层**；**P-2 = Graphics 按稿色值画，零新素材**；
> **`schemaVersion` 2 → 3，开发期删档重建**；§9.2c 的写命令日志收口默认一并做。

## 9.1 两个待拍板的架构点

这两条我不替你定，选了就要写进实现，改回来成本高。

### P-1 五屏的节点树怎么建

| 方案 | 做法 | 利 | 弊 |
| --- | --- | --- | --- |
| **甲（推荐）** | `Mall.scene` 只加 3 个空容器（`Pages/Manage`、`Pages/Layout`、`Overlay`）；页面与弹窗的节点全部由新脚本在运行时按 @750 设计坐标建 | 不碰 89KB 手写场景的序列化字段（**本机没有 Cocos 连接器**，`agents.md` 点名的正是这条风险）；结构在 `.ts` 里可读可 diff 可 review；沿用 M05 已裁定的「代码建节点」先例；导航与 HUD 常驻、切页只切 `active`，轮询与串行队列不打断（直接满足验收 9） | 布局坐标写在代码里；编辑器里看不到页面成品，只能出包看 |
| 乙 | 五屏全部手写进 `.scene` + 配对 `.meta` | 编辑器可见、可继续可视化微调 | 手写上千行 Cocos 序列化 JSON 并配对 uuid，引用极易错；`.scene` 的 diff 不可读，code-review 形同虚设；每次调布局都要重导验证 |
| 丙 | 每页一个 `.scene`，`director.loadScene` 切页 | 页面彻底隔离 | 切场景要重建 store / 轮询 / 表演层；而稿上三导航与 HUD 是**同屏常驻**，这条路反而更难做 |

### P-2 弹窗的木面板与角标用什么画

| 方案 | 做法 | 利 | 弊 |
| --- | --- | --- | --- |
| **甲（推荐）** | 用 `Graphics` 按稿上色值画（面板 630 宽 / 圆角 22 / 描边 6px、遮罩 `rgba(74,47,30,.58)`、锁与加号角标），色值集中到一个主题文件 | **零新素材入库**、主包体积不涨；设计稿本身就是 CSS 画的，按色值复现是最忠实稿的做法；不被 M03（真素材 0/13）卡住 | 与最终摄影级美术有落差，真素材到位后要换件（登记为遗留项） |
| 乙 | 把工程外 `assets/_staging/` 的 `ui_panel_wood`(179KB)、`ui_tag_wood`、`ico_plus` 三件挪回 `bundles/ui-main/` | 更接近终态观感 | M04 实测这批预留件让主包从 65.1% 涨到 72.0%（**Bundle 整目录进包，不看引用**），现在回挪就是立刻付这笔体积；而 `ui_panel_wood` 本身只是 interim 参考件 |

## 9.2 后端改动（`internal/game`、`internal/api`）

**a. 逐店单价拆分 + 升级后单价（纯派生，不动存档）**

`unitPrice` 现在一函数混算两件事（`service.go:398-404`）。拆成两个具名派生，再让 `ShopView` 各给一份：

| 字段 | 取法 | 不变式 |
| --- | --- | --- |
| `unitPriceBase` | `cfg.Shops[i].LevelCoinsPerVisitor[level-1]` | — |
| `floorBonus` | `floorFull(state, floor)` 为真时取 `cfg.FullFloorBonus`，否则 0 | `unitPriceBase + floorBonus == unitPrice` |
| `nextUnitPrice` | 下一级的 base + **同一个** `floorBonus`；`level == maxLevel` 时为 `null` | 升级不改满铺状态（`floorFull` 只看 `unlocked`+`prepared`，与 level 无关），故加成不变 |

稿 06 的「升级后 基础 8 + 满铺 1」里的 base 由 `nextUnitPrice − floorBonus` 得出（属 D-4 已裁允许的纯算术，不引新字段）。

**b. 当日累计收益 `todayEarned`（动存档）**

- `State.TodayEarned int64` + `MallView.TodayEarned int64`。
- 累加点：`accrue` 的逐客人循环里与 `Coins`/`Revenue` 同处（`service.go:529-533`）。不加溢出分支——`TodayEarned ≤ Σ shop.Revenue`，而 `Revenue` 的 `maxSafeInteger` 门禁已在前面。
- 归零点：`accrue` 的业务日翻转分支（`service.go:483-490`），与 `CapFrozen`/`DailyVisitorCap` 一起重置。
- 自证：`validateState` 补一条 `0 <= TodayEarned <= Σ shops[].Revenue`（与既有账目恒等式同族，不是新式防御）。
- 语义写进注释与 GDD 对表：**只含到店收益，不含解锁/升级支出**（那两笔走 `Spent`，稿上的「今日收益」是 `+332` 这种纯进账）。
- ⚠️ **破坏性变更**：`schemaVersion` 2 → 3。不升的话旧档的 `todayEarned` 反序列化为 0，而当天其实已赚过钱——那会让界面上挂一个**看不出来的错数**，比删档更糟。按 `agents.md` 与 `架构现状` §8-6 的既定取舍，开发期删档重建。**这条要单独点头。**

**c. 写命令日志（§7.2 冲突 E 的收口，可选）**

`respondOperation` 已同时知道 path 与结果码，补两个日志字段即可让「结算 / 解锁 / 升级」三类写操作可按操作类型与结果码追踪（B5.6 要求、M02 起一直「仍成立」）。M06 是这两条命令第一次变成玩家可点的按钮，此时收口最省。**建议做**，不推就把 E 留着。

**d. 路由与请求体：0 改动。** 七条路由原样，写命令仍只收 `{}`。`config/development.json` **不改**（无一个数值变动 ⇒ 规则指纹不变）。

## 9.3 客户端改动（`NewProject/assets/scripts`）

| 文件 | 改动 |
| --- | --- |
| `ApiTypes.ts` | 镜像新字段：`MallView.todayEarned`、`ShopView.unitPriceBase/floorBonus/nextUnitPrice` |
| `ApiClient.ts` | 新增 `unlockSlot(slotId)`、`upgradeShop(shopId)` 两个命令（复用现有 `request`，错误码原样抛） |
| `MallStore.ts` | `StoreOp` 扩 `"unlock" \| "upgrade"`；`StoreTransport` 注入两个新命令；新增两个入队方法（走同一条串行链，玩家点击不被 busy 拦） |
| `MallScene.ts` | 挂页面层与弹窗层；绑三导航切页；S3 铺位点击 → 店铺详情弹窗；把 M05 的临时开店入口按稿收口到经营页的「开业」按钮（那条已裁定的偏离自此结案） |
| **新** `MallPages.ts` | 经营页 + 布局页：按 @750 稿坐标代码建节点，渲染**同步且幂等**（沿用 `MallScene` 的既有约束：预加载取帧、渲染不引异步） |
| **新** `MallPopups.ts` | 详情 / 解锁确认 / 升级确认三弹窗 + 遮罩 + 关闭；确认后**只发命令，不本地改数**，成功用响应内新快照重渲染 |
| **新** `MallErrors.ts` | 稳定错误码 → 玩家可读提示的唯一映射（验收第 8 条）。刻意做成**不 import "cc"** 的纯映射，`tools/m05_probe.ts` 才能逐条断言「每个码有提示、互不相同、不甩码」；MallScene 的招牌 / 页内 / 弹窗三处提示位共用它 |
| **新** `MallTheme.ts`（仅 P-2 选甲时） | 稿上色值与字号集中一处（美术圣经 §2.2 / §8.4），避免逐屏散落。**实测后扩了职责**：页面层与弹窗层共用的 Graphics 原语（圆角牌 / 锁与加号角标 / 纱罩 / 折线）也收在这里——两屏画同一套角标，散成两份必然漂移 |
| `scene/Mall.scene` | **未改**。P-1 甲的原文写「加 3 个空容器」，实现时页面与弹窗节点全部挂在 Canvas 下由代码建（与 M05 客人同先例），场景一个字节没动——比原方案更省，回写更正 |

**素材：P-2 选甲则 M06 零新素材入库。** 页面里需要的店面缩略图全部复用 `bundles` 里现有的 `shop_*_open` / `slt_f*_empty`（现状是占位图，真素材替换属 M03）；客人飘字与 HUD 件 M05 已就位。

**架构约束（写进代码注释与 review 清单）**：`ApiTypes.ts` 的「客户端只读不推」继续成立 ⇒ 界面里允许出现的运算只有两种：服务端字段之间的加减（D-4 已裁）、以及逐层「已开业数 / 该层铺位数」这类**纯计数**（GDD §6.1 明文许可）。任何 `+1`「满铺值」、单价表、解锁价、升级价都不得出现在 `scripts/` 里。

## 9.4 任务清单（按序，每项独立可验）

> **进度（2026-09-23，含当日二次复跑）**：1–8 已完成并过质量门；**独立复核已于 2026-09-24 做掉**（SC-M06-QA-002，另会话只喂规格 §1–§8 与设计稿、明令不读本文第二部分，结论见 §10.8 与 `docs/M06-验收口径评审.md`）。
> 1–3 的实测读数：`gofmt -l .` 无输出（LF 归一化副本上零输出；工作区因 `core.autocrlf=true` 全列 CRLF，属环境假象）、`go vet ./...` 无告警、`internal/api` ok、
> `internal/game` 全 PASS、`node --experimental-strip-types tools/m05_probe.ts` **28 项全通过**（M05 原 20 项 + 本模块 ⑧ 组 4 项 + ⑨ 组 4 项）。
> 覆盖率（2026-09-23 复跑）：`internal/game` 97.3% / `internal/api` 99.2%。⚠️ `-race` 在本机不可用：Windows Application Control 策略拦截 cgo 的 gcc，与代码无关。
> 反向验证三处，逐条确认断言不是空转：去掉「下一级单价沿用同一笔加成」→ `TestShopViewPriceSplit` 红；
> 去掉「跨日归零」→ `TestServiceShanghaiDayRefresh` 红（报 `17 vs 累计 10`）；
> 去掉 `TodayEarned > Σrevenue` 自证 → 子用例「当日收益盖过累计收益」红；
> 去掉成功路径的结果码 → `TestWriteCommandLogsCarryResultCode` 红。

1. ✅ 后端 a：`unitPrice` 拆分 + `nextUnitPrice` 派生 → Go 单测（含满级返回 null、满铺刚生效那一轮 base/bonus 相加恒等于 unitPrice）
2. ✅ 后端 b：`todayEarned` 落档 + 跨日归零 + `validateState` + `schemaVersion` 3 → Go 单测；✅ 后端 c：写命令结果码日志（§7.2 冲突 **E** 一并结清）→ `gofmt -l .` / `go vet ./...` / `go test -count=1 ./...` 全绿
3. ✅ 客户端镜像：`ApiTypes` / `ApiClient` / `MallStore`（+`unlock`/`upgrade` 两个入队方法）+ 探针 ⑧ 组；ADR 0001 已补「纯算术边界」一节
4. ✅ 经营页（含「开业」按钮收口 M05 临时入口）→ 出包 + `hud` 读数对稿
5. ✅ 布局页（含主按钮可解锁 / 金币不足两态、满铺进度）→ 同上
6. ✅ 三弹窗（详情 / 解锁确认 / 升级确认，含禁用态与满级态）→ 出包 + `hud` 读数对稿，实测见 §10.3
7. ✅ 异常态专项：金币不足两路径、`MAX_LEVEL`、未开业置灰、未解锁不开弹窗、失败保留成功值 → 真实进程 e2e，实测见 §10.3；六个拒绝码另用 curl 对真实后端逐个复现（2026-09-23 复跑留证，见 §10.5）
8. ✅ 包体数字：**2772.2 KiB = 主包上限 4 MiB 的 67.7%**（2026-09-23 本 worktree 重建复现，与 §10.3 读数逐字一致；引擎 cocos-js 1453.4 KiB，增量 +10.4 为 Graphics 功能裁剪回退，其余 +93.4 全在脚本编译产物——**零新素材入库**，P-2 甲兑现）；文档回写已完成（本文件 + `开发规划文档` v1.19 + `架构现状` §7.3）。**当前权威数字为 2773.4 KiB = 67.7%**（2026-09-24 在含 M07 合并的同一 HEAD 上重跑，+1.2 KiB 全在 assets 内的脚本编译产物，见 §10.8）；独立复核已做：**SC-M06-QA-002 有条件放行**（无 P0/P1，三条 P2 中一条待裁定，见 §10.8）

## 10. 任务 4–5 的实测记录（2026-09-22，web-desktop + 真实后端 + 无头 Chrome）

取证配方：`.work/m06-save.json` 新档（不碰既有存档）→ 后端 `127.0.0.1:18081` → 静态服务 `127.0.0.1:8123`
→ `node tools/m05_web_drive.mjs http://127.0.0.1:8123 .work/qa-m06 <步骤>`。
读数通道有两条并互相印证：运行中 Label 的 `hud` 读数，与页面层自己打的 `[m06] 渲染` 摘要行。

| 判据 | 实测读数 | 判定 |
| --- | --- | --- |
| 经营页字段与三态 | 咖啡「单客 7 金币（6+满铺1）· 累计 25 客流 · 160 金币」营业中；花束「单客 10 金币（9+满铺1）· 累计 10 客流 · 100 金币」营业中；衣橱/甜屋/书店「未开放 · 需先在布局页解锁（600/800/1200 金币）」 | ✅ 逐行状态互斥、价格取服务端配置 |
| 累计收益不得现推（验收 3） | 咖啡 `25 客流 · 160 金币`，而 `25 × 7 = 175 ≠ 160` | ✅ 界面显示的是服务端 `revenue`，不是「客流 × 当前单价」 |
| 今日收益（D-2 的新字段） | 一位客人到店后概览格从 `+0` 变 `+6`，与 HUD 金币 `1280 → 1286` 同步 | ✅ 未冻结时 `— / —`、冻结后 `1/35` |
| 满铺加成（D-1 的拆分） | 开满一层后：咖啡 `6 → 7`、花束 `9 → 10`，构成行同时出现 `（6+满铺1）`；加成格「一层生效」 | ✅ `+1` 来自服务端字段，客户端无该常量 |
| 布局页（稿 01） | 「二层 · 楼面 0 / 3 已开放」「一层 · 临街 2 / 2 已开放」；加成两行完整；主按钮「解锁「衣橱」铺位 600 金币（可解锁，不透明度 255）」 | ✅ 与稿的进度、卡片三态、主按钮文案一致 |
| 切页与导航 | 点「经营」后 `NavManage` 从 y −579 移到 −581（按下态下沉 2px）+ 亮度 −12%；三次点击全部「✔ 引擎已收到」；异常 0 条 | ✅ |

### 10.1 这一轮改掉的三个真问题

1. **切页后不渲染**（最严重）：`render` 只在服务端状态变化时被调，而空闲 settle 是空转、被
   `revision` 守卫整份丢掉 ⇒ 新页面挂着构建时的占位串，且「营业中/开业按钮/锁牌」三个状态节点
   **同时可见并互相盖住点击**——点「开业」实际命中的是锁牌，走成了跳布局页。
   修法：`show()` 里立刻用当前快照渲染一次（`MallPages.show`）。
2. **`Graphics` 被引擎功能裁剪砍掉**：`cc.Graphics` 不在产物导出表里，`addComponent` 抛
   `Error 3804（Type must be non-nil）`，整个页面层建不起来、Mall 场景启动失败。
   修法：`settings/v2/packages/engine.json` 的 `graphics` 开为 `true` 并加进 `includeModules`
   （等价于在「项目设置 → 功能裁剪」里勾选）。⚠️ **这条会动包体**，任务 8 的体积数字必须体现它。
3. **两处对稿偏差（已按稿修正或按实调整，登记在此）**：
   - 布局页加成卡原本按累计间距算到 1052，压进主按钮（1009）底下 ⇒ 层间距改回稿值 9px、
     卡高 150 → 132；页内错误提示位从主按钮上方移到顶栏右侧（顶栏标题宽度相应 400 → 200）。
   - 经营页行内主文案我一开始写成 26px，稿是 22px ⇒ 22px + 文案去掉「铺位」二字，
     否则「未开放 · 需先在布局页解锁铺位（1200 金币）」会压到右侧锁牌上。
     稿上这一行本身有两套措辞，取带价格的那套为准。

### 10.2 取证通道本身的两条事实（影响后续所有模块）

1. **`window.cc` 只在引擎重构建后的产物里可用**：改功能裁剪会触发重构建，之后 `hud` 步骤才读到
   Label。第一次跑（未重构建的产物）报「页面里没有 window.cc」。所以 `AGENTS.md` 里
   「`hud` 是唯一能归因到显示的是哪个数的手段」这句，前提是产物带得出引擎全局。
2. **Chrome 会复用磁盘缓存**，导致「改了没生效」的假阴性（同一份旧 `cc.js`，报错字节偏移都一致）。
   驱动器已加 `Network.setCacheDisabled`。这条不修，之后每次重建后的取证都可能是读旧产物。
3. **`window.cc.UIOpacity` 在 web-desktop 产物里不存在**（2026-09-23 实测，驱动器 `hud` 的 `engine` 自检列报
   `UIOpacity:false`，而 `Label`/`Graphics`/`Sprite` 都在）。后果：按类取组件返回 null；按名
   `getComponent("UIOpacity")` 也取不到（类名被压成 `e`）。**明度通道**（禁用态 78% 那一列）因此只能
   靠"对组件列表按有没有数值型 `opacity` 字段做鸭子类型"来读——驱动器 `opacityOf` 已按
   「按类 → 按名 → 鸭子类型」三档降级实现，取不到一律报 `null` 而**不报默认值 255**。
   ⚠️ 教训：早先只写第一档并把 null 兜底成 255，等于把禁用态的 78% **假验**了一遍——
   取证通道里任何"读不到"都必须显式露出来，不许兜底成"正常值"。

### 10.3 任务 6–8 的实测记录（2026-09-23，web-desktop + 真实后端 + 无头 Chrome）

取证配方沿用 §10：后端 `127.0.0.1:18081`、静态服务 `127.0.0.1:8123`、
`node tools/m05_web_drive.mjs <url> <outDir> <步骤>`。异常态各用一份**构造存档**
（`.work/m06-save-poor.json` 金币 50、`.work/m06-save-max.json` 咖啡 Lv5、
`.work/m06-save-flow.json` 当日还剩 20 位客流），构造时把 `capFrozen/visitorsRemaining` 定死，
取证期间数字静止、读数可逐字对；存档恒等式（`coins == 1280 + Σrevenue − spent`、
`0 <= todayEarned <= Σrevenue`、时间戳与业务日同天）全部满足 `validateState`。
驱动器本轮新增 `reqs:<标签>` 步骤：报「这一步以来发了几个 `/api/v1/` 请求、其中几个是写命令」，
验收第 9 条与「禁用态点了不发命令」自此有硬证据（早先只能看客户端日志，容易把"打了日志"当成"发了请求"）。

| 判据 | 实测读数 | 判定 |
| --- | --- | --- |
| 详情弹窗（稿 04） | 咖啡 Lv2 营业中；单客 `8 金币`、构成 `基础 7 + 满铺 1 = 8`；累计 `50 位 / 335 金币`；预告 `升到 Lv3 后 单客 9 金币 (+1) / 花费 700 金币`；主按钮 `升级到 Lv3` + 加号角标 + 不透明度 255 | ✅ 逐字段对稿；累计两行逐字等于服务端原值（335 ≠ 50×8，验收第 3 条） |
| 升级确认（稿 06） | `升级「咖啡」 Lv1→Lv2`；当前 `7（基础 6 + 满铺 1）` → 升级后 `8（基础 7 + 满铺 1）`；`每位客人多赚 1 金币`；`− 300 / 1348 / 1048 金币`；规则说明含 `已经赚到的 160 金币不会改变` | ✅ 升级后基础价 = `nextUnitPrice − floorBonus`（D-4 纯算术）；余额预览与提交后 HUD 金币逐字一致 |
| 解锁确认（稿 05） | `二层 · 1 号铺位「衣橱」`；前后同图（左纱罩+锁、右亮）；`− 600 / 1540 / 940 金币`；三条变化清单含 `（一层已生效）` | ✅ 提交后服务端只出现一条 `POST slots/f2-s1/unlock`，卡片转「待开业」、主按钮推进到 `解锁「甜屋」800 金币` |
| 开合弹窗 / 切页不额外发请求（验收 9） | 切经营页、开详情、开升级确认、开解锁确认、遮罩关闭：**每一步 0 个请求**；升级提交那一步恰好 1 个写命令、无补拉快照 | ✅ 轮询节拍不变（5 秒一次 settle） |
| 金币不足 · 布局页主按钮 | `再攒 550 金币` + `600 金币`、不透明度 199、锁角标；点击后服务端**无** unlock 行 | ✅ 三重编码齐、不发写命令 |
| 金币不足 · 详情弹窗 | 咖啡 Lv4：`再攒 1950 金币`、不透明度 199、锁角标、预告仍显示（稿 04 右图同构）；点击 0 写命令 | ✅ |
| 满级（裁定 D-5） | 徽章 `Lv5 MAX`、预告整块隐藏、主按钮 `已达最高等级` + 无角标 + 199；面板高度收掉 107（不留空白）；点击 0 写命令 | ✅ |
| 未开业铺位的详情 | 衣橱：状态 `待开业`（无对勾）、`基础 12 + 满铺 0 = 12`、累计 `0 位 / 0 金币`、预告隐藏、主按钮 `开业后才能升级` + 锁 + 199；点击 0 写命令 | ✅ §8-8 边界的正面验收（字段有值也不画成可升级） |
| 未解锁铺位（验收 7） | 点二层未开放卡：日志 `点了未开放铺位 f2-s2，解锁入口在本页主按钮`，**不出弹窗**、0 请求 | ✅ |
| 失败保留成功值（验收 8） | 弹窗开着时停掉后端：6 次 settle 全失败，三次读数金币恒 `1065`、客流恒 `50/50`；弹窗内提示 `连接中断，稍后会自动重试` | ✅ 数值不跳变、提示看得见 |
| 六个拒绝码 | curl 真实后端逐个复现：`SHOP_NOT_FOUND`（店铺/铺位两路）、`SLOT_LOCKED`、`SLOT_ORDER`、`SHOP_NOT_OPEN`、`MAX_LEVEL`、`INSUFFICIENT_COINS`；客户端映射由探针 ⑨ 组逐条断言 | ✅ 码与提示一一对应；UI 侧因门禁齐全走不到这些码，属设计使然 |
| 显示金额与入账一致（验收 4） | 同一现场：飘字 `+8` 与弹窗 `单客 8 金币` 同值；变价那一轮服务端入账 `+7`（旧价）而弹窗显示新价 `8`，与探针 ⑧ 的取价用例同口径 | ✅ 飘字读入账价、弹窗读当前档位，互不顶替 |
| 包体（验收 10） | `python tools/m04_build.py`：合计 **2772.2 KiB = 主包上限的 67.7%**（基线 65.1% / 2668.4 KiB）；其中引擎 `cocos-js` 1453.4 KiB（+10.4，Graphics 功能裁剪），其余 +93.4 KiB 全在脚本编译产物——**本模块零新素材入库**（P-2 甲兑现） | ✅ 增量可归因 |

**这一轮改掉的三个真问题**（都先有实测证据再动代码）：

1. **布局页「n / m 已开放」按已营业数算**：解锁 f2-s1 后标题停在 `0 / 3`，玩家花掉的 600 金币在界面上
   等于没发生。§3.2 给的口径是 `unlockedSlots`。已改：标题读新增的 `unlockedOn`（纯计数），
   满铺加成卡维持已营业数（加成要等开业）；解锁确认的「进度变成 n / m」同口径，并删掉
   「解锁后立即满铺」的错误分支（解锁只让铺位**可开业**）。复验：解锁后标题 `1 / 3 已开放`。
   已登记 `架构现状` §7.2 冲突 **P**。
2. **满级/未开业态面板下方空一整块**：隐藏预告两行后按钮上移、面板高度没跟着收。已加
   `resizeDetail`：只重画面板与奶白板、内容区尺寸不动但**重新按父节点高度摆位**——
   `place()` 的 y 由父节点高度算，第一版连内容区一起改尺寸，结果内容比板面高出一截、
   标题压到木框上（截图实测），改成自顶向下 `place(panel) → place(inner) → place(content)` 才锚对。
3. **取证通道的明度列是假验**：见 §10.2-3。

**按实调整（稿上没画、登记在此）**：

- 待开业铺位的详情形态（稿 04 只画了营业中）：状态写「待开业」且不给对勾，主按钮禁用、
  文字「开业后才能升级」+ 锁角标——服务端对未开业升级会拒 `SHOP_NOT_OPEN`，画成可点就是骗玩家。
- 满级不给锁角标（它是终态不是"被挡住"），三重编码里的形状通道改由 `Lv5 MAX` 徽章承担。
- 构成条在未满铺时照写 `基础 12 + 满铺 0 = 12`（服务端 `floorBonus` 给 0，不藏）。
- 稿 06 的规则说明框是 3px **虚线**描边，Graphics 没有虚线，退成同色实线。
- 弹窗的失败提示落在面板上方的遮罩区（稿上没画失败态），三屏共用一条，不占面板几何。

### 10.4 独立复核的安排（写验分离）

本轮（实现方会话）内跑的探针 ⑨ 组与 e2e 都**只算自验**。按项目口径，M06 的验收门要另开会话、
只喂 `docs/M06-页面与弹窗.md` §3/§5 与 diff 区间、明令不读本文结论；在那之前本模块状态位保持 🔄。
本轮若另起 agent 做审查，结论一律记为「加强自查」，不得顶替那道门。

### 10.5 任务 8 的补齐复跑（2026-09-23，feat-popups-store-upgrade worktree）

上一轮（主仓库会话）把任务 6/7 的 e2e 跑完后，任务 8 有两件没有落盘证据：**微信包体数字**只出现在
会话输出里、**六个拒绝码的 curl 复现**没有保存读数。本轮在同一提交（`5adfb0f`）的工作树副本上
重建复现，并先把上一轮的全部取证（`.work/qa-m06-pop` 的 runA–runE、`qa-m06-flow`、`qa-m06-float`、
`qa-m06-poor`、`qa-m06-max`、`qa-m06-fail` 六份 run.txt 与 hud-reads）逐行核对过——§10.3 表里
每一行读数都能在那些文件里找到原样出处，任务 6/7 的判定不需要重跑即已成立。

| 判据 | 本轮实测读数 | 判定 |
| --- | --- | --- |
| 质量门复跑 | `go vet ./...` 无告警；`go test -count=1 ./...` 全绿（`internal/game` 97.3% / `internal/api` 99.2%）；`node --experimental-strip-types tools/m05_probe.ts` **28 项全通过** | ✅ 与任务 1–3 的原始判定一致 |
| `gofmt` | 工作区 `gofmt -l .` 列出全部 9 个 Go 文件——逐文件归一化 CRLF→LF 后复跑**零输出**，是 `core.autocrlf=true` 的环境假象，非格式问题 | ✅ 判据修订登记（此前「无输出」的表述在本机复现不了） |
| `-race` | 本机不可用：Windows Application Control 策略拦截 cgo 所需的 gcc（`fork/exec …llvm-mingw…gcc.exe` 被拒）。与代码无关，属环境限制 | ⚠️ 登记；M02/M05 当时的 `-race` 全绿结论不受影响（同一台机器的策略是后装的） |
| 六个拒绝码（验收 8） | 对 `go build` 出的真实后端逐个 curl：`INSUFFICIENT_COINS`（解锁 50<600 与升级 50<2000 两路）、`SLOT_LOCKED`（锁铺位开店）、`SLOT_ORDER`（跳序解锁）、`SHOP_NOT_OPEN`（已解锁未开业升级）、`SHOP_NOT_FOUND`（店铺/铺位两路）、`MAX_LEVEL`（Lv5 再升级）；读数落盘 `.work/qa-m06-errors-codes.txt` | ✅ 码与 HTTP 状态逐条对应（409/404） |
| 包体（验收 10） | `python tools/m04_build.py`：合计 **2772.2 KiB = 4 MiB 的 67.7%**（cocos-js 1453.4 KiB、assets 1132.2 KiB、src 28.4 KiB），AppID 补丁 `wx6ac3f5090a6b99c5 → wx5a00e2270436e6e0` 生效；产物 `assets/main/index.js` 含 `MallPages.ts` 等 M06 编译产物（退出码 36 为已知 SIGTERM 收尾噪音）。落盘 `.work/qa-m06-build-size.log` | ✅ 与 §10.3 的 67.7% **逐字一致**；增量可归因：引擎 +10.4（Graphics 裁剪回退）+ 脚本 +93.4，零新素材。⚠️ 此数为**复核修复前**的构建；修复五项后的最终构建为 **2772.5 KiB = 67.7%**（`.work/qa-m06-final2-build.log`，脚本 +0.3 KiB），§9.4-8 与规划文档以最终数为准 |
| 存档构造 | 三份构造档从上一轮会话复制复用（指纹 `6e297a10…` 与未改动的 `config/development.json` 一致，`validateState` 全过）：poor（金币 50/仅一层/咖啡 Lv4）、main（f2-s1 已解锁/衣橱未开业/咖啡 Lv2）、max（咖啡 Lv5） | ✅ 恒等式满足 |

**独立复核已做（子代理代跑，记「加强自查」）**：结论与逐条处置见 §10.6；取证期间的环境干扰见 §10.7。

### 10.6 独立复核（加强自查）的记录

> 按 §10.4，子代理代跑的结论**不得顶替**主理人亲自开的那道验收门。

**2026-09-23 复核结论（SC-M06-QA-001，子代理代跑，只喂 §3/§5 规格 + `ee6c3bb..HEAD` diff，明令不读 §8/§9/§10）**：**有条件放行**——十条验收静态核对 8 条通过、2 条带瑕疵；无 P0；3 个 P1 + 5 个 P2。逐条处置（P1 全部已修并复验，P2 修 2 条、登记 3 条）：

| # | 级别 | 问题 | 处置 |
| --- | --- | --- | --- |
| P1-1 | P1 | `MallPages.ts` 字段重命名没改全：声明 `manageCells`、使用 `todayCells`（TS2339 类型错误 + 死字段）。构建链纯转译不报，运行期不炸，故未升 P0 | ✅ **已修**（声明改名 `todayCells`）。运行期本就正确（页面读数一直对），修复后类型一致 |
| P1-2 | P1 | 写命令成功路径上 `MallScene.onStoreUpdate` 先 `render()` 后 `acknowledgeWrite()`：解锁后 `nextSlotId` 前移、升满级后 `upgradeCost` 变 null，`renderUnlock/renderUpgrade` 的有效性断言必失败 → 每次解锁打一条**假 error**「数据不再成立」并 `closeAll()`；**升到满级那一次详情弹窗被连带关掉**，「Lv5 MAX / 已达最高等级」的当场交代丢失，违背本文件 §3.3 与 §5-6 | ✅ **已修**：两处有效性分支加 `if (this.pending) return;`——写命令在飞时响应里的"数据不再成立"是预期（服务端已受理），关弹窗只由回执驱动。**e2e 复验**（`.work/qa-m06-fix4`）：Lv4→Lv5 提交后确认层关掉、**详情留在原地**显示 `Lv5 MAX / 已达最高等级 / 预告隐藏 / 面板收高 107`，控制台无「数据不再成立」，请求台账恰好 1 个写命令 |
| P1-3 | P1 | 金币账三行颜色未按稿设置：`buildMoneyBox` 注释声称「花费金色、余额绿色」，渲染只设 `string` 不设 `.color`；稿 05/06 的 `.mrow .neg{ gold-deep }` / `.left{ green-deep }` 落空 | ✅ **已修**：`renderUnlock/renderUpgrade` 对花费行设 `C.goldDeep`、余额行设 `C.greenDeep`。**e2e 复验**（eval 读运行时 Label 颜色）：`cost [200,138,16]=#C88A10`、`balance [60,78,60]=#3C4E3C`、现有行默认 `#4A2F1E`，与稿逐字一致 |
| P2-1 | P2 | 「全部铺位已解锁」终态带锁角标 + 78% 压暗——锁=被挡住，与"已全部开放"语义相反 | ✅ **已修**：终态不给锁、不压暗（255）。**e2e 复验**（`.work/qa-m06-fix6/fix7`）：主按钮 `全部铺位已解锁` + `actionDim 255` + 点击 0 写命令；截图留证 |
| P2-4 | P2 | 满铺加成卡「已生效」由客户端逐层计数判定（`opened === total`），而 ADR 0001 纯算术一节明列「判满铺是否生效」为**禁止项**，服务端本就有逐店 `floorBonus` 可答 | ✅ **已修**：`MallPages/MallPopups` 的 `fullFloors` 与布局页 `fullFloor` 改判 `shops[].floorBonus > 0`，计数只留在显示位（`n / m`）。**回归复验**（`.work/qa-m06-fix7`）：布局页两行加成读数与改前逐字一致（一层 `2 / 2 已满铺（已生效）`、二层 `1 / 3`）。已登记 `架构现状` §7.2 冲突 **Q** |
| ⚠️ 2026-09-24 核账 | — | 上表 P2-4 那行的两句在盘上**都核不上** | ①「已登记冲突 Q」——`架构现状` §7.2 实际停在 P，`git show e9c4279 -- docs/architecture/架构现状.md` 只改了文件头与 §7.3 一行，**Q 从未写进去**（本轮已补登，见 §10.8）；② 加成行「二层 `1 / 3`」与本文件 §10.3 修复 1 的复验读数「加成行仍二层 `0 / 3`」**自相矛盾**，且在盘的 `.work/qa-m06-pop/runE.txt:18` 支持 `0 / 3` 那一侧。fix7 的 run 文件随 worktree 删除丢失，无法裁定当时到底读到什么——登记为遗留，e2e 结案时一并重取 |
| P2-5 | P2 | `LOCAL_ONLY`（handler.go:82，403）没进 `MallErrors` 映射表，会落到兜底串 | ✅ **已修**：表内补 `LOCAL_ONLY`，探针 ⑨ 的「其余码」清单同步补该码（漏配要在那里红）。协议层三码（`JSON_REQUIRED`/`BODY_TOO_LARGE`/`INVALID_COMMAND`）客户端恒不可达，按「不为不可能发生的场景配文案」不补 |
| P2-2 | P2 | 好友页缺「未开放」占位 UI（§3.6 要求「只保留入口与『未开放』占位」），实现仅入口 + console.log | ⬜ **登记不修**：六屏设计稿没有好友页，「没有设计图的页面不动工」是项目原则；占位形态需主理人先定（建议 M08 启动时一并做） |
| P2-3 | P2 | 未开放行文案删「铺位」二字（稿为「…解锁铺位（800 金币）」） | ⬜ **登记不修**：§10.3 已记（两套稿文取带价格那套，否则压到右侧锁牌），属按实调整 |

**修复后的质量门复跑**：`go vet` / `go test -count=1 ./...` 全绿；探针 **28 项全通过**；`gofmt` LF 归一化零输出；**最终包体 2772.5 KiB = 67.7%**（较修复前 2772.2 KiB 增加 0.3 KiB，全在脚本编译产物）。⚠️ 按 §10.4，本次复核 prompt 由实现方起草、跑在本会话的子代理里，结论记「加强自查」；**正式验收门已于 2026-09-24 由另一会话跑掉，见 §10.8**。

> ⚠️ **本表引用的取证文件不在盘**：`qa-m06-fix4` / `fix6` / `fix7` 的 run 记录、`qa-m06-errors-codes.txt`、`qa-m06-final2-build.log` 留在已删除的 worktree 里，主仓库 `.work/` 只有修复前那一轮（`qa-m06-pop` / `float` / `flow` / `poor` / `max` / `fail`）。⇒ 上表的 e2e 复验读数**只剩文档抄本**，包体已在本轮重跑（§10.8），其余待 e2e 结案时重取。

### 10.7 取证期间的环境干扰（2026-09-23，跑 e2e 前务必先读）

本轮复跑撞上一个**并发的自动化客户端**（下称「幽灵」）：它在驱动器命令启动后约 2 秒加载游戏页面、约 8 秒时先手发一笔写命令（升级/解锁），反复污染构造存档——五轮 e2e 有三轮的初始状态被它改掉。证据链与结论：

- **它从标准端口 8123 进场**：前一天遗留的静态服务（PID 37140，9/22 23:51 启动）一直占着 8123，提供的是**主仓库旧构建**；本轮重建 8123 后又被它继续利用。**任何在 8123 上提供的新构建都会把幽灵导向当时后端所在的端口**（baseUrl 是编译期烧进包里的）。
- **隔离方案（有效）**：后端换 `18082` + 静态服务换 `8124` + 独立存档文件 + `BuildConfig.ts` 改指 18082 后重建，并**关掉 8123**。之后幽灵仍会加载（它跟着驱动器命令走），但：① 终态/禁用态下它的点击发不出写命令；② 构造存档的 `lastObservedAt` 若略晚于真实时刻，`CLOCK_BACKWARDS` 守卫会把它的先手写命令挡掉（`.work/qa-m06-fix4` 实录：幽灵 15:59:43 的升级被拒，我方 16:00:08 的升级 OK）。
- **判定证据是否被污染的方法（本轮每条都做了）**：驱动器 `api-requests.json` 台账 ↔ 后端服务日志逐条对时间戳，全对得上才采信读数。`qa-m06-fix4`（升满级）与 `qa-m06-fix6/fix7`（终态 + 回归）**完全干净**；`qa-m06-fix2/fix3/fix5` 三轮被污染、读数作废。
- **遗留**：幽灵真身未查明（机器上有一个 9/21 起常驻的 `chrome-devtools-mcp` 进程 PID 44052，疑似并行会话的浏览器自动化；已确认无定时任务）。**后续跑 e2e 前先 `Get-NetTCPConnection -LocalPort 18081,8123` 看端口归属，起服务后核对监听 PID 与所提供构建的字节数**——这是 M04 时代「旧进程占端口」教训的静态服务版。

### 10.8 正式独立复核（SC-M06-QA-002，2026-09-24）

> 按 §10.4 的口径执行：主审会话**未参与 M06 实现**（代码在 `feat-popups-store-upgrade` 与 `feat-implement-m07` 两个 worktree 会话里完成并合入），复核员只喂**本文第 1–170 行（§1–§8，含六条裁定）+ 五张设计稿 + 上游规格**，明令不得读第 171–427 行、也不得读规划文档的 M06 行与 `架构现状` §7.2。逐条判定、源码证据、反向验证全文在 `docs/M06-验收口径评审.md`，这里只回写状态与新增事实。

- **总判定**：**有条件放行**。无 P0 / 无 P1；十条验收 7 条 ✅、验收 1 带对稿瑕疵、验收 4 与 10 本轮只判到「源码可判的部分 + 补数」（主理人选择静态复核，未重跑 e2e）。
- **复核方自跑的质量门**：`go vet` 干净；`go test -count=1 ./...` 全绿（含 M07 新增的 `internal/session`）；探针 **28 项全通过**。
- **反向验证两条**（不复用他人结论）：删 `internal/game/service.go:744` 的 `+item.FloorBonus` ⇒ `TestShopViewPriceSplit` **FAIL**；把 `MallStore.ts:91` 飘字取价的 `before.unitPrice` 改成 `shop.unitPrice` ⇒ 探针 **26 通过 / 2 失败**。⇒ 验收 3 与验收 4 背后的两条断言**不是空转**。
- **两条比"看日志"硬的结论**：`MallPages.ts` / `MallPopups.ts` **不 import** `ApiClient` / `MallStore` ⇒ 切页与开合弹窗在结构上发不出请求（验收 9）；禁用态的拦截发生在 handler 之前 ⇒「点了不发写命令」不只是改了文案（验收 5）。
- **三条 P2 的处置**（逐条回源码验过，未照单全收）：
  - **002-01 成立、交裁定**：楼层标题（已解锁数，`MallPages.ts:512`）与满铺加成卡（已营业数，`:507`）同屏两分子。**根因在稿不在代码**——稿 01 的 `:215` 拿解锁数当加成分子，而 `floorFull`（`service.go:415-421`）要的是「每铺位既解锁又开业」，照稿数字实现会出现「3 / 3 却没有加成」。登记为 `架构现状` §7.2 冲突 **R** + 规划文档 §八-18①。
  - **002-02 成立、属对稿偏差**：经营页列表顶取 358（稿 336），五行底缘 1120 距禁放线 1121 只剩 1px；同类提示位在布局页已按 §10.1-3 挪去顶栏右侧，两页不一致。登记于 §八-18②。本轮**未改**（改后须出包用 `hud` 验顶栏不重叠，属 e2e 范畴）。
  - **002-03 一半驳回**：「括号改全角」是**误报**——§3.1 第 47 行原文本就是全角「（800 金币）」，实现逐字一致；「删铺位二字」与 §10.3 及上一轮 P2-3 重复，不另计。
- **包体（验收 10）**：同一 HEAD（已含 M07 合并）重跑 `python tools/m04_build.py` ⇒ **2773.4 KiB = 4 MiB 的 67.7%**（assets 1133.4 / 引擎 cocos-js 1453.4 / src 28.4），日志 `.work/qa-m06-size-post-m07.log`。较 §10.5 的 2772.5 涨 1.2 KiB，**全在 assets 内的脚本编译产物**（M07 给客户端 +94 行），引擎与素材未动。**此数取代 §10.5 / §10.6 的过期数字**。
- **本轮另两处核账**（主审会话回文件验，复核员不知情）：① §10.6 声称登记的冲突 **Q** 实际从未写进 `架构现状` §7.2，本轮补登；② §10.6 的 P2-4 回归读数「加成行二层 `1 / 3`」与 §10.3 修复 1 的「加成行仍 `0 / 3`」**互斥**，在盘的 `.work/qa-m06-pop/runE.txt:18` 支持后者。
- **M06 转 ✅ 前仍欠三件**：① 002-01 / 002-02 两条口径待主理人裁；② 一次 e2e 结案（验收 1 的转灰度可分、验收 4 的同场对账、六个拒绝码的 UI 侧实到）——跑前先按 §10.7 查端口归属；③ 冲突 **O**（主界面「待开业」铺位直开）原样待裁。
- **这道门的成色**：机制上仍是「主审会话派子代理」，残留弱点是那份 prompt 由读过 §9/§10 的会话起草（未转述给复核员）。要最严格形态需主理人亲自开只贴规格路径的会话，已在 `docs/M06-验收口径评审.md` §0.4 登记。

### 10.9 裁定后的两条修复与复验（2026-09-24，主理人裁「两条一起改」）

改动只有两处，全在 `MallPages.ts`（**零新增素材、零后端改动、零规则常量**）：

| # | 改动 | 落点 |
| --- | --- | --- |
| 002-01 | 满铺加成卡的措辞按**开业**口径改：未生效行由「n / m —— 再开放 K 个铺位」改为「n / m **已开业** —— 再开 K 家店即可获得 +N」；楼层标题「n / m 已开放」（已解锁数）不动 | `MallPages.ts:525-532` |
| 002-02 | 经营页页内提示位挪到**顶栏右侧**（与布局页同一槽、同一组坐标，`buildTopBar` 已把标题收成 200 宽让出 x≥320），列表顶回稿值 **336** | `MallPages.ts:248-256` |

**复验配方**（隔离端口，按 §10.7 的前置检查做过）：后端 `127.0.0.1:18082` + 静态服务 `127.0.0.1:8124` + 独立存档副本 `.work/qa-m06-fixA-save.json`（从 `m06-save.json` 复制，主存档未动）；取证四轮 A/B/C/D/E 落 `.work/qa-m06-fix*-run.txt`。

| 判据 | 实测读数 | 判定 |
| --- | --- | --- |
| 002-01 新文案 | 布局页 `boost=["一层 2 / 2 已满铺 —— 该层单客收益 +1（已生效）","二层 0 / 3 已开业 —— 再开 3 家店即可获得 +1"]`，同一屏标题仍 `1 / 3 已开放`（`qa-m06-fixA-run.txt:19`） | ✅ 「已开放 / 已开业」两词分开，同屏两分子不再撞车 |
| 002-02 列表回稿值 | 可点坐标 `PageManage/List/Row0/OpenButton` 由 `(271,236)` 变 `(271,258)`（+22），末行中心 `-358` ⇒ 底缘 **1098**，距禁放线 1121 的余量从 1px 回到 **23px** | ✅ |
| 失败保留成功值（验收 8，当前包复验） | 停后端后 6 次 settle 全失败，金币恒 `1515`、客流恒 `50/50`、五行读数逐字不变（`qa-m06-fixA-run.txt:25-27`） | ✅ |
| 切页 / 开合弹窗不额外发请求（验收 9，当前包复验） | 整轮台账 `GET config | GET mall | POST settle ×6`，**写命令 0 个**；两次切页与一次开弹窗各自的 `reqs` 步都是 0 新增 | ✅ |
| 状态机随服务端切换点击语义（顺带撞到） | 同坐标 `(271,-50)` 在「待开业」时发 `POST shops/clothing/prepare`（`qa-m06-fixD-run.txt:21`，写命令 1 个）；该店已营业后再点同一处变成**开详情弹窗、0 请求**（`qa-m06-fixE-run.txt`） | ✅ 两条路径各自正确，无串扰 |
| 包体（验收 10） | 改后重跑 `python tools/m04_build.py` = **2773.4 KiB = 67.7%**，与 §10.8 同数（改动是字符串与坐标，不进素材）；注释收敛后再出一份仍 **2773.4 KiB = 67.7%**（`.work/qa-m06-fixF-wechat.log`，web-desktop 同步重出） | ✅ 无体积代价 |
| 质量门 | `go vet` 无告警；`go test -count=1 ./...` 全绿（api / game / session）；探针 **28 项全通过** | ✅ |

⚠️ **一条没验到 + 一条新登记（SC-M06-QA-002-04，P2 待查）**：
- 没验到：002-02 挪走的**页内提示位本身是否显示**——五轮 `hud` 里 `PageManage/Notice`、`PageLayout/Notice` **从未读到非空值**，而同一时刻招牌位读到过「连接中断，稍后会自动重试」（`qa-m06-fixA-run.txt:26`）。 ⇒ 位置正确性由"与布局页同坐标"保证，可见性未取证。
- 新登记的疑点：`MallScene.onStoreError` 把同一条信息写给**当时可见那一页**的提示位（`MallScene.ts:182`），切页不重放；而横幅只停 3 秒、轮询节拍 5 秒，导致这条提示**要么落在看不见的页上、要么落在读取窗口外**。数值与写命令行为都不受影响（本轮已证），是"失败提示要看得见"（验收 8）在页面层的一个缺口。修法候选：`MallPages.show()` 里重放最后一条提示文本；或页内提示位不自清、由下一次成功渲染清。待裁定后单独做。

## 9.5 风险与已知的不安点

1. **`Mall.scene` 又要手改**：即使只加 3 个空容器，也得走「改完出一次包验证导入不报错」这条固定流程（`agents.md`）。选 P-1 甲时这个面已经压到最小。
2. **P1-4 的老问题会在弹窗上放大**：`mutate` 先 `accrue` 后 `apply`，同一条 Prepare/Upgrade 响应里 `unitPrice` 是**新价**、客人按**旧价**入账。M05 靠「单价取上一份快照」绕过；详情/升级弹窗是**常驻展示**，必须按验收第 4 条实测对账，不能沿用「下一轮自愈」的说法。
3. **满铺进度的「再开放 K 个」是纯计数**，但「该层单客收益 +1」里的 `+1` 只能来自 `config.fullFloorBonus` 的显示值。这条边界若被写成客户端常量，就是直接违反 ADR 0001。
4. **`todayEarned` 删档**会让 M05 已记录的实测读数（`— / —`、`2/35`、`+6`）在新档上重现一次，需要重跑取证确认没被本模块改坏（回归验证）。
5. **设计稿的字体现是系统字体占位**，正式字体未子集化 ⇒ 逐屏对稿时「字号一致」只能按 px 判，字形观感不作为判据（稿自己的「待办依赖」也是这么写的）。

## 9.6 确认后要做的文档回写

- `production/开发规划文档.md`：§二 M06 状态位改 🔄；§八-17 的 B/C/D 三条按 2026-09-22 裁定结清（A 条随主理人另裁）；§三 M06 的验收标准按本文件 §5 具体化，**修订记录留痕**。
- `docs/architecture/架构现状.md`：冲突 **M**（升级后单价）随实现结清；冲突 **N**（当日收益）随实现结清；冲突 **E** 若做则结清；§6 路由与 §3 自证清单补 `todayEarned` 一条；§8-8 那条边界补一句「M06 已按 `prepared`/`unlockedSlots` 置灰，正面验收见本模块验收 7」。
- `docs/architecture/adr/0001`：把 D-4 这条边界（**允许对服务端字段做不含规则常量的加减**）正式并入那条「口径澄清」，并注明程序由主理人 2026-09-22 追认。
- `design/gdd/系统-经营与成长.md` §6.1：**只在主理人明确要改措辞时动**，本模块默认不改规格原文。
