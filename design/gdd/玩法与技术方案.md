# 《街角百货铺》玩法与技术方案

> 版本 v1.4 · 2026-09-15（v1.3 → v1.4：M02 落地完成，按实际实现标注 B4 / B5 的实现状态；v1.2 → v1.3：N-4 补注 B5.2 分段触发点以《系统-经营与成长》§4.3 为准、N-6 标注 B4.2 `ALREADY_UNLOCKED` 不实现；v1.1 → v1.2：同步 C3-3 客流上限冻结时机口径；v1.0 → v1.1：同步当日拍板的 9 项决策）
> 上游文档：《需求文档》。下游文档：《开发规划文档》
> **文件名说明**：本文档原名「玩法与技术方案（草案）.md」，已于 **2026-09-15** 重命名为「玩法与技术方案.md」（原因：文件名残留的「（草案）」与文档实际状态不符——标题早已无括号、内容已至 v1.4 且为正式上游依据）。历史文档中大量以「**草案 A4**」「**草案 B5.2**」等**简称**引用本文档，该简称沿用自旧文件名、**含义不变**，故历史正文（尤其 ADR 与评审记录）未逐一改写。
> 本文档分两部分：**A 玩法方案**（玩法规则与数值）与 **B 技术方案**（实现方式）
> 所有数值均为**开发期初版**，集中放在规则配置里，调数值不改代码
>
> **2026-09-14 决策同步记录**（用户拍板，已落入本文档）
> | 决策 | 落点 |
> | --- | --- |
> | 移除手动关店 | A3、B4.2（接口删除）、B5.3（校验不放宽） |
> | 一层临街优先轮询 | A4（并标注 config 数组顺序需改） |
> | 升级的可见回报（四级阶梯） | A3 新增小节 |
> | 「取舍」改为解锁与升级的排序 | A3 新增小节 |
> | 满级后无长期出口 = 已知缺口 | A5 新增小节 |
>
> 未落入本文档的其余决策：素材试打样与键色、统一加雨棚、顾客头身比、UI 材质语言、HUD 5 槽、金币保留 `$`（用户 2026-09-14 明确「与参考图一模一样」） —— 均属视觉与素材流程，落在 `design/art-bible/` 与《需求文档》6.1。
>
> **v1.2 修订记录（2026-09-14，SC-P2-005）**：同步 **C3-3 冻结时机口径**——客流上限在「**当天首次实际产生客人的结算**」（`openShopCount ≥ 1`）时冻结（0 店开业不冻结、不落盘），取代旧表述「每天业务日第一次结算时 / 跨日刷新时确定」。依据 `docs/M02-验收口径评审.md` §6.4 传播链排查（本文档此前未随 GDD v1.1 跟改）。
>
> | 位置 | 原内容 | 处置 |
> | --- | --- | --- |
> | A4 客流模型 | 「上限在每天业务日第一次结算时确定并写死当日值」 | 改为「当天首次实际产生客人的结算时确定」（0 店不冻结） |
> | B5.2 存档 schema · `dailyVisitorCap` 行 | 「跨日刷新时确定后写死当日值」 | 改为「当天首次实际产生客人的结算时确定」（0 店不冻结、不落盘） |
> | B5.3 校验规则 · 客流上限行 | 「与存档中的 `dailyVisitorCap` 一致，且不超过理论上限」 | 按《系统-经营与成长》§5.7 同步为 `capFrozen` 二态校验（未冻结 ⇒ 占位 0；已冻结 ⇒ 落在合法档位集合） |
>
> **v1.3 修订记录（2026-09-14，SC-P2-006）**：清理 `docs/M02-验收口径评审.md` §7 复核发现的 9 项 P2 一致性问题中归本文档的 2 项（N-4 / N-6）。**本文档为历史文档，不重写正文**，仅加注 / 标注。均不阻塞放行、不改任何验收判定。
>
> | 位置 | 原内容 | 处置 |
> | --- | --- | --- |
> | **N-4** B5.2 账单分段 | 「单价变化只有两个触发点——升级、所在层满铺状态变化」 | 加注：**现行口径以《系统-经营与成长》§4.3 为准**，为**三个**触发点（新增「开店 / `prepare`」建立首个分段）；本节正文保持历史原貌不改 |
> | **N-6** B4.2 错误码表 | 仍列 `ALREADY_UNLOCKED` | 标注「**不实现**（C1-1 拍板：重复解锁为幂等成功 `changed=false`）」，与同表 `/close` 行的处置方式一致 |
>
> **v1.4 修订记录（2026-09-15，SC-M02-001）**：M02 后端扩展**已实现**（实现者自验通过、主理人独立复现质量门全绿；**质量侧的实现独立复核尚未完成**）。本文档仍为历史文档，**不重写正文**，仅按实际实现**标注实现状态**，并指向新的实现文档 `docs/M02-后端扩展.md`。实现**未做任何数值取舍**，规则数值全部照抄《系统-经营与成长》§3.1。
>
> | 位置 | 原状态 | 实现状态 |
> | --- | --- | --- |
> | B4.1 M01 已实现（5 条路由） | 已实现 | **仍已实现**（`/api/v1/mall` 返回体由存档 `State` 改为客户端视图 `MallView`，见下） |
> | B4.2 `/api/v1/slots/{slotId}/unlock` | 需要新增（M02） | ✅ **已实现**（入参 `{}`，`slotId` 形如 `f2-s1`） |
> | B4.2 `/api/v1/shops/{id}/upgrade` | 需要新增（M02） | ✅ **已实现**（入参 `{}`，`id` 为 `shopId`） |
> | B4.2 新增错误码 `SLOT_LOCKED` / `SLOT_ORDER` / `INSUFFICIENT_COINS` / `MAX_LEVEL` | 计划 | ✅ 全部实现；另**新增 `SHOP_NOT_OPEN`**（对「待开业」店铺升级），对应《系统-经营与成长》§5.2 |
> | B5.1 规则配置 v1 → v2 | 计划 | ✅ 已实现（`rulesVersion = m02-v2`；`dailyVisitors` 删除，`shops[]` 顺序改为一层优先，轮询顺序由数组顺序决定、未另写顺序配置） |
> | B5.2 存档 schema v1 → v2 | 计划 | ✅ 已实现（`unlockedSlots` / `capFrozen` / `dailyVisitorCap` / `spent` / `level` / `segments`） |
> | B5.3 校验规则调整 | 计划 | ✅ 已实现（金币下界放宽为 `coins >= 0`；金币恒等式加 `− spent`；逐段恒等式；`unlockedSlots` 加载校验；客流上限二态校验） |
> | B5.4 版本兼容 | 计划 | ✅ 已实现（指纹变化 ⇒ 旧存档拒绝加载且保留原文件；开发期直接删档，未写迁移工具） |
> | B6 存档与容错 | 计划 | ✅ 沿用 M01 机制（原子替换、损坏拒绝、时钟回退、数值上限），新增分段账目后仍保持自证 |
>
> **B4.1 返回体形态变化的说明**：`GET /api/v1/mall` 与所有命令返回体的 `state` 由**存档结构**改为**客户端视图 `MallView`**——新增 `capFrozen` / `dailyVisitorCap`（未冻结为 `null`）/ `visitorsServed` / `unlockedSlots` / `nextSlotId` / `nextUnlockCost` / `shops[].level` / `shops[].unitPrice` / `shops[].upgradeCost`；**不再下发** `shops[].segments` 与 `nextShopIndex`（账单明细与轮询游标只留在存档里）。该变化属接口契约调整，已记入 `docs/M02-后端扩展.md` §3.2。

---

# A 玩法方案

## A1 核心循环

```
打开游戏
  → 一眼看到今天的客流与金币
  → 做一件花钱或赚钱的事（开店 / 解锁铺位 / 升级店铺）
  → 客人按固定节奏到店，金币缓慢增长
  → 当天客流耗尽，收益停在当天的上限
  → 次日 0 点客流恢复，回到第一步
```

循环的**长度是"一天"**：玩家不需要在一天内多次回来，也没有任何"错过就亏"的时间窗。

## A2 楼层与铺位

| 层 | 铺位 ID | 位置 | 对应店铺 | 解锁条件 |
| --- | --- | --- | --- | --- |
| 二层 | `f2-s1` | 左 | 衣橱 | 600 金币 |
| 二层 | `f2-s2` | 中 | 甜屋 | 800 金币 |
| 二层 | `f2-s3` | 右 | 书店 | 1200 金币 |
| 一层 | `f1-s1` | 左 | 咖啡 | 开局已解锁 |
| 一层 | `f1-s2` | 右 | 花束 | 开局已解锁 |

- 二层必须**从左到右依次解锁**（不能跳过中间直接开右边）。
- 铺位与店铺在 v1 是**一对一固定绑定**。v1 不做店铺换位（换位会牵动存档模型与美术表现，列入后续版本）。
- 未解锁的铺位显示为空铺（绿棚木门、橱窗为空）。

## A3 单客收益

```
单客收益 = 该店当前等级的单价 + 该层满铺加成(0 或 1)
```

**等级单价表（初版）**

| 店铺 | Lv1 | Lv2 | Lv3 | Lv4 | Lv5 |
| --- | --- | --- | --- | --- | --- |
| 咖啡（一层） | 6 | 7 | 8 | 10 | 11 |
| 花束（一层） | 9 | 10 | 12 | 14 | 16 |
| 衣橱（二层） | 12 | 14 | 16 | 19 | 22 |
| 甜屋（二层） | 8 | 9 | 11 | 13 | 15 |
| 书店（二层） | 10 | 12 | 14 | 16 | 19 |

**满铺加成（v1 唯一的"搭配"规则）**

- 该层所有铺位**都已解锁且都已开业** → 该层每家店的单客收益 **+1 金币**。
- 一层满铺（2/2）与二层满铺（3/3）各算各的，互不影响。
- 同一层只要有一家店处于「未开放」或「待开业」，该层加成不生效。

> 为什么用"固定 +1"而不是"百分比 +10%"：百分比会让 6 变 6.6、9 变 9.9，产生小数和四舍五入误差，低等级升级会被取整吃掉。固定加值全程整数，玩家看得懂，服务端也好校验。

### 搭配规则的设计意图（2026-09-14 修正）

原本的设想是让「先把一层凑满」和「攒钱解锁二层」形成一次取舍。**但这个取舍并不成立**：客流上限随店铺数增长、满铺加成又叠加其上，算下来"能开就开"永远最优。

真实的取舍发生在**「解锁铺位」与「升级店铺」的先后排序**上——金币只有一份时，是先解锁一家新店，还是先把现有的店升一级。**G5《经济与平衡》的目标是让这两个选择在不同阶段各有优势，而不是让其中一个永远正确。**

### 等级的可见回报（2026-09-14 拍板）

升级不能只涨数字。四个等级各对应一次场景变化，实现方式全部是**材质参数 + 挂件**，不新增立绘：

| 等级 | 场景变化 | 实现 |
| --- | --- | --- |
| Lv1 | 基准 | — |
| Lv2 | 橱窗暖光变亮 | 材质参数（自发光 +8%） |
| Lv3 | 招牌挂一串小彩灯 | 复用资产 `prp_lights` |
| Lv4 | 门口加一个花箱 | 复用资产 `prp_planter` |
| Lv5 | 招牌描金边 | 材质参数（描边色 → `gold`） |

> 四个等级变化只需 **2 个新资产 + 3 个参数**。视觉规格见《美术圣经》§7.7。

## A4 客流模型

- **到店间隔**：初版 5 秒一位。
- **每日客流上限**：`基础 20 + 每家已开业店铺 × 15`

| 已开业店铺 | 客流上限 |
| --- | --- |
| 咖啡 + 花束 | 50 |
| + 衣橱 | 65 |
| + 甜屋 | 80 |
| + 书店 | 95 |

- 上限在**当天首次实际产生客人的结算**（`openShopCount ≥ 1`，即至少一家店已开业并开始接待客人）时**确定并写死当日值**；**0 店开业时不冻结、不落盘**（尚无店铺营业，本轮不产生客人）。当天中途开店不补当天客流，次日生效。
- 客人按固定顺序轮转并**跳过未开业的店铺**，顺序为**一层临街优先**（2026-09-14 拍板）：
  `咖啡 → 花束 → 衣橱 → 甜屋 → 书店`

> ⚠️ **与现有实现的差异**：`config/development.json` 里 shops 数组当前是 `衣橱 → 甜屋 → 书店 → 咖啡 → 花束`（二层在前）。M02 落地时必须把数组顺序改成上面的一层优先顺序，**轮询顺序由数组顺序决定，不要另写一份顺序配置**。本节的日均收益数值全部按"咖啡优先"算，改数组即对齐。
- 不足一个到店间隔的时间余量**结转到下一次结算**，不丢弃。
- 跨业务日只刷新当天客流，**不补算离线期间的历史客流**，也不扣除任何东西。

### 为什么客流上限要跟着店铺数量涨

如果客流上限固定，开一家单价低于当前均价的店会**把同一批客人分走，导致日均收益下降**。用固定上限 50 算一遍就能看到这个反向激励：

| 已开业店铺 | 轮转顺序 | 日均收益（固定 50 客流） | 变化 |
| --- | --- | --- | --- |
| 咖啡 + 花束 | 6, 9 | 375 | — |
| + 衣橱 | 6, 9, 12 | 447 | +72 |
| + 甜屋 | 6, 9, 12, 8 | **435** | **−12（花了 800 金币反而变差）** |
| + 书店 | 6, 9, 12, 8, 10 | 450 | +15 |

改成"客流随店铺增长"后，每一家新店都有明确回报，解锁成本才有意义：

| 已开业店铺 | 客流上限 | 日均收益（全 Lv1） | 较上一档增量 |
| --- | --- | --- | --- |
| 咖啡 + 花束 | 50 | 375 | — |
| + 衣橱 | 65 | 582 | +207 |
| + 甜屋 | 80 | 700 | +118 |
| + 书店 | 95 | 855 | +155 |

## A5 经济模型

### 产出

`当日产出 = 客流上限内轮转到的每一位客人的单客收益之和`

各阶段日均产出（含满铺加成）：

| 阶段 | 日均产出 |
| --- | --- |
| 开局（一层 2 店 Lv1） | 375 |
| 二层全部解锁（5 店 Lv1，满铺加成生效） | 950 |
| 五店全部满级（Lv5 + 满铺） | 1672 |

### 消耗

| 项目 | 单价 | 说明 |
| --- | --- | --- |
| 解锁 `f2-s1` 衣橱 | 600 | 日均 +207 → 约 3 天回本 |
| 解锁 `f2-s2` 甜屋 | 800 | 日均 +118 → 约 7 天回本 |
| 解锁 `f2-s3` 书店 | 1200 | 日均 +155 → 约 8 天回本 |
| 店铺升级 Lv1→2 | 300 / 店 | 五店共 1500 |
| 店铺升级 Lv2→3 | 700 / 店 | 五店共 3500 |
| 店铺升级 Lv3→4 | 1200 / 店 | 五店共 6000 |
| 店铺升级 Lv4→5 | 2000 / 店 | 五店共 10000 |

- 解锁全部二层铺位的总投入 **2600 金币**，按开局 375/天计算约需 7 天（期间还会同步升级一层）。
- 五店全部满级的总投入 **21000 金币**，属于长线目标（从 950/天 提到 1672/天，回收期约 29 天）。**不要求玩家达成**。
- 金币只进不出的情况在**前中期**被排除了：只要有没解锁的铺位或没满级的店，金币就一直有去处。

### 已知缺口：满级之后金币没有去处（2026-09-14 记录）

全部铺位解锁 + 全部店铺满级之后（总投入约 **2600 + 21000 = 23600 金币**），金币不再有任何消耗出口，会无限累积。第一版**不做长期出口**，但这里显式记录为已知缺口，不假装它不存在。

候选解法（留待后续版本设计，第一版不实现）：新楼层或新店铺品类、装修换肤、季节活动。**真正要避免的是为了消耗而堆一套没有意义的新货币**——那会直接违反「数字要克制」这条原则。

### 失败态

**没有。** 没有破产、没有倒闭、没有客流差评、没有负债。玩家唯一的"惩罚"是进度慢一点。

## A6 业务日与离线

- 业务日按 `Asia/Shanghai` 的 0 点切分（与 M01 已实现的规则一致）。
- 跨业务日：刷新客流上限与剩余客流，**累计收益与店铺状态全部保留**。
- 离线：不产生收益，也不扣任何东西；不弹"你离开了 3 天"。
- 服务器时钟回退时拒绝结算（避免重复计算），M01 已实现。

## A7 好友经营排行榜

- 排序依据：**总金币**（服务端权威值）。
- 数据来源：客户端把服务端返回的金币上报到微信托管数据，再用开放数据域读取好友数据并渲染排行榜。
- 一个好友都没有时：显示自己的分数与名次，不留空白页。
- 第一版不做互访、互助、留言。设计图里的邮戳信纸（`13.jpg`）留给后续的互访留言功能。

---

# B 技术方案

## B1 术语对照（写给不熟悉游戏引擎的人）

读下面的方案前先对一下词。引擎里的概念基本都能映射到通用前端/后端概念：

| Cocos / 引擎概念 | 等价于你熟悉的东西 | 在本游戏里指什么 |
| --- | --- | --- |
| 场景 Scene | 一个页面 / 一个路由视图 | `Boot` 启动场景、`Mall` 主界面场景 |
| 节点 Node | DOM 元素 | 楼层、铺位、按钮、文字都是节点，靠父子层级组织 |
| 组件 Component | 挂在元素上的行为脚本（类似 React 组件 / 混入） | `ShopView` 负责一家店的状态与显示 |
| 预制体 Prefab | 可复用的组件模板（一次定义、多处实例化） | `Guest` 预制体，每来一位客人实例化一个 |
| 资源 Asset / Bundle | 静态资源 / 代码分割后的懒加载 chunk | 店铺立绘放独立 Bundle，不进首包 |
| 帧循环 `update(dt)` | `requestAnimationFrame` 回调 | 推进客人走动的每一帧 |
| 锚点 Anchor / 坐标系 | CSS 的定位原点 + `transform-origin` | 铺位对齐靠锚点，不靠手调像素 |
| 图集 Atlas（自动图集） | 雪碧图 sprite sheet | 把 UI 小图合并，减少渲染批次 |
| Draw Call | 一次 GPU 绘制批次 | 太多会掉帧，所以要合图集 |
| 补间 Tween | CSS `transition` / `animation` 的代码版 | 金币飘字、客人走位 |

## B2 总体架构与职责划分

```
微信小游戏客户端（Cocos Creator 3.8.8，工程 NewProject/）
        │  HTTPS + 会话令牌
        ▼
Go 服务端（模块 streetcorner，仓库根目录）
        │
        ▼
存档（开发期：本地单文件 / 上线后：云端存储）
```

**职责红线**

| 归服务端 | 归客户端 |
| --- | --- |
| 金币、客流、收益、等级、解锁状态 | 展示与动画 |
| 业务日判断与跨日刷新 | 玩家操作意图的收集与提交 |
| 时间推进与时间余量 | 请求节流、重试、错误提示 |
| 所有数值计算 | 不做任何数值计算，也不上报金币/时间/客流 |

M01 已经把这条红线用接口约束住了：`POST` 命令只接受空 JSON 对象 `{}`，客户端塞任何字段都会被 400 拒绝（已实现并有测试覆盖）。

## B3 客户端方案

### B3.1 引擎与目标平台

- 引擎：Cocos Creator **3.8.8**（`NewProject/package.json` 已确认）。
- 构建目标：**微信小游戏**（AppID `wx5a00e2270436e6e0`，名称「街角百货铺」）。目前工程是全新未配置状态（无设计分辨率、无资源、AppID 未写入构建配置）。
- 构建产物需要在微信开发者工具里跑通，才能进入实机验证。

### B3.2 分辨率与适配

- 设计图 `主界面图.jpg` 为 1080×1918，比例 9:16。
- 设计基准分辨率取 **750×1334**（同为 9:16，便于按 1.44 倍关系换算设计图的像素值）。
- 适配策略：**等比缩放 + 高度贴合（Fit Height）**，宽度方向按机型实际比例左右延展，超出 9:16 的机型用背景延伸而不是拉伸主体。
- 底部 HUD 固定在屏幕底部，并避开 iOS 底部横条安全区。

### B3.3 场景与节点结构

```
Boot（启动场景）
  └─ 拉取 /api/v1/config 与 /api/v1/mall → 进入 Mall
Mall（主界面场景）
  ├─ Marquee        顶部招牌「街角百货铺」+ 绿色雨棚
  ├─ Floor2         二层楼面
  │   ├─ Railing    木栏杆 + 猫（装饰）
  │   └─ Slot ×3    f2-s1 / f2-s2 / f2-s3
  ├─ Floor1         一层临街
  │   ├─ Staircase  中间楼梯（装饰）
  │   └─ Slot ×2    f1-s1 / f1-s2
  ├─ GuestLayer     客人层（运行时动态实例化）
  └─ Hud            底部 HUD（固定）
      ├─ CoinIcon + CoinLabel
      ├─ NavButton ×3（经营 / 布局 / 好友）
      └─ VisitorLabel（已到店 / 上限）
```

**关键预制体**

| 预制体 | 说明 |
| --- | --- |
| `Slot` | 一个铺位。三种表现：未解锁（空铺立绘）、已解锁未开业（空铺 + 开门状态）、已开业（对应店铺立绘） |
| `ShopView` | 店铺立绘 + 招牌，挂在 `Slot` 下 |
| `Guest` | 一位客人。正/侧/背三视图按行走方向切换 |
| `Hud` | 底部木条底板 + 金币 + 三个圆形按钮 + 客流 |
| `Panel` | 通用圆角木面板（`10.jpg`），所有弹窗复用 |

### B3.4 资源管线

1. **素材必须先抠图**：现有 19 张图全是 JPG，`3/4/5/7/8/9` 等看起来透明其实是烘焙进图里的棋盘格，JPG 本身也没有 alpha 通道。**全部素材需要统一跑抠图流水线转成真正的透明 PNG**，再压缩后导入。
2. **目录约定**
   ```
   assets/scenes/        Boot / Mall
   assets/prefabs/       Slot / ShopView / Guest / Hud / Panel
   NewProject/assets/textures/shop/ 店铺立绘、空铺位底图
   NewProject/assets/textures/ui/   HUD 底板、图标、面板
   NewProject/assets/textures/guest/ 客人
   assets/scripts/       组件脚本
   ```
3. **首包/分包策略**：启动必需的 UI 底图与图标进主包；店铺立绘、空铺位底图放独立 Bundle（等价于前端懒加载 chunk），按需加载。主包体积上限需按官方最新文档核对。
4. **合图集**：UI 小图（图标、按钮）走自动图集合并，减少 Draw Call。

### B3.5 表现层（v1 最小可玩）

- 客人：从商场入口沿路径走到目标店铺门口 → 停留约 0.5 秒 → 店铺位置金币飘字 → 淡出消失。
- 客人外观按行走方向切三视图（`6.jpg` 已提供正/侧/背）。
- 店铺状态区别：已开业用原立绘，未解锁用空铺位底图（`1.jpg` / `2.jpg`）。
- 金币变化用 Tween 补间数字滚动，不做逐枚硬币飞行动画（省性能）。

### B3.6 与服务端的同步节奏

- 进入前台 / 打开游戏：拉一次 `/api/v1/mall` 全量快照。
- 停留在主界面期间：每 5 秒调一次 `POST /api/v1/mall/settle` 拉增量，返回体自带最新快照与本次收益，客户端只做补间动画。
- 切到后台：停止轮询，回到前台立即**结算一次**（`POST /api/v1/mall/settle`，服务端会把这段时间的客流算出来，返回体自带最新快照）。
  > ✏️ **2026-09-21 主理人裁定改写**（架构现状 §7.2 冲突 K）：本条原写「立即拉一次快照（服务端会把这段时间的客流算出来）」，括注与实现矛盾——`Service.Snapshot()`（`internal/game/service.go:255`）明确「不结算收益、不刷新业务日」，照字面拉 `GET /api/v1/mall` 拿不到后台期间的客流，HUD 的业务日也可能停在上一天。故改的是**规格这句**，不是实现。
  > ⚠️ 约束：回前台这一下是**写命令**，会推进服务端状态并可能落盘，不得实现成心跳或高频触发。
- **客户端不跑"虚拟时间"**：界面上的客流与金币永远以最近一次服务端响应为准。

## B4 接口契约

### B4.1 M01 已实现

> ✅ **M02 实现状态**：5 条路由全部仍在；`/api/v1/mall` 与命令返回体的 `state` 已由存档结构改为客户端视图 `MallView`（见文首 v1.4 记录）。

| 方法 | 路径 | 入参 | 说明 |
| --- | --- | --- | --- |
| GET | `/healthz` | 无（免令牌） | 本机健康检查 |
| GET | `/api/v1/config` | 无 | 规则配置 |
| GET | `/api/v1/mall` | 无 | 存档快照（不结算、不刷新日期） |
| POST | `/api/v1/shops/{id}/prepare` | `{}` | 开店 |
| POST | `/api/v1/mall/settle` | `{}` | 结算 |

### B4.2 需要新增

> ✅ **M02 实现状态**：`/api/v1/slots/{slotId}/unlock` 与 `/api/v1/shops/{id}/upgrade` 均已实现；新增错误码 `SLOT_LOCKED` / `SLOT_ORDER` / `INSUFFICIENT_COINS` / `MAX_LEVEL` 已实现，另补 `SHOP_NOT_OPEN`（对「待开业」店铺升级，依据《系统-经营与成长》§5.2）。**两套 ID 不得混用**：slotId 与 shopId 互相传错一律 404 `SHOP_NOT_FOUND` 且零副作用。

| 方法 | 路径 | 入参 | 说明 | 所属模块 |
| --- | --- | --- | --- | --- |
| POST | `/api/v1/slots/{slotId}/unlock` | `{}` | 解锁铺位，`slotId` 形如 `f2-s1` | M02 |
| POST | `/api/v1/shops/{id}/upgrade` | `{}` | 升级店铺 | M02 |
| ~~POST~~ | ~~`/api/v1/shops/{id}/close`~~ | — | **不实现**：第一版不做手动关店（见《需求文档》4.2） | — |
| POST | `/api/v1/session/wechat` | `{ code }` | 微信登录换会话 | M07 |

**错误码沿用 M01 已定义的稳定集合**（`INVALID_COMMAND` / `UNAUTHORIZED` / `LOCAL_ONLY` / `ORIGIN_DENIED` / `SHOP_NOT_FOUND` / `CLOCK_BACKWARDS` / `NUMERIC_LIMIT` / `BODY_TOO_LARGE` / `JSON_REQUIRED` / `SAVE_FAILED`），新增：

| HTTP | code | 触发条件 |
| --- | --- | --- |
| 409 | `SLOT_LOCKED` | 目标铺位尚未解锁 |
| 409 | `SLOT_ORDER` | 试图跳过顺序解锁二层铺位 |
| 409 | `INSUFFICIENT_COINS` | 金币不足 |
| ~~409~~ | ~~`ALREADY_UNLOCKED`~~ | **不实现**（C1-1 拍板：重复解锁为**幂等成功**，返回 `changed=false`，不重复扣费、不写存档） |
| 409 | `MAX_LEVEL` | 已满级仍请求升级 |

### B4.3 好友排行榜

排行榜**不需要新增服务端接口**：

1. 客户端从 `/api/v1/mall` 拿到服务端权威金币；
2. 主域调用微信托管数据接口上报自己的金币；
3. 开放数据域读取好友数据，在子域内渲染排行榜。

> 注意：开放数据域是独立沙箱，不能访问主域的资源与节点；好友相关能力需要用户授权，**具体授权入口与限制必须在动手前核对官方最新文档**，不要凭记忆实现。

## B5 服务端需要的变更

M01 已具备：规则配置校验、开店、结算、客流轮询、业务日刷新、时间余量结转、原子存档、仅本机访问、令牌认证、幂等与并发保护。

需要扩展的部分：

### B5.1 规则配置（v1 → v2）

> ✅ **M02 实现状态**：已实现。`config/development.json` 的 `rulesVersion = m02-v2`，`dailyVisitors` 已删除，`shops[].coinsPerVisitor` 已改为 `shops[].levelCoinsPerVisitor`，`slots[]` / `fullFloorBonus` / `upgradeCosts` 已加入，`shops` 数组顺序已改为**一层临街优先**（轮询顺序由数组顺序决定，未另写一份顺序配置）。数值全部照抄《系统-经营与成长》§3.1，**未做任何取舍**。

- 新增每铺位解锁价格、解锁顺序。
- 新增店铺等级单价表（5 店 × 5 级，整数）。
- 新增满铺加成值（固定 +1）。
- 把 `dailyVisitors` 从固定值改为 `基础客流 + 单店客流`。
- 一句话：**调数值只改配置，不改代码**（M01 已支持这条）。

### B5.2 存档 schema（v1 → v2）

> ✅ **M02 实现状态**：已实现，`schemaVersion = 2`。除下表字段外，另新增 `capFrozen`（bool，冻结状态的**唯一判据**，与 `dailyVisitorCap` 配对）、`shops[].segments[]` 与 `spent`。`unlockedSlots` 按 `unlockOrder` 升序、同序按 config 数组顺序**规范化写入**；加载时按**集合语义**校验（非规范顺序本身不是损坏，加载后被规范化但不重写文件）。未开业店铺的 `segments` 为 `null`。

新增字段：

| 字段 | 说明 |
| --- | --- |
| `unlockedSlots[]` | 已解锁的铺位 ID |
| `dailyVisitorCap` | 当日客流上限。在**当天首次实际产生客人的结算**（`openShopCount ≥ 1`）时确定后**写死当日值**（否则当天中途开店会改变当日上限）；**0 店开业时不冻结、不落盘**（「未冻结」的表示见《系统-经营与成长》§5.7） |
| `spent` | 累计支出（解锁 + 升级） |
| `shops[].level` | 店铺等级 1–5 |
| `shops[].segments[]` | 账单分段，见下 |

**账单分段（推荐方案 A）**

把每家店的累计收益改为分段记录：单价每变化一次就新开一段。单价变化只有两个触发点——升级、所在层满铺状态变化——所以每店最多几段，总量可控。

> ⚠️ **现行口径（N-4 加注，2026-09-14）**：本节为**历史原貌，未重写**。分段触发点**现行口径以《系统-经营与成长》§4.3 为准，为三个**——新增「**开店（`prepare`）建立首个分段**」（开店不改变单价数值，但为该店分段的起点），故每店分段数上界为 **1（开店）+ 4（升级）+ 1（满铺）= 6 段/店**（见该 GDD §4.3 / §5.6）。本文「两个触发点」为草案写作时的旧表述。

```
shops[i].segments = [ { unitPrice, visitors, revenue }, ... ]
```

校验时对每一段要求 `revenue == visitors × unitPrice`，仍然保持 M01 那种"存档可自证"的强校验。

- 方案 B（更省事）：不存分段，只校验金币恒等式 `coins == initialCoins + Σrevenue − spent`，不做逐笔可追溯。
- **推荐方案 A**：M01 已经把"存档能自证账目"当成设计基线，放弃它会让一份被改过的存档无法被发现，而分段方案的实现成本只是每次改价时 append 一条。

### B5.3 校验规则调整

> ✅ **M02 实现状态**：本表 5 行全部实现，另补 3 项：**金币下界放宽为 `coins >= 0`**（M01 的 `coins >= initialCoins` 在 v2 会误杀合法存档——玩家一解锁铺位金币即低于初始值，存档被判定损坏、拒绝加载；《系统-经营与成长》§5.5 已明确要求放宽）、**`unlockedSlots` 加载校验**（未知 / 重复 / 数量越界 / 缺开局铺位 / 跳序 ⇒ 存档损坏，§3.7）、**店铺聚合与分段一致**（`visitors == Σsegments[].visitors`、`revenue == Σsegments[].revenue`、`prepared == (len(segments) > 0)`）。另：**支出溢出护栏不可达故未实现**——金币恒等式 `coins == initialCoins + Σrevenue − spent` 与 `coins >= 0` 合起来已保证 `spent + cost ≤ initialCoins + Σrevenue ≤ maxSafeInteger`。

| 规则 | M01 | v2 |
| --- | --- | --- |
| 金币恒等式 | `coins == initialCoins + Σrevenue` | `coins == initialCoins + Σrevenue − spent` |
| 每店账目 | `revenue == visitors × 固定单价` | 逐段 `revenue == visitors × unitPrice` |
| 未开业店铺 | 不得有累计客流 | **保持不变**（2026-09-14 修正）。原计划"放宽"，但手动关店已移除，`prepared` 变成单调递增（只可能 false→true），这条不变量继续成立，**不需要放宽** |
| 客流上限 | 固定配置值 | 二态校验（见《系统-经营与成长》§5.5 / §5.7）：**未冻结**（`capFrozen == false`）⇒ `dailyVisitorCap == 0`（占位值，读取方忽略）；**已冻结**（`capFrozen == true`）⇒ `dailyVisitorCap` 落在合法档位集合 `{基础 20 + 单店 15 × k \| k ∈ [1,5]}` |
| 轮询游标 | 指向已准备店铺 | 基本不变。移除关店后 `prepared` 单调，且「未解锁 ⇒ 未准备」，原逻辑已覆盖 |

### B5.4 版本兼容

以上变更会改变规则指纹。按 M01 现有机制，**旧存档会被拒绝加载**——这是预期行为而不是 bug。上线前没有真实用户，开发期直接删除旧存档文件即可，不需要写迁移工具。

> ✅ **M02 实现状态**：已实现，并已端到端验证——把 `schemaVersion = 1` 的旧存档放进 `MALL_DATA_PATH` 后，进程以非零码退出、日志给出 `invalid save; original left untouched: schema or rules changed; use a separate development save`，**原文件未被改写**。指纹覆盖**整个 `Config` 结构体**（ADR 0004），`capFrozen` / `dailyVisitorCap` / `unlockedSlots` / `level` / `spent` / `segments` 是存档字段、不在 config 内，不会污染指纹。

### B5.5 微信登录与云端存档（M07）

- `code2session` 换取 openid 与 session_key，签发会话令牌替代当前的本地开发令牌。
- M01 的本地开发令牌与仅本机访问限制**必须在上线前彻底关掉**，不能带到线上。
- 会话令牌要有过期时间；同一账号的写操作由 M01 已有的互斥锁串行化。

### B5.6 日志与可观测

> ⚠️ **M02 实现状态（部分）**：日志仍只记录**方法、路径、耗时**（M01 形态），**未记录结果码**——本节未标注所属模块、也不在 M02 的交付物与 11 条验收标准内，实现结果码需要改动 M01 稳定的中间件（包装 `http.ResponseWriter`），故**未擅自扩围**，原样上报给用户决定归属。令牌与查询参数不落日志这一条**已实现并有测试覆盖**（`api.TestRequestLogDoesNotLeakSecrets`）。

- 结算、解锁、升级三类写操作都要有可追踪日志（方法、路径、耗时、结果码）。
- 日志**不记录令牌、查询参数与个人信息**（M01 已实现并有测试覆盖）。

## B6 存档与容错

| 场景 | 处理 |
| --- | --- |
| 写入过程中断电 | 先写临时文件、`fsync` 后原子替换，不会出现半个存档（M01 已实现） |
| 存档损坏 | 拒绝启动并保留原文件，不静默重置（M01 已实现） |
| 规则版本变化 | 拒绝加载旧存档（M01 已实现） |
| 服务器时钟回退 | 拒绝结算，返回 `CLOCK_BACKWARDS`（M01 已实现） |
| 重复提交 | 开店天然幂等；解锁/升级靠"目标状态已达成则直接返回成功"实现幂等 |
| 数值溢出 | 触及安全整数上限时返回 `NUMERIC_LIMIT` 且不写入部分结果（M01 已实现） |

## B7 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 主包体积超限 | 无法提审或首屏加载慢 | 美术走分包 + 压缩，UI 合图集，立绘按需加载；体积上限动手前核对官方文档 |
| 好友排行授权受限 | 排行榜做不出来 | 排在最后做，动手前核对开放数据域文档；准备"只显示自己分数"的降级方案 |
| 素材全是 JPG 假透明 | 无法直接进引擎 | 统一走抠图流水线转透明 PNG，作为独立任务先做 |
| 缺设计图的页面被抢跑 | 返工 | 布局/经营/好友三页在补图前不动工 |
| 数值手感受质疑 | 中期大改 | 数值全部走配置；改配置不改代码 |
| 存档校验强度下降 | 存档被改不容易发现 | 采用分段强校验（方案 A），保留 M01 的自证基线 |
| 上线时忘记关掉本地开发令牌 | 安全漏洞 | 把"关闭本地开发令牌与仅本机限制"列进上线检查清单（《开发规划文档》M09） |
