# M03 素材投喂包（外部生成件）

> **用途**：本文档是**投喂操作清单**——供逐件生成、交接与进度跟踪。**13 件素材全部入库后本文档即废弃**，不作为长期规格引用。
> **规格与提示词的权威源**：`design/art-bible/资产基准模板与生成提示词.md`（§1.7 生成前检查清单 / §1.8.3 逐资产验收口径 / §3.1–§3.10.5 提示词正文）与 `design/art-bible/美术圣经.md`（§5.1 命名 / §5.3 逐资产规格）。
> **本文件不复制提示词正文**（避免"同一事实两处维护"）。**若本表与上述权威源冲突，以权威源为准，并回报冲突。**
> **版本**：v1.0 · 2026-09-15 · 由 M03 投喂流程建立

---

## 1. 逐件清单（共 13 件）

> 生成比例 = 建议出图比例（§3 各节标注）；交付尺寸 = 管线降采样后的最终像素尺寸。
> 体积目标为**运行时 ASTC 形态**（含 mipmap），源 PNG 只约束像素尺寸、不设 KB 上限。

| # | 资产名 | 生成比例 | 交付尺寸 | 体积目标 | 提示词落点 |
| --- | --- | --- | --- | --- | --- |
| 1 | `bg_mall_base` | 9:16 | 1125×2000 | ≤ 768 KiB | §3.1 |
| 2 | `slt_f2_empty` | 1:1 | 512×640 | ≤ 208 KiB | §3.2 |
| 3 | `slt_f1_empty` | 1:1 | 704×1024 | ≤ 448 KiB | §3.3 |
| 4 | `shop_clothing_open` | 1:1 | 512×640 | ≤ 208 KiB | §3.4 |
| 5 | `shop_dessert_open` | 1:1 | 512×640 | ≤ 208 KiB | §3.5 |
| 6 | `shop_bookstore_open` | 1:1 | 512×640 | ≤ 208 KiB | §3.6 |
| 7 | `shop_coffee_open` | 1:1 | 640×768 ⚠️ | ≤ 304 KiB | §3.7 |
| 8 | `shop_flowers_open` | 1:1 | 640×768 ⚠️ | ≤ 304 KiB | §3.8 |
| 9 | `prp_lights` | 3:1 | 384×128 | ≤ 24 KiB | §3.10.4 |
| 10 | `prp_planter` | 1:1 | 256×256 | ≤ 32 KiB | §3.10.4 |
| 11 | `app_icon` | 1:1 | 512×512 | ≤ 128 KiB | §3.10.5 |
| 12 | `app_share` | 5:4 | 500×400 | ≤ 96 KiB | §3.10.5 |
| 13 | `ui_hud_bar`（5 槽） | 4:1 | 1024×256（九宫格） | ≤ 128 KiB | §3.10.2 |

> 体积目标均为**运行时 ASTC 形态**：按《美术圣经》§5.3.0 算式 `⌈宽÷块边长⌉ × ⌈高÷块边长⌉ × 16 B`，**背景 / 立绘 / 空铺底图**开 mipmap（再 ×1.33），道具 / UI / 图标 / 应用级不开。**源 PNG 不设 KB 上限。**
> ⚠️ **第 7 / 8 项的交付尺寸存在文档冲突**——见 §4。**该冲突不影响投喂**：两套口径的出图比例都是 **1:1**，差异只在入库时的目标像素尺寸。

---

## 2. 通用红线（每件生成前逐条确认，生成后逐条打勾）

> 完整版见 `资产基准模板与生成提示词.md` §1.7（A1–A14 + B/C/D/E 分表）。以下为**最容易翻车的 7 条**：

1. **背景 = 单一纯品红 `#FF00FF`**，无渐变、无纹理、无阴影。出现渐变或杂色 → **直接重出**，不要硬抠（硬抠必然毛边）。
2. **画面内无任何文字 / 汉字 / 字母 / 数字 / logo / 水印**（招牌板、黑板、书脊、花器上都不许有）。
3. **正视图平视**：立柱垂直线互相平行，可见侧面 ≤ 宽度 8%。出现斜视 / 俯仰 / 等距 → 重出。
4. **光源左上 30–45°，影落右下**，只有接触影，不画长投影。
5. **不用棋盘格表示透明**（这是本轮要根除的老问题）。
6. **咖啡立绘严格单层**（不得出现上方住宅楼层）。
7. **金币类图标保留 `$`**（唯一允许的货币符号例外；其余图标内不得有任何字符）。

---

## 3. 交付方式（给我什么、怎么给）

| 项 | 要求 | 原因 |
| --- | --- | --- |
| **格式** | **PNG（必须）**。不要 JPG | JPEG 有损压缩会在纯品红背景上产生噪点与色偏，直接导致键色抠像毛边、边缘残留 |
| **分辨率** | 给**原始高分辨率图**（短边 ≥ 1024），**不要自行缩放到交付尺寸** | 缩放与裁切由管线统一做，先缩后裁会损失质量 |
| **候选** | 每件 **3–4 个候选**，命名加后缀区分（如 `bg_mall_base_c1.png`） | AI 生图有随机性，一次命中不现实 |
| **文件名** | 直接用资产名（`<资产名>.png` / `<资产名>_c1.png`） | 便于管线自动识别目标尺寸 |
| **放置位置** | 建议 `D:\lingou-codex\game-operation\.work\incoming\`（该目录已在 `.gitignore` 内） | 避免污染 `git status` |
| **交付动作** | 告诉我路径即可，我跑管线（抠像 → 裁切 → 降采样 → 自检 → 入库 → 更新处理记录） | — |

---

## 4. 待裁定（投喂前需你确认）

| # | 冲突 | 两种口径 | 影响与暂定处置 |
| --- | --- | --- | --- |
| 1 | **一层立绘交付尺寸** | **A. 640×768**：`美术圣经` §5.3.2 + `资产基准模板` §3.10.1 + `素材处理记录` §3.1（三处）<br>**B. 704×1024 / 640×1024**：`资产基准模板` §1.2 交付尺寸对照表 + §3.7 / §3.8 提示词（三处） | **暂按 A**——依据 `开发规划文档` §五 指定「素材的权威源是 `美术圣经` §5.3 与 `素材处理记录`」。⚠️ 附带疑点：`美术圣经` §5.3.2 自身「显示 290×420」（比例 0.690）与「出图 640×768」（比例 0.833）**不自洽**；而 `资产基准模板` §1.2 的「345×493」（0.700）与 704×1024（0.688）**自洽**——故 B 在比例自洽性上更强。**不阻塞投喂**（两套口径的出图比例同为 1:1） |
| 2 | **外部生成件计数** | 逐件展开为 **13 件**；`开发规划文档` §5.1 与 `素材处理记录` §9.3 写「12 件」；`资产基准模板` §3.10.1 写「9 项」 | 影响进度口径与 M03 完成判定。**本清单与入库管线按逐件展开的 13 件执行** |

---

## 5. 进度跟踪

| # | 资产名 | 已投喂 | 已入库 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | `bg_mall_base` | ☐ | ☐ | |
| 2 | `slt_f2_empty` | ☐ | ☐ | |
| 3 | `slt_f1_empty` | ☐ | ☐ | |
| 4 | `shop_clothing_open` | ☐ | ☐ | |
| 5 | `shop_dessert_open` | ☐ | ☐ | |
| 6 | `shop_bookstore_open` | ☐ | ☐ | |
| 7 | `shop_coffee_open` | ☐ | ☐ | 尺寸待裁定 |
| 8 | `shop_flowers_open` | ☐ | ☐ | 尺寸待裁定 |
| 9 | `prp_lights` | ☐ | ☐ | |
| 10 | `prp_planter` | ☐ | ☐ | |
| 11 | `app_icon` | ☐ | ☐ | |
| 12 | `app_share` | ☐ | ☐ | |
| 13 | `ui_hud_bar`（5 槽） | ☐ | ☐ | 九宫格切线由工程侧标定 |

---

> **入库判据**：每件须过 §1.8.3 的客观验收口径（正视图 / 光源 / 无文字 / 无棋盘格 / 描线色 / 色板 / 安全区 / 单层 / 等高 / 圆章 / 九宫格可拉伸 / 体积），任一不满足即退回重出。**验收记录写入 `assets/素材处理记录.md` 的逐资产表**，不为每张图单独建文件。
